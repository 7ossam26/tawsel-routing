extends:
  - minimal
rules:
  struct: error
  no-unresolved-refs: error
  no-unused-components: off
  # P07 has local session paths but no published production deployment URL.
  no-empty-servers: off
