# Tawsel — Discovery and Decision Log

Updated: 21 September 2026

Status: operational discovery consolidated; stack, meaningful Vitest and very simple driver UX accepted. D-109 requires smaller self-contained tasks. D-110 adds the recommended Codex model/reasoning setting per phase; D-111 requires concrete ERP planning/integration handoff files and explicit responsible phases; D-112 locks the ERP-agnostic public-contract boundary and connector ownership. Master-plan revision 7 and package revision 3 contain 42 prompts, 65 requirement groups and D-01–D-112 traceability. The prior 11-phase package is archived. Phases 01–02 are implemented and locally verified within their workspace/schema foundation scope; Phase 03 is specified and document-verified with owner review pending (docs/phase-03-evidence.md); P04 development UI fixtures and the P05 PostgreSQL command foundation are locally verified, with evidence in docs/phase-04-evidence.md and docs/phase-05-evidence.md. P06 tenant/membership/capability/resource enforcement is locally verified with labelled principals (docs/phase-06-evidence.md); P07 real login, recovery and separate sessions are locally verified (docs/phase-07-evidence.md). P08 ERP provisioning and issuer reconciliation are locally verified (docs/phase-08-evidence.md). Phases 09–42 remain unstarted. Session/account and P08 provisioning APIs are available locally; delivery domain APIs/events remain designed.

## 1. Sources and precedence

1. The owner's latest instruction asks for a Codex model in every phase (D-110) and concrete assurance about files usable for future real ERP planning/integration, including which phases produce them (D-111). D-109 retains smaller complete prompts/checkpoints; D-108 authorizes preparation. D-102–D-107 remain binding: screens are visual references; decisions define operations; shadcn/ui + Smooth UI, meaningful connected Vitest, very simple UX and the accepted stack.
2. The user's comprehensive discovery prompt supplied on 19 September 2026, including the Stitch inventory, V1 operational tracking, transactional correctness, and reliable ERP synchronization.
3. The user's ten answer rounds on 20–21 September 2026. Rounds 9/10 preserve actual ERP branch receipt/disposition, bounded driver self-correction, manual address/pin entry and in-app reports with Excel export. D-101 closes the final urgency clarification: ERP may set urgency before departure; afterward only the assigned driver may change it. Eligible urgent work follows the protected current target and precedes ordinary work. Sections 27–30 preserve these decisions. The engineering baseline is in master-plan.md, with phase preparation authorized by D-108; do not restart settled discovery.
4. Attached `STACK-CONTEXT(2).md` and `TAWSEL-ENGINE-CONTEXT(1).md`, both read in full and verified byte-for-byte equal to their repository counterparts during the 19 September inspection.
5. Repository `7ossam26/tawsel-routing`: historical inspection at `62d9d4610c38446818e2abc715095c218d9c2f51`; refreshed repository/configuration and nine-screen source/visual assessment on 21 September at `3d6291697fb0baeb69215237bf1d09dbf6d1d9cd`. See docs/planning/repository-and-ui-assessment.md for observed evidence and limitations.

The retained context preserves architecture/discovery history. D-108 authorizes preparation, D-109 governs granularity/completeness, D-110/D-111 add model guidance and explicit ERP handoff proof, and D-112 clarifies the existing integration boundary; they do not retroactively approve every historical assistant suggestion or establish implementation. Earlier suggestions do not become decisions merely through silence. Use the decision map to avoid restoring superseded behavior.

## 2. Existing scope retained

- Engine means Nominatim, OSRM, VROOM, and their core configuration/integration.
- Extend only `tawsel-routing` with the shared delivery frontend, backend, application database, contracts, and deployment configuration.
- Preserve existing Engine data, volumes, paths, and working setup. Ordinary application releases must not trigger data import or preprocessing.
- A centrally maintained tenant-aware application serves B2B companies and independent B2C drivers. B2C is a lightweight routing/delivery aid used independently of the driver's employer systems, with manually entered shipments/locations and no branch-return or inventory/custody workflow. Simple outcomes, optional collection and daily reporting are retained. Round 8 excludes item-level partial delivery from initial B2C; do not copy the B2B return journey. Company and B2C accounts are separate, with logout/login to change accounts, not linked workspaces in one account. Billing is explicitly deferred until after the product owner tries and corrects the basic app. Do not add subscription gates, billing tables, pricing/payment UI, or paid activation to the current foundation. Record a future extension only; this does not decide that the eventual public product must remain free.
- B2B users may be assigned multiple branches. The same effective role/user permissions apply across every assigned branch; no per-branch capability overrides. Branch membership and own-driver assignment remain resource restrictions. Single-branch-only access was an intermediate decision and is superseded.
- In B2B, each ERP remains a separate project with its own database and server-side connector. It owns commercial orders, customer master, finance, settlement, inventory, and business rules. Tawsel owns the accepted delivery execution state, tasks, trips, assignments, route revisions, attempts, and progress.
- B2B shipment selection and initial driver assignment happen in the ERP. Definitive assignment is the ERP's assertion that the driver received the shipment; Tawsel does not independently prove physical possession. Prepared-for-driver work remains distinct from received/on-board work. Receipt triggers re-optimization. Before departure, staff may remove a shipment and assign it to another driver through an ordinary ERP action, without mandatory reasons or additional handover confirmations. The driver prepares/starts without an extra dispatcher route approval. Shared planning/map/monitoring stays in Tawsel; screens 3/6 must not duplicate ERP assignment administration.
- Drivers use the delivery PWA directly; React Native and Expo remain later work.
- Preassigned areas/tasks, no initial global fleet assignment, and external next-stop navigation remain the baseline. The accepted limit is 50 remaining stops in the route being planned, including planned branch-service visits. Reject an incoming over-limit addition batch atomically without assigning part, splitting automatically, or retaining rejected work as a driver-owned backlog. This is not a daily shipment cap. Every shipment stays separate even at an identical recipient/pin; no automatic consolidation. Default service time is ten minutes per customer. The three existing vehicle modes are car, motorcycle, and bicycle. Default route end is the last customer, with an explicit branch visit or optional B2C fixed endpoint when selected.
- Re-optimization requires server connectivity. A roughly 24-hour offline support target is accepted for already-started downloaded work and queued actions. Before a new round begins and before departure to its first stop, synchronization and a server-confirmed start require connectivity. Pending actions are not deleted at 24 hours. Deliberate logout/account switching is blocked while actions are still unsynchronized; closing/reopening an already-started round is separate from starting a new one.
- V1 includes server-confirmed operational tracking and durable integration synchronization. GPS collection, live driver markers, background tracking, and telemetry storage remain deferred to V2/V3. Learning across companies remains deferred to V3 or later.
- V1 planning origins use the last driver-confirmed stop with optional manual map-pin correction, as accepted on 21 September. This is a planning input, not a live position or measured GPS accuracy. Deferral is an earliest permitted date/time, not a narrow appointment window.
- In B2B, a return-required shipment stays with the driver until a branch handover is confirmed or a distinct authorized operational disposition is recorded. Returns go only to the originating branch. The driver requests handover; staff confirm received items in their native ERP screen. The approved branch interruption pauses heading-to work and requires resolving an already handled customer attempt. Round 7 rejects a whole-batch clearance/quota gate: confirm the items actually received, handle other statuses in ERP, and permit continuation without requiring every offered item to be settled. Items claimed as handed over still require actual confirmation; do not fabricate receipt of the remainder. ERP owns inventory and settlement; Tawsel receives the relevant confirmed operational status. This return clarification does not remove the separate 50-stop route limit. Round 9 confirms actual branch receipt/disposition as a narrow authorized boundary; general staff changes to departed execution remain prohibited.
- An ERP-initiated cancellation-request workflow during delivery is not wanted. Staff contact the driver outside the application; the driver records a return outcome. Pre-start removal/correction is a simple authorized ERP operation, with no additional mandatory reason/receipt procedure. Do not restore the rejected cancellation protocol or treat a phone call as a committed execution state.
- B2C recipient phone is mandatory; recipient name and phone must remain easy to see during execution. Number tap opens calling, the WhatsApp icon opens the requested direct chat link, and the recipient name can open existing stop details. External-app activation is not proof of a call outcome or sent message.
- All call counters, three-call limits, resets, and counter-specific controls are cancelled. Retain the simple `No answer` outcome and ordinary automatic execution history. In B2B, no-answer work leaves the active delivery sequence and is intended for return while still held. Round 7 approves the same driver's explicit retry of an untouched whole shipment before source-branch receipt, with re-optimization and preserved history. After confirmed receipt, use ERP re-dispatch; rejected partial remainders remain excluded. Calls still do not prove travel, arrival, or a physical visit.
- On B2B complete refusal, collect the supplied shipping fee; rare refusal to pay that fee is explicitly recordable, while merchant liability/settlement remains ERP-owned. This is a narrow exception, not permission for arbitrary underpayment. Ordinary/partial delivery still requires the authorized amount for the goods accepted and applicable shipping. Contents/prices cannot change after departure.
- The approximate online freshness targets are now accepted: three seconds for the shared Tawsel view and five seconds for a healthy ERP projection after Tawsel commit. Conditions, percentiles, measured load capacity, and recovery checks must be specified and verified. Small pilot numbers are not a system capacity limit.
- Local ACID transactions protect each application's own database. Outbox/inbox delivery, deduplication, revision checks, and recovery support eventual consistency between applications. No global ACID transaction or exactly-once network guarantee is promised.
- A clearly identified mock ERP/receiver demonstrates the real integration boundary now; this project does not implement the actual shipping ERP. Round 7 selects a real B2C pilot with separate B2B mock scenarios. Production B2C intake is a separate product path, not a mock shortcut.
- Reports use an explicit workday from the first round to `End day`, even across midnight, with actual Egypt-local timestamps. Still-held unfinished work carries forward without ERP re-entry, respecting deferrals. Staff cannot edit departed execution outcomes. Round 9 accepts B2B driver self-correction during the open workday before dependent branch receipt/re-dispatch, preserving history and valid quantities/amounts. Actual branch receipt and ERP-owned loss/damage handling remain distinct authorized actions. B2C self-correction remains accepted. Provide both in-app reports and Excel export with the same authorized data and filters.
- Show estimated completion without a hard shift-end cutoff. Round 8 accepts expected-versus-actual timing for each stop and the whole round, retaining the start forecast and identifiable revised forecasts. Actuals come from existing recorded actions; missing times remain unavailable, with no extra timing buttons, inferred GPS motion or substitution of delayed server receipt time. This is V1 operational reporting, separate from deferred predictive learning.
- If optimization is unavailable, keep the last valid remaining order and usable outcome controls; the driver may explicitly choose eligible assigned work or use an honest manually ordered first route. Show manual/re-optimization-pending status and protect an already selected current target until explicitly changed or resolved.
- Pilot deployment target is Hostinger KVM 2 managed with Dokploy. Android/Chrome and iPhone/Safari PWA coverage is accepted, subject to actual device/browser verification. No current server capacity measurement or deployed-version claim follows from that choice.
- B2C login remains phone/password, with verified email for recovery only. B2B uses one company identity/credential authority with an independent Tawsel session and ERP-managed users/roles. Exact federation/provisioning mechanics are engineering work still to specify. Multiple phones may view the same active round. Execution normally belongs to the starting phone and may be transferred explicitly online to another phone without approval from an unavailable old device. Preserve old queued evidence; prevent duplicate rounds and stale-device overwrites.
- Prioritize quick, basic operational journeys. Do not require a button, justification, or dual confirmation for each ordinary physical movement. Original transactional safety, automatic audit, and reliable synchronization remain backend responsibilities, not extra staff procedures. The user permits larger discovery batches where useful, with a recommendation per question. The master-plan review and subsequent D-108 instruction now authorize producing executable implementation phase prompts.

- Both ERP and driver may mark urgency. D-101 restricts ERP changes to before departure; afterward only the assigned driver may change it, including in response to a customer's request. Eligible urgent shipments are served before ordinary remaining work after the protected current target, optimizing within the urgent group and respecting earliest dates/times. This may increase distance and is not a hard arrival-time guarantee.

## 3. Repository and Stitch evidence from 19 September

Reviewed the recursive repository tree, README, Compose configuration, startup script, VROOM configuration, example optimizer request, and relevant motorcycle-profile settings. No repository AGENTS.md or DESIGN.md was found in the inspected tree. The repository contains Engine configuration, context documents, and static UI exports; no application backend, application schema, implemented authorization, or released application contracts were found.

VROOM remains documented as a separately created container, not a Compose service. OSRM uses a `latest` image tag. Current port bindings and startup scripts describe local development, not a validated production deployment. The operational measurements in STACK-CONTEXT.md are historical reports from the user's Windows machine; no Engine runtime tests were performed here.

The repository's TAWSEL-NEW-CHAT-PROMPT.md predates the complete Stitch amendment pasted into this conversation. The user's pasted prompt governs this discovery.

`stitch-export/README.md` and `stitch-export/manifest.json` were read. For every screen below, the original image was visually inspected, the complete HTML structure/text and inline styles/scripts were reviewed, and metadata was inspected. All image and HTML hashes matched the manifest. These were source and visual inspections, not browser interaction tests.

| Screen | Directory under stitch-export/screens | Original screen ID | Image reviewed | HTML and metadata reviewed |
| --- | --- | --- | --- | --- |
| 1 Active driver trip | 01-active-driver-trip | 3d3f4e407ee74b11b2e047e0a61a6dfb | screen.jpg | Yes |
| 2 Stop details | 02-stop-details | c51dd41ed3f74ceab7226835d38b75df | screen.jpg | Yes |
| 3 Dispatcher workspace | 03-dispatcher-workspace | 71ae553e941a4312a5bef12d1da8712a | screen.jpg | Yes |
| 4 Login/workspace | 04-login-workspace | 4d692192b93241c2ac5792e97d1bd7cd | screen.png | Yes |
| 5 Driver daily trips | 05-driver-daily-trips | 0a46baa0b55645088f06fbba66bddfd0 | screen.png | Yes |
| 6 Route preparation | 06-route-preparation | c8ec135a704d4720a044cfcd263fe2df | screen.png | Yes |
| 7 Location review | 07-location-review | dff9a66b5a3c4fcdb44e27b31a2e421c | screen.png | Yes |
| 8 Trip completion | 08-trip-completion | 3c394f6e10df494fb82013f630a439d3 | screen.png | Yes |
| 9 Sync/conflicts | 09-sync-conflicts | b4cfe4329fb144cfbb83c314402bb943 | screen.png | Yes |

Material findings:

- Screens 1 and 2 are mobile layouts despite DESKTOP metadata and landscape captures. Screens 1–3 contain capture/clipping/layout defects; device frames and fixed capture dimensions must not define production layouts.
- Screen 3 has an effectively empty planning-controls area. Its scripts reference missing `monitoringSummary`, `btnOptimize`, and `draftNotice` elements. The selected layout needs functional completion.
- Screen 7's GPS age, measured accuracy, match-percentage, StreetView, and service-latency claims are prototype content, not supported product behavior. Screen 9's GPS breadcrumb queue is outside V1.
- COD, warehouse custody, rewards, barcode, and proof-of-delivery content are generated proposals. The user's latest answers reject advanced proof-of-delivery and request a basic COD/reporting subset; details remain open.
- Role/workspace selection in screen 4 cannot grant privileges. Its encryption, device-security, background-sync, and version copy are unverified.
- Delivered tasks, failed attempts, deferred work, and processed stops require distinct counters. A completed workday can contain unfinished tasks. Failed delivery does not prove physical warehouse return.
- The maps are static graphics/images, navigation links are prototypes, and alerts/timers simulate success. None constitute backend, synchronization, optimizer, or payment evidence.
- Recommended visual reconciliation remains Cairo, light surfaces, navy primary actions, blue accents, and separate semantic state colors. Preserve map/list relationships and role-appropriate navigation. This recommendation is not a user-approved token specification yet.
- External fonts, Material Symbols, Tailwind CDN, and hosted images require deliberate production handling. No production/PWA asset strategy has been implemented.

## 4. Confirmed or explicitly expressed in the user's first answers

| ID | User answer | Recorded intent | Boundary or unresolved detail |
| --- | --- | --- | --- |
| D-01 | 1, 3 | A customer company can have multiple branches. Its Company Admin creates staff, drivers, and dispatcher users in the ERP. | Do not assume duplicate company/user administration screens in Tawsel. Exact authentication/provisioning bridge remains open. |
| D-02 | 1 | Access must be flexible, using a permission matrix and configurable roles. Application behavior must not depend on hardcoded business-role names. | Stable operation/permission definitions still need implementation. Page-wide versus individual-action grants is open. |
| D-03 | 1 | Direct user-level access changes are required. A user may be permitted a distribution page regardless of job title and prevented from using it when that user's access is closed. | Exact inheritance, multiple-role combination, and explicit-deny precedence need confirmation. |
| D-04 | 1 | Originally requested multi-select branch assignment. Users act only within permitted branches. Drivers see their own daily work and ordering. | Temporarily changed to single branch by D-17, then explicitly restored to multiple branches with identical permissions by D-25. Company-wide administrator scope and staff transfers still need detail. |
| D-05 | 4 | Explicit trip start, heading-to-stop, and arrived actions are accepted. | The preceding answer also proposed one active trip at a time; the user's broad agreement should be reconciled with the newly clarified workday model before fixing schema constraints. |
| D-06 | 5 | When a customer asks for a later visit, the driver can trigger rescheduling and automatic route recalculation without dispatcher approval. | Earliest service time versus a narrow appointment window, impossible schedules, and repeat attempts remain open. |
| D-07 | 6 | A dispatcher can give a driver additional work during the day; the driver may return to collect it and continue. | Assignment is not evidence of physical collection. Collection readiness and route timing must be defined. |
| D-08 | 6 | The driver can end the day without completing every task. A daily report is required. | Disposition of unfinished tasks and the distinction between trip closure and day closure remain open. |
| D-09 | 7 | Re-optimization during execution should be automatic when relevant changes occur and must not routinely require dispatcher approval. | Triggers, current-stop preservation, source location, result application, concurrent changes, and failure behavior need rules. Initial publication authority remains open. |
| D-10 | 8 | The user expects offline operation to lose assistance features and asks whether its impact matters before live tracking. | This is a concern/question, not explicit approval to discard downloaded trips or queued outcomes. Offline execution conflicts still affect V1. |
| D-11 | 9 | Basic delivery outcomes and reasons/notes are sufficient. Advanced photo/signature/OTP/barcode proof is unnecessary in V1. COD-related expected money and a record/report of activity are wanted. | The user has not yet defined actual cash confirmation, partial/no collection, handovers, or cash-balance authority. Delivery must not imply payment. |
| D-12 | 10 | Monitoring should be faster than the proposed 10-second Tawsel / 30-second ERP targets, and the last update must be visible. | No numerical freshness SLO, pilot concurrency, or stale threshold is approved. |

## 5. Corrections to previous assistant assumptions

- Withdraw the suggestion of implementing a separate Company Admin account-management workflow in Tawsel as if already agreed. ERP user management is explicit; Tawsel still needs secure execution sessions and authorization enforcement.
- Treat the names Company Admin, Dispatcher, and Driver as descriptive roles/personas, not hardcoded authorization conditions.
- Do not interpret access to an ERP page as sufficient evidence for a Tawsel API request. The integration must carry verified identity and delegated delivery permissions, tenant/branch scope, and applicable assignment context.
- Dispatcher approval for ordinary driver re-optimization is contrary to the latest requested behavior.
- A rule requiring all daily work to be processed before ending the day is contrary to the latest requested behavior. Trip and workday closure must be separated during discovery.
- Manual task intake and CSV import were previously suggestions. The latest answers now approve driver-entered shipments for the independent B2C path. They do not approve a second B2B assignment/intake administration module or CSV import.
- Username/password and the previous monitoring targets were suggestions, not approved requirements. Account creation in the ERP is confirmed; login mechanics are not.
- The prototype's cash settlement, financial custody calculations, rewards, and advanced delivery proof remain outside the approved V1 subset. A minimal operational return-to-branch handover is now explicitly requested; distinguish it from ERP inventory/warehouse management and financial settlement.
- The latest assignment-as-receipt clarification overrides a mandatory second pickup confirmation in Tawsel for the same definitive B2B assignment. Future prepared work and pre-start removal still need distinct, honest semantics.
- A shared B2B/B2C account with workspace switching was recommended but rejected. Use separate accounts with logout/login. Billing is a future extension beyond the basic application and user testing.
- The proposed ERP cancellation-request/driver-acknowledgment workflow was declined. Preserve its history below, but do not include it as a required active V1 workflow. After-dispatch instructions to stop are communicated outside the app and recorded by the driver as return-required.

## 6. Current synthesis and recommendations — updated through D-109

Sections 11–30 preserve the answered discovery rounds and historical proposals; D-101 resolves urgency. Sections 31/32 record visual/component, Vitest/UX and stack decisions. Section 33 records the first package; section 34 supersedes its division with 42 bounded prompts and explicit requirement/decision coverage. master-plan.md revision 5 is the current baseline. A recommendation is not approved merely because it appears here; labelled engineering proposals still require specification and evidence.

### Identity, modes, and permissions

B2B user/role administration belongs to the ERP. One configurable role per user supplies inherited permissions, with identical effective capabilities across assigned branches. Round 8 confirms direct inherit/allow/deny exceptions: an explicit user exception overrides the inherited permission, but never bypasses tenant/branch/integration/assignment restrictions or legal execution-state transitions. Role changes propagate rather than leaving copied stale grants. No hardcoded business-role-name checks, duplicate ERP role editor, or permission that silently restores the newly prohibited staff post-departure edits.

Company code plus username/password remains the B2B entry UX, using the same company identity and credential authority as ERP with an independent Tawsel session. B2C keeps phone/password login and verified email for recovery only. Federation/provisioning, revocation and session mechanics remain to specify without copying passwords between databases. Round 8 accepts second-device viewing and explicit online execution transfer without approval from an unavailable old phone. The active round is shared; concurrent execution is not the approved default. Delayed evidence from the old phone is preserved and revalidated, not erased or blindly applied.

B2C is an independent delivery/routing aid with required recipient phone, simple outcomes, an optional collection amount and a daily report. Round 8 explicitly excludes item-level quantities/prices and partial delivery from the initial B2C experience; those remain B2B features. No branch-return/receiver/custody process or current billing. The first real-user pilot is B2C, with separately labelled B2B mock tests.

### Admission, planning, and workday

Each shipment remains independent. ERP owns selection and assignment; prepared work precedes actual receipt, which triggers optimization. Simple pre-start removal/reassignment requires no additional staff procedure. The accepted 50-stop limit applies to remaining stops in the route being planned, including planned branch-service stops. Reject a new over-limit addition batch atomically, without partial/phantom assignment or automatic splitting. It is not a daily limit. Reserve clear handling for reactivation or a branch interruption at capacity; do not let bookkeeping prevent recording a physical outcome.

The driver prepares/starts without a second dispatcher approval. Round 7 explicitly requires connectivity and successful synchronization before starting a new round and moving to its first stop. Validate the latest authoritative list and start atomically; network availability alone is not confirmation. Already-started downloaded work can continue offline. Acceptance, optimization, activation, and execution remain separate. An Engine outage does not undo a committed outcome or receipt and does not cancel the approved manual-first-route fallback when the application server is reachable.

Manual continuation is now accepted when optimization fails: retain the last valid remaining sequence, permit explicit selection of eligible assigned work, and allow an honest manually ordered first route. Show manual/pending planning status; do not imply a successful optimization. Preserve the current heading/arrived target until explicitly changed/resolved. Async results must validate source revisions and must not overwrite a later manual decision.

Use all three existing modes: car, motorcycle, bicycle. Default customer service time is ten minutes, replacing the five-minute proposal. Routes normally finish at the last customer; explicit branch visits and a selected B2C fixed endpoint are supported. Fifty ten-minute customer stops already represent 500 minutes of service before travel, so 50 is an admission limit rather than a promise to finish them in one shift. Round 7 selects estimates without a hard shift-end constraint and requires expected-versus-actual reporting. Forecast baselines must survive re-optimization; user-recorded actions are actuals with known limits, not measured traffic or GPS dwell times.

In-app reporting and authorized Excel export are accepted. Daily reporting follows a workday from the first round to `End day`, even after midnight, with actual Egypt-local timestamps. Carry still-held unfinished work into the next workday without ERP resubmission; respect existing deferral dates. Ending the day does not imply full success, physical return, or settlement.

### Contact, outcomes, corrections, and B2B returns

Remove all call counters, three-call limits, reset periods, and associated call-logging UI. Keep name/phone visibility, call/WhatsApp links, and the simple `No answer` outcome. Ordinary automatic action/attempt history remains necessary for execution correctness; do not turn it into an administrative call log. An external-app click is not proof of contact, movement, or arrival.

No-answer work leaves the active delivery sequence and is intended for return while still with the B2B driver. Round 7 approves the narrow retry action for an untouched whole shipment still held by that same driver, before confirmed source-branch receipt; preserve the failed outcome and validate route/assignment revisions and capacity. After confirmed receipt, use ordinary ERP re-dispatch. Do not revive rejected partial remainders, lost/damaged goods, or duplicate collection. Whole untouched shipments may still be deferred.

Contents/prices stay fixed after departure. Use supplied final unit prices and fees for B2B whole-piece partial delivery; no fractional quantities. Complete refusal normally collects the outstanding shipping fee; rare explicit refusal to pay the fee is recorded separately and settled by ERP. Do not infer that exception from no answer, double-charge prepaid money, or permit arbitrary underpayment for accepted goods. Include shipping collected on unsuccessful shipments in the report. Round 8 withdraws ERP staff authority to change dispatched shipment execution state, including the earlier proposed staff conflict/outcome correction. Round 9 accepts driver self-correction during the open workday before dependent branch receipt/re-dispatch, with preserved history and validated quantities/amounts. B2C self-correction remains accepted. The deposit answer does not specify commercial allocation rules: retain supplied due amounts and require explicit source allocation before any partial-prepaid calculation; no deposit/refund/settlement module is approved.

Returns go to the source branch. Round 9 explicitly preserves actual driver-requested branch receipt confirmation and ERP-owned loss/damage handling in native ERP/mock screens. Independent confirmed subsets do not require whole-batch clearance. This is a narrow authorized operational boundary, not general staff power to change outcomes, pins, amounts or held assignments after departure. Re-dispatch follows confirmed branch receipt; loss/damage resolution is distinct from physical receipt and stock availability. No extra acknowledgment or broad staff correction privilege is added.

### Offline, monitoring, and measured deployment

Retain the 24-hour offline target for already-started downloaded work and queued actions and the block on deliberate logout/account switching with unsynchronized work. New-round start requires successful online synchronization and server acceptance. The duration is not a deletion timer. Outcomes may be locally pending even when the device appears online; server and ERP confirmation require actual accepted responses/events. Capture time, commit order, and ERP application time remain distinct.

Accepted approximate healthy-path targets remain three seconds for Tawsel and five seconds for ERP after Tawsel commit. Choose measurable percentile/load conditions, monotonic revisions, recovery snapshots, durable outbox/inbox, and visible stale/last-refresh states. An idle driver is not automatically offline.

Pilot deployment is Hostinger KVM 2 with Dokploy. Hostinger's public specification inspected on 21 September is 2 vCPU, 8 GB RAM, 100 GB NVMe; this is not a measurement or confirmation of the user's allocated/free resources. Engine co-location, existing applications, dataset runtime memory, and build/import overhead remain unknown. Test headroom beyond the nominal five-driver pilot, measure Engine jobs separately from application freshness, and use bounded work/efficient reads. Do not claim that all three routing instances, geocoder, databases, tiles, workers, and builds fit safely before measuring. Preserve Engine data and avoid routine imports/rebuilds. Android and iPhone PWA coverage is accepted; actual devices and browser versions still require validation.

Urgency may be set by ERP before departure and by the assigned driver during execution, as clarified by D-101. Eligible urgent work precedes ordinary remaining stops after the protected current target, with earliest times respected and optimization within the urgent group. Manual address entry and dropped-pin confirmation are the B2C intake baseline; map-link import is not an approved requirement.

## 7. Round 2 questionnaire — answers now received

The questions below are preserved for continuity. Section 11 records Round 2 answers; section 13 records later amendments. Section 29 contains the remaining explicit clarification. R2-03 still does not fully settle per-action permissions or deny precedence. The later answers distinguish a return decision from physical branch receipt and decline the suggested ERP cancellation-request protocol.

| ID | Decision and concrete edge case |
| --- | --- |
| R2-01 | Where does initial task selection and assignment happen? Does ERP submit tasks already assigned to branch/driver and open the shared Tawsel planning view, or submit eligible tasks for assignment in that shared view? Preserve the selected UI while defining its responsibilities. |
| R2-02 | Before the real ERP exists, is the goal development/integration testing with the mock only, with a real pilot after ERP is ready; or a real pilot beforehand needing a small operational intake interface? A fixture/mock is not automatically a production intake product. |
| R2-03 | Does page access grant all its actions, or can a user view dispatch while being unable to assign/cancel? Confirm direct-user deny precedence over role grants. |
| R2-04 | If a user is assigned two branches, are capabilities identical in both, or can they dispatch in one and only view the other? |
| R2-05 | A driver has eight undelivered tasks on board and receives five new tasks still at the branch. Should the return for collection happen after the current round or be inserted into remaining work? What explicit minimal action makes the new tasks eligible for delivery? |
| R2-06 | When the driver has explicitly started heading to a stop, should automatic replanning keep that stop fixed and reorder only later work? This is especially important without GPS. |
| R2-07 | A failed/no-answer attempt is recorded at 11:00; the customer calls at 12:00 and can receive the parcel. May the driver schedule another attempt that day without dispatcher approval? For a timed deferral, does the driver enter an earliest revisit time? |
| R2-08 | At day end, what happens to unfinished tasks: automatic next-day scheduling for the same driver, or a review/reassignment queue? Never automatically label them delivered, failed attempts, physically returned, or cancelled. |
| R2-09 | A driver reports delivery offline while ERP cancellation/reassignment is accepted online. Who may resolve the received conflict? Recommend a separately granted conflict-resolution capability, preserving the driver report until review. |
| R2-10 | For COD 450, must the driver explicitly confirm actual collected money, including zero/partial collection? How should a discrepancy be recorded and presented to the ERP without fabricating payment or silently changing commercial rules? |

## 8. Subsequent discovery backlog

The remaining explicit clarification is in section 29. This backlog records remaining coverage, not another questionnaire to answer now. Resolved subjects are not reopened merely because earlier sections contain them.

- Implement settled B2C phone/password and email recovery, simple outcomes/optional collection/reporting without partial delivery, and contact-link validation; manual address/pin input is accepted; no map-link parser is required. No branch-return, receiver, custody, employer connector, or billing workflow in this mode.
- Billing/pricing/payment/subscription eligibility remains deferred until after the basic-app trial. Do not add implementation gates or ask for a business pricing decision now.
- Tenant/integration onboarding, trusted identity/provisioning, authentication bridge, revocation and branch removal while disconnected, capability propagation, and separation of connector credentials from user sessions. One ERP-configured role, inherited grants, user exceptions, and identical permissions across multiple assigned branches are already confirmed.
- Finalize page/action capabilities using the confirmed inherit/allow/deny precedence, administrator resource scope, staff transfers and authorized historical visibility. Enforce the latest prohibition on staff changes to departed execution; Round 9 confirms the narrow actual receipt/disposition boundary. Do not re-ask role multiplicity or create ERP user/role administration in Tawsel.
- Source task fields/references, confirmed whole-piece quantities, source revisions, duplicate submissions, mixed integrations, and contact isolation. Keep shipments separate even at the same pin; grouping is rejected. Required phone/name presentation and permitted pin corrections are already accepted.
- Apply the accepted remaining-route 50-stop limit and atomic rejection, including branch-service visits. Specify capacity on approved reactivation/interruption and prepared versus received work. Online synchronization/start is now mandatory; no hard shift-end constraint is wanted. Urgent-first ordering is accepted; only post-departure ERP urgency authority remains in R10-01. Three vehicle profiles, ten-minute default service time, and default last-customer endpoint are settled; do not re-ask them.
- Geocoding confidence wording, invalid or missing pins, scope of partial planning, tenant-local address reuse, map renderer/tiles/style and Arabic labels, external assets, and no guaranteed offline basemap. Retaining the original source address and assigned-driver pin correction are accepted. Staff pin corrections must respect the new post-departure restriction; no GPS in V1.
- Implement the now-approved bounded reactivation of untouched no-answer/return-required shipments. Specify earliest deferral dates, infeasible timing, and truthful current/next/counter projection after corrections. All call counters and limits are removed. Workday reporting/carryover are settled; staff post-departure correction is now prohibited and bounded driver self-correction is accepted in D-96; rejected partial remainders cannot be deferred or reactivated by assumption.
- Replanning triggers/debouncing, obsolete jobs, unassigned optimizer work, restoration of automatic planning after a manual fallback, and current/next recovery. Manual choice/first-route fallback during an Engine outage is accepted and must be visibly identified. Never infer movement, arrival, or successful optimization from a click.
- Offline queue durability, idempotency retention exceeding the 24-hour target, reconnect order and conflicts, the approved online execution transfer, updates/reinstallation, session recovery and resolution of evidence received by the server without the rejected staff execution override. The offline target and deliberate-logout block for unsynchronized work are accepted, not open product choices.
- Supplied additive prices, prepaid fee handling, narrowly recorded refusal to pay shipping, corrected outcomes, and full/partial/failed-report semantics. Contents/prices freeze after departure; refusal normally still collects shipping. No live ERP quote, arbitrary underpayment for accepted goods, refund, or financial settlement in Tawsel.
- Source-branch-only return batches, minimal driver acknowledgment, ERP-native receipt, partial receipt and operational loss/damage resolutions, staff/network/ERP outages and collecting fresh work during the same visit. Round 7 permits independent confirmed subsets and rejects a whole-batch clearance gate; items not confirmed remain in their real state. Do not reinstate B2C return handling or a mandatory shared Tawsel staff receipt screen.
- Re-dispatch after actual confirmed return or approved ordinary pre-start removal, preserving assignments/attempts and references without double possession. A lost/damaged-item resolution does not itself mean physical receipt or available stock.
- Measurement conditions for accepted 3-second Tawsel / 5-second ERP freshness targets, stale thresholds, snapshot/subscription or polling recovery, signature/retry/replay behavior, and received-versus-applied ERP lag. Initial pilot size is not a capacity ceiling. Android/iPhone PWA coverage is accepted; actual test devices/versions and viewport evidence remain to arrange.
- Hostinger KVM 2/Dokploy co-location and other workloads, measured growth envelope, private Engine services, secrets, independent UI/API releases, migrations, workers/health/lag, backup/restoration, replay/reconciliation, and evidence gates. No capacity claim without tests on stated resources.
- Screen 4 confirmed account/recovery paths and accepted execution transfer; screens 3/6 shared planning with ERP-owned assignment; screens 1/2 contact controls, simple no-answer and accepted bounded retry restoration, refusal shipping, source-branch return and item-based partial execution; screens 5/8 held work, expected/actual reporting and authorized Excel export; screen 9 real pending/conflict states. Preserve all nine references and their provenance. Billing, GPS, advanced proof, unrelated warehouse/finance screens, and generated unverified claims remain excluded.

## 9. Technical references consulted on 20–21 September

- [OWASP Authorization Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Authorization_Cheat_Sheet.html): authorization on every request, server-side enforcement, and resource/context restrictions. This supports enforcement recommendations; it does not determine the user's product permission matrix.
- [VROOM API documentation, master branch](https://github.com/VROOM-Project/vroom/blob/master/docs/API.md): input locations and task service time windows. The upstream master documentation was readable; attempts to retrieve the v1.15.0-tagged document through the web tool failed. Do not treat master-only features as verified for the user's deployed binary. Version-specific adapter verification remains required before implementation.

- [web.dev — Click to call](https://web.dev/articles/click-to-call), consulted 21 September: a `tel:` link opens a device-dependent phone handler; phones may ask for confirmation and desktops may use an external telephony application. Consequently, a link click alone is not evidence of a completed or unanswered call. This informed the earlier counter discussion, but Round 6 cancels the counter entirely. Retain only the truthfulness boundary for external call/chat actions. The page's publication/update age is not confused with this inspection date.
- [WhatsApp Help Center — How to use click to chat](https://faq.whatsapp.com/5913398998672934): the official page was identified, but its body was not available through the research tool. The user's direct WhatsApp-link requirement is confirmed; exact link construction and actual mobile/desktop behavior still require verification. No claim of message delivery or call-result detection is made.

- [Hostinger — VPS hosting](https://www.hostinger.com/vps-hosting), inspected 21 September: the public KVM 2 listing gives 2 vCPU, 8 GB RAM, 100 GB NVMe and 8 TB bandwidth. The user chooses KVM 2; these published figures are not observed free resources or evidence of Tawsel capacity. No purchase or price recommendation is made.
- [Dokploy — Installation](https://docs.dokploy.com/docs/core/installation), inspected 21 September: the installation guidance lists at least 2 GB RAM and 30 GB disk and notes Docker build resource needs. These are installation guidance, not a fixed reserved allocation to subtract from RAM or a Tawsel performance guarantee. The documentation also uses host port 3000 for the panel; the existing VROOM host mapping also uses 3000, so deployment must preserve internal service access and resolve the host-port conflict without exposing Engine services or replacing the existing panel. No installer or deployment command was run.

## 10. Implementation and evidence status

Completed: ten answered operational discovery rounds; D-101 closes urgency. The supplied chat HTML/context documents were read in full and repository/configuration/nine UI source-image sets assessed at HEAD `3d6291697fb0baeb69215237bf1d09dbf6d1d9cd`. The master plan includes the visual/component, Vitest, simple-UX and accepted stack decisions. Smooth UI full text was fetched and relevant guidance reviewed. D-108 authorized preparation; D-109 prompted replacement of the first 11-phase division with 42 bounded prompts, 65 current requirement groups, decision traceability and a review checklist. Historical inspection/research evidence is not a new runtime test.

Not completed: finalized machine-readable feature contracts/identity configuration, application implementation or actual Vitest files/runs, browser/device interaction checks, integration proof, or production capacity assessment. All 42 current phases are not started. Phase preparation does not establish implementation or separate approval of every detailed operational default.

Next step: use docs/phases/01-workspace-test-harness.md as the complete first implementation prompt, then proceed numerically with actual checkpoint/test evidence in docs/implementation-status.md. Do not execute the archived 11-phase prompts, reopen settled stack decisions or treat an agent's remembered context as a prerequisite. No packages were installed and no Engine/server/production configuration was changed by these planning updates.

## 11. Round 2 decisions and amendments — history, updated by section 13

These are product decisions or explicitly stated intent, not claims about existing code. Boundaries in the last column identify decisions still needed.

| ID | Source answer | Confirmed intent / amendment | Remaining boundary |
| --- | --- | --- | --- |
| D-13 | Intro | A mock ERP is acceptable, must be visibly identifiable during use, and must not create an incompatible integration path for a later real ERP. | Use the same published integration contracts and validation, durable synchronization, and conformance scenarios. Real ERP field/identity mapping and a production connector will still require implementation and verification. No zero-effort migration promise. |
| D-14 | 1 | B2B shipment selection and driver assignment occur in the ERP; the driver opens Tawsel and sees assigned shipments with their current states. | ERP submits intent; Tawsel validates execution transitions. Initial route review/activation and collection confirmation remain open. Screens 3/6 must reflect this division. |
| D-15 | 2 | Two product paths are required: ERP-connected B2B and independent B2C, where the driver enters shipments and Tawsel optimizes the ride. | This is a material addition to the previous ERP-focused assumption. B2C execution/reporting depth, accounts, workspace separation, and release timing remain open. Driver entry is now real functionality, not mock-only intake. |
| D-16 | 3 | Configurable roles grant permissions inherited by their users; user-specific permission exceptions are also required. | Recompute inherited permissions when roles change. This implementation recommendation follows inheritance, but exact allow/deny precedence, multiple roles, and view-versus-action capability choices still need decisions. |
| D-17 | 4 | Round 2 assigned a user to one branch with no access to other branches, restricted by role/user permissions. | Historical intermediate decision, explicitly superseded by D-25 on 21 September: multiple branches with identical effective permissions. |
| D-18 | 5 | Additional assigned shipments can wait for collection after the current round. The app needs a view of orders actually with the driver and an optimized route for eligible on-board work. Planning should consider the driver's current place. | Exact selective pickup confirmation, readiness, and route activation remain open. Actual current coordinates are unavailable without an input source; no GPS scope amendment is yet approved. Assignment alone is not collection. |
| D-19 | 6 | Keeping the current explicitly selected heading-to/arrived stop stable during automatic replanning is accepted. | Define cancellation/abandonment exceptions and async source-revision checks. Do not infer travel to the next stop from completion. |
| D-20 | 7 | Retry after no answer is permitted, with indicators such as 1/3, 2/3, 3/3. A deferral can specify a future date. | Is the count phone calls or physical failed visits? Is three a hard limit, over what period, and what happens at exhaustion? Appointment-window semantics and infeasible schedules remain open. |
| D-21 | 8 | Once collected, an unfinished shipment stays with its driver across day end until an explicit return action with a reason. | A return decision and physical return are not necessarily the same event. The meaning/confirmation of returned, custody visibility, and future-day route eligibility need decisions. |
| D-22 | 9 | Shipments must not be transferred from one driver to another, and a shipment must not end up both cancelled and delivered. | No ordinary reassignment workflow is approved. Clarify erroneous assignment before collection, driver incapacity, re-dispatch after actual return, and cancellation acknowledgment during offline operation. Do not bypass this rule by silently minting a new task ID. |
| D-23 | 10 | Due amount greater than zero means COD; display that amount and let the driver confirm collection. Collecting less is prohibited. Prepaid shipments have zero due. | Do not invent partial collection, underpayment, overpayment, or negotiated-amount flows. Decide whether successful COD delivery and full collection are one confirmation/transaction. A driver confirmation does not settle ERP finance. |
| D-24 | 10 | The required report lists delivered shipments and how much should be collected for those deliveries, separately for each driver. | This replaces the broader proposed cash-balance/discrepancy reporting. Clarify period/date basis, corrected outcomes, and whether reported collection appears alongside expected COD; no financial settlement or current physical-cash balance. |

### Round 2 integration/UI recommendations — superseded where section 13 says otherwise

- Recommend a persistent, plain-language `Mock ERP / Demo data` marker in the mock and in its demonstration workspace. Keep its identities, integration credentials, external references, and data separate from real customer work. Test the actual APIs/events, idempotency, revisions, signatures, retry/replay, durable inbox/outbox, and authoritative recovery reads. Do not make simulated success or prototype demo switches part of normal production journeys.
- A later ERP connects through the delivery backend's documented boundary. It does not need to adopt mock database tables or call raw VROOM as the execution API. It must implement mapping, authentication/delegation, atomic outgoing intent where required, durable receiver processing, and recovery. Demonstrate that behavior in the mock now; verify it again for the actual connector later.
- B2B administration remains in the ERP. B2C account creation and shipment entry now need legitimate Tawsel journeys. Recommend isolated personal and company workspaces if one person may use both; do not combine company contacts/orders with personal jobs in a route without explicit approval. No account-linking mechanism is selected yet.
- Screen 4 needs the agreed independent/account and ERP-authorized entry paths. Screens 3/6 retain shared operational planning/monitoring but must not contradict ERP-owned shipment selection/initial assignment. Screens 1/2 need locked-current-stop replanning, retry semantics, full-COD confirmation, and pending states. Screen 5 needs on-board versus awaiting-collection visibility. Screen 8 needs day closure with unfinished held work and the focused per-driver delivery/COD report. Screen 9 needs genuine received conflicts and cancellation status, without GPS breadcrumbs.
- B2C shipment entry, selective collection, returns, and planning-origin controls require specification of the smallest necessary screens/dialogs/states within the selected Stitch visual system. No new visual identity or generic replacement dashboard is approved.
- Upcoming transactional specifications must prevent conflicting final outcomes and double collection/attempt increments, preserve return and retry history, validate pickup/assignment/route revisions, and keep stored monitoring counters coherent. A pending offline report is not server-confirmed truth; ERP cancellation intent is not automatically a completed delivery-state transition.

## 12. Round 3 questionnaire — answers received on 21 September

Preserved as questionnaire history. Section 13 records the answers and remaining ambiguities. In particular, general approval of R3-03 must be read with the more specific assignment-as-receipt clarification; R3-05 did not resolve calls versus visits; R3-08 was declined; and R3-09 was accepted while adding the divisible-order case. Section 29 contains the remaining explicit clarification.

| ID | Question / edge case | Recommendation under discussion |
| --- | --- | --- |
| R3-01 | Does independent B2C include the same start/arrival/outcome/retry/full-COD/day-report journey, or only entering shipments, optimizing a route, and opening navigation? | Reuse the agreed execution flow in a simpler personal workspace if execution tracking is wanted. B2C release timing remains to be scheduled after scope is agreed. |
| R3-02 | May the same driver use both a company-authorized B2B context and a personal B2C context? | If yes, separate workspaces and data; deliberate authorized switching. Shared sign-in/account linking needs its own secure design rather than automatic identity matching. |
| R3-03 | For the first round, is ERP assignment plus the driver's confirmation of the shipments actually received enough to prepare a route and let the driver explicitly start it, or is separate authorized route approval required? | Show assigned-but-uncollected work immediately; let the driver confirm actual pickup, calculate eligible work, and explicitly start. Keep acceptance, calculation, publication/activation, and execution distinct. |
| R3-04 | To account for the driver's current place in V1, is a visible planning origin based on the last confirmed stop with optional manual map-pin correction sufficient, or is an actual GPS reading required? | Keep the present no-GPS V1 baseline unless the user explicitly approves an amendment. A confirmed stop is not a live position; a manual pin is not measured GPS accuracy. A requested one-time location reading would be a separate scope decision, not approval for live/background tracking. |
| R3-05 | Do no-answer indicators 1/3 through 3/3 count phone calls or physical failed delivery attempts, and what must happen after the third? | Do not automatically equate a exhausted counter with a physical return. Preserve distinct attempts and deduplicate action retries. |
| R3-06 | Does marking a shipment returned mean the driver decided to return it while it remains with them, or that it was physically handed back? Who confirms the latter in B2B? | Distinguish return-required from confirmed physical return; keep it on the driver's held-work list until the actual handback is recorded. An ERP acknowledgment can confirm handback without implementing warehouse management here. |
| R3-07 | Does the no-transfer rule also prohibit correcting an assignment before collection or assigning another driver after a confirmed physical return, for example when the original driver is ill? | Respect no direct on-board transfer; any exception requires explicit approval, traceable ownership, and a real handback/readiness process. Do not silently sidestep the rule with a replacement ID. |
| R3-08 | If ERP requests cancellation while an offline driver is about to deliver, should cancellation remain pending until that driver acknowledges it, rather than claiming immediate final cancellation after pickup? | Final cancellation before pickup only after validating committed state. After pickup, pending stop-delivery request; if acknowledged in time, return-required. If delivery was accepted first, reject final cancellation and reconcile the source. Preserve late offline evidence for unresolved races. |
| R3-09 | Should a COD shipment use one explicit confirmation for successful delivery plus the full displayed collection amount, while prepaid uses delivery confirmation with zero due? | Commit delivered and driver-reported full collection together. This is an execution record, not proof of bank/cash settlement. No partial-collection input. |
| R3-10 | When a driver defers to Thursday at 13:00, does that mean any time from 13:00 onward, or an appointment window such as 13:00–14:00? | Make the promised service constraint explicit. If the optimizer cannot satisfy it, keep the task visible as requiring rescheduling; do not silently change the customer's agreed date/time. Detailed infeasibility and next-day handling follow the chosen rule. |

## 13. Round 3 answers — history, amended by section 15

The user explicitly asks to continue discovery and include a recommendation with each question. No master plan has been approved. The decisions below supersede conflicting recommendations or intermediate decisions above.

| ID | Source answer | Confirmed intent / amendment | Remaining boundary |
| --- | --- | --- | --- |
| D-25 | Opening correction | Restore multi-branch assignment for each B2B user. The same effective role/user permissions apply in all assigned branches; do not define different action permissions for each branch. | Supersedes D-17 and restores/refines D-04. Multiple-role and direct-user override precedence remain open. Branch access never implicitly grants every driver/task/integration operation. |
| D-26 | 1 | B2C uses the same execution steps and daily reporting as B2B. The driver enters the shipments/locations rather than receiving them from an ERP. | Specify independent input fields, branchless returns, partial-delivery policy without an ERP, and account onboarding. Shared execution does not imply inventing a B2C warehouse or dispatcher. |
| D-27 | 2 | Company and personal work are not combined in one account. The person logs out of the company account and logs into a separate B2C account to use the independent product. | Replaces the linked-workspace recommendation. Safe logout with an active trip or pending actions, account recovery, and session/device policy remain open. Do not erase or submit one account's offline records as another account. This choice alone does not establish cross-device identity or enforce a global ban on two accounts belonging to one person. |
| D-28 | 2 | The B2C path must be chargeable; the user does not intend free access merely because a person can create/use a personal account. | No price, monthly/per-use model, trial, payment gateway, subscription activation, or expiry behavior is selected. Recommend separating subscription eligibility from role permissions and enforcing both on the server. Billing for Tawsel access is distinct from shipment COD and ERP commercial finance. |
| D-29 | 3, read with 7 | Accept the proposed initial route preparation and explicit driver start, without additional dispatcher approval of the optimized route. | The specific answer 7 replaces the earlier independent pickup-checklist assumption: definitive ERP assignment already asserts receipt. Keep preparation, valid calculation, route activation, and start distinct, even if coordinated through one approved start flow. |
| D-30 | 4 | The last confirmed stop with optional manual planning-pin correction is sufficient for V1. | GPS collection remains excluded, including a one-time GPS reading. Do not label the planning pin as a live current location or measured accuracy. Branch distance suggestions must identify the chosen planning origin. |
| D-31 | 5 | Accept the proposal to require an explicit decision at the attempt limit rather than inferring physical return from the counter. | The user did not answer calls versus physical visits. Do not finalize the meaning of 1/3, a hard three-attempt limit, reset periods, or retry spacing. |
| D-32 | 6 | During a trip, a shipment may be marked for return but remains with the driver. Add an operation named approximately `Deliver returns to branch`: interrupt the day's delivery work, visit a branch, have driver and dispatcher/authorized receiver confirm the handover, then resume delivery work. | Explicitly requested operational scope, replacing the earlier broad exclusion of all handover UI. Eligible branch, permitted interruption points, exact pause/current-next behavior, dual confirmation sequence, partial receipt, missing staff/network, and resume conditions require discovery. |
| D-33 | 7 | Definitive ERP assignment means the shipment has been received by the driver; the existing paper list is confirmed in that workflow. The dispatcher may remove a shipment from the list before the driver starts the route. | Reconcile future work waiting at a branch with assignment-as-receipt. Define whether pre-start removal corrects an erroneous assignment or records actual handback, and prevent a stale route start from ignoring the latest list. Do not destroy historical records. |
| D-34 | 7 | If a driver becomes ill, shipments may be physically returned to the branch, confirmed by the dispatcher, and then assigned to another driver. In the ERP they become available for dispatch again. | The former absolute no-reassignment interpretation is superseded: direct transfer while held is excluded; re-dispatch after confirmed branch receipt is allowed. Suggested execution label: `Returned to branch`; ERP availability label: `Available for dispatch`. ERP inventory state names remain ERP-owned. Preserve prior attempts, assignments, and external order identity. |
| D-35 | 8 | Do not add an ERP cancellation-request workflow during delivery. Staff call the driver and the driver records the work as return-required. | This declines the proposed cancellation protocol. A phone conversation is outside the app and not an automatic state update. Pre-start list removal remains a separate allowed case. Unsupported post-start deletion/cancellation must not silently mutate execution state; reconciliation tests should use approved conflicts rather than assuming this rejected flow. |
| D-36 | 9 | Accept one explicit delivery-plus-full-COD confirmation. The user additionally requires discovery of orders marked divisible/eligible for partial delivery by the ERP. | Conditional subset delivery is not arbitrary underpayment. Granularity, allowed quantities, due-amount authority, fee/discount handling, remainder disposition, prepaid handling, quote freshness, reporting, and offline eligibility are not yet approved. |
| D-37 | 10 | Deferral means delivery may occur at or after the chosen date/time; avoid narrow appointment windows in V1. | Timezone, date changes, route horizon, infeasibility, and eligible-work carryover still need specification. Do not silently move the customer's earliest date earlier. |

### Round 3 recommendations — historical, not all accepted

Section 15 supersedes billing-now, additional pre-start procedures, a shared Tawsel staff receipt UI, online ERP quotes, and deferring rejected portions. Do not implement these historical recommendations as current requirements.

- **Assignment and physical receipt:** use the latest ERP assertion as the authoritative submitted business fact, with source identity and revision. To retain the earlier requested visibility of upcoming work at the branch, recommend a distinct prepared-for-driver state that does not yet assert possession. This recommendation requires approval. Route edits cannot silently erase physical responsibility or a recorded assignment. A pre-start removal is not a customer-order cancellation.
- **First start and concurrency:** recommend validating the latest assigned list online when starting a new round, with expected revisions and atomic start/withdrawal checks. This would prevent ordinary stale-start acceptance but reduces offline start capability; ask the user rather than silently imposing it. Previously started work and pending outcomes are a separate offline policy. Corrections still need audit and visible rejection/recovery, not raw deletion.
- **Return-required versus returned:** a driver return decision removes the affected quantity from customer-delivery eligibility but does not assert branch possession. A branch service visit is an operational stop/activity and must not inflate delivered-shipment counts. The current delivery target may be suspended only through the agreed explicit interruption rule; automatic replanning alone must not change it.
- **Two-party receipt:** recommend both participants confirming the same identified shipment/quantity set, using authorized users and branch scope. Matching confirmed items can transition once; disputed or merely pending items must not become available to another driver. Identical retries are idempotent; conflicting item revisions or receipt sets require review. Exact partial-batch acceptance and resume policy are questions, not implemented invariants yet.
- **ERP receipt boundary:** proposed UX is that the dispatcher opens an authorized shared Tawsel receipt view from the ERP, avoiding a second company-administration module. Both confirmations commit in Tawsel before the event describes a confirmed handover; the ERP then applies the inventory availability projection locally and durably. This surface/sequence is proposed in R4-06. If the user instead wants ERP-native confirmation, specify an authenticated actor-aware command and local outgoing intent. Neither design creates one ACID transaction spanning databases. Inventory availability cannot be promised from an unreceived or unapplied event.
- **Authorization and branches:** receiving returns does not justify unrestricted visibility of another branch's tasks or contacts. Resolve eligible receiving branches and the minimum item details needed to confirm a specific incoming handover. A role name such as Dispatcher does not itself grant receipt capability.
- **Partial delivery:** preserve stable ERP line/parcel/unit references and authorized remaining quantities. Distinguish what was delivered, what remains with the driver for a later visit, and what is return-required or actually returned. Do not let two retries, visits, or assignments deliver the same quantity twice. One physical customer visit can have a partial task result; quantities, attempts, successful full deliveries, and processed stops must not collapse into one counter.
- **Amounts for a subset:** the original order total cannot be split proportionally without an ERP-approved rule. Discounts, delivery fees, prepaid portions, and minimum charges remain source-owned. Recommend an online ERP-authorized amount for the selected subset initially; this makes partial confirmation depend on ERP availability. Preapproved additive allocations are a possible later/alternate contract only if the ERP explicitly supplies and authorizes them. Do not silently substitute a guessed amount while offline. The user must choose the intended commercial rule and availability tradeoff before contract finalization.
- **Remaining goods:** a partially delivered shipment may need return-required and/or deferred quantities. R4-10 asks whether both are supported. Do not reset completed portions on a later route or charge already delivered quantities again. Prepaid partial delivery must not invent a refund workflow in Tawsel.
- **B2C billing:** recommend a monthly subscription per independent account for the initial commercial model, with server-controlled paid eligibility. This is a proposal, not market research, an approved price, a payment-provider selection, or evidence of implemented billing. A simple manually activated pilot subscription is an option; whether online payment is required at first release is still open. Decide expiry/grace rules without making loss of execution history or queued actions an accidental consequence of expiry.

### Affected UI, contract, evidence, and documentation work

- Preserve all original Stitch exports. Screens 1/2 need a branch-return action/activity, explicit pause/resume, permitted partial-delivery confirmation, and accurate full/partial/remaining labels. Branch markers are destinations, not live driver markers.
- Screens 3/6 retain authorized shared planning and monitoring; add the agreed receipt/review surface if R4-06 is accepted. ERP assignment-as-receipt and pre-start withdrawal must map to actual commands and revisions rather than generated prototype controls.
- Screen 4 needs distinct B2B/B2C account entry, correct logout behavior, and subscription-status/activation states. B2C registration and payment/activation detail remain to be chosen. Do not restore the rejected one-account company/personal switcher.
- Screens 5/8 need held versus return-required quantities, branch-visit progress, unfinished work across days, and reports that distinguish full/partial delivery without pretending to settle money. Screen 9 needs genuine received conflicts such as withdrawn-before-start work, receipt-list disagreement, changed partial-delivery source data, and authorization changes; remove assumptions of an active ERP cancellation-request workflow and GPS breadcrumbs.
- Contracts must express prepared work if approved, definitive ERP receipt assertions, before-start removal, trip interruption/recovery, receipt confirmation and history, re-dispatch after confirmed return, and partial-delivery identity/quantity/amount validation. Contract names and schemas are not finalized by this log.
- Extend eventual integration evidence with assignment/withdrawal-versus-start races; duplicate two-party receipt; partial/mismatched handover; commit-before-notification and ERP-inbox recovery; re-dispatch against a stale availability projection; repeated partial outcomes; changed/expired amount authorization; and quantities that are returned then dispatched again. Continue to verify tenant/branch/integration/account isolation and freshness.
- DESIGN.md and docs/ui-spec.md must map these approved additions to the existing design system during the later foundation work. Tracking/consistency and integration documentation must separate execution receipt, ERP inventory projection, driver-reported money, and future billing eligibility. No master plan, phase prompt, new application code, or operational test is produced in this discovery update.

## 14. Round 4 questionnaire — answers received

Preserved as questionnaire history. Section 15 records replies, including rejected recommendations; section 16 preserves the next historical batch; sections 29/30 contain the final clarification and answer. Recommendations in this table are not requirements unless explicitly accepted.

| ID | Question / concrete scenario | Recommendation to discuss |
| --- | --- | --- |
| R4-01 | For paid B2C access, should the initial charging unit be a monthly subscription per account, or usage such as routes/shipments? | Start with a monthly subscription per independent account, separate from COD. Do not select a price yet. Manual paid activation is an option for the pilot; first-release online payment and expiry behavior need later confirmation. |
| R4-02 | The driver is away with ten shipments; staff prepare five more still at the branch. If definitive assignment means receipt, how should those five appear before physical handoff? | Distinguish `Prepared for you / awaiting handoff` from definitive `With driver`. Only final ERP handoff/assignment asserts receipt; prepared work does not enter the executable route yet. |
| R4-03 | If staff remove a shipment before the driver starts, does that mean it was physically taken back or its assignment was corrected? How should the driver avoid starting an obsolete downloaded list? | Record a reasoned pre-start withdrawal/correction without deleting history. Validate the latest list online at the initial start; started execution continues under the separate agreed offline policy. This online-start limitation needs explicit acceptance. |
| R4-04 | May a driver return shipments to any company branch, only the originating branch, or an eligible branch within the driver's authorized branches? | Permit a receiving branch within the driver's authorized branches that is allowed to accept returns; reflect the actual receiving branch to the ERP. No cross-company receipt and no claim of the nearest actual branch without a current planning origin. |
| R4-05 | May `Deliver returns to branch` interrupt a driver already heading to a customer, or even after they have marked arrival and are handling that customer? | Allow an explicit interruption while heading; preserve the paused target/history. If already handling a customer, finish or explicitly resolve that attempt first. Show branch-return activity as current, suspend delivery execution without closing the day, then replan from the confirmed branch and explicitly resume. |
| R4-06 | What is the exact two-party receipt journey and where does the dispatcher confirm it? | Driver selects the return shipments/quantities; receiver opens an authorized shared Tawsel receipt view from the ERP and confirms the same set. Finalize only matching server-confirmed driver/receiver acknowledgments, then emit the ERP event. Whether the receiver UI instead lives natively in the ERP remains the user's choice. |
| R4-07 | The driver offers five returns, but the receiver can confirm only three. Must the whole handover wait, or can matching items be finalized separately? | Let both parties confirm the agreed three; keep the remaining two explicitly unresolved and attributed to the last confirmed holder in the system until resolved. Do not fabricate physical receipt or block all unrelated deliveries merely because an item is disputed. Exact continuation and offline receipt policy remain to be clarified. |
| R4-08 | What does an ERP-divisible order mean: individual items/quantities inside one order, separate parcels, or only predefined complete sub-shipments? | Use stable ERP-defined parts and permitted quantities, selected by the driver. Only ERP-authorized divisible work gets the partial-delivery action. Ordinary underpayment for all goods is not partial delivery. |
| R4-09 | Who supplies the exact due amount when the customer takes only some parts, including fees/discounts, and must this work while offline? | Initially obtain an ERP-authorized amount for the selected subset while online, display it for explicit confirmation, then record full payment of that amount with the delivered quantities. This adds an ERP dependency; preapproved per-part additive amounts are an alternative only with an explicit ERP pricing guarantee. Do not compute an arbitrary proportion or permit free-form reduced collection. |
| R4-10 | After two of three items are delivered, is the remaining item always returned, or may it remain with the driver for a later delivery date? | Let the driver choose an authorized reasoned return or earliest-time deferral for the remainder. Preserve the already delivered quantities/amount and optimize only the eligible outstanding portion. Prepaid refunds and price adjustments remain ERP-owned. |

## 15. Round 4 answers — history, amended by section 17

The user explicitly prioritizes a practical basic app with few staff steps, defers monetization decisions, and permits more than ten questions when useful. Discovery continues; there is no approved master plan or implementation authorization.

| ID | Source answer | Confirmed intent / amendment | Boundary still requiring definition |
| --- | --- | --- | --- |
| D-38 | 1 | Defer B2C billing until the basic app is planned, personally tried, and corrected. The business team will decide the charging method later, then a later Codex task can add it. | Supersedes current-foundation subscription/gating proposals. Do not add billing UI, mandatory entitlement checks, pricing/payment decisions, empty billing tables, or billing phases now. Preserve a short future note; ordinary account authorization still applies. |
| D-39 | 2 | Accept prepared-for-driver work before receipt. Actual receipt converts it to on-board work and triggers re-optimization. | A prepared shipment must not be falsely counted as physically held or enter the executable delivery route. Keep current-stop preservation and asynchronous revision checks. |
| D-40 | 3 | Before the driver departs, staff may remove a shipment and put it on another driver's list as a normal ERP operation. The co-located team handles the physical situation directly. | Reject the proposed mandatory reason/extra handover procedure. The online-only-start proposal is not approved and must not be imposed. A simultaneous removal/start needs automatic validation/recovery, not extra staff forms. After departure, the agreed return-to-source-branch path remains distinct. |
| D-41 | 3 | The application should help a small operational team finish work; do not create a recorded manual step, button, or approval for every physical movement. | Interpret this as a product/process constraint. Original automatic transactional audit/idempotency/outbox requirements remain background responsibilities; do not turn them into clerical tasks, tracking of unreported physical movement, or corporate workflows. |
| D-42 | 4 | Returns go only to the branch from which the shipment was dispatched. | Supersedes the proposal to choose any authorized receiving branch. Multi-branch access does not allow changing a shipment's return destination. Source-branch identity must be supplied/stable for that dispatch cycle. |
| D-43 | 5 | Accept the proposed branch-return interruption: pause while heading, resolve a customer already being handled, show branch-return activity, then replan/resume from the branch without closing the workday. | Clarify whole-shipment deferral separately. Exact offline/pending branch-arrival/receipt and missed receipt recovery remain to design. |
| D-44 | 6 | The dispatcher confirms in a native ERP screen: open driver X, see the pending return request, inspect the items, and confirm receipt. | Reject an additional mandatory shared Tawsel staff receipt UI. Specify an authenticated receiver-actor command and durable ERP intent. The mock must demonstrate this exact ERP-side surface/contract, without becoming a real warehouse module. Minimize repeated driver confirmations. |
| D-45 | 7 | Confirm an agreed subset of returned goods while other goods remain unresolved. The ERP dispatcher settles cases such as loss/theft or damage. | An authorized operational resolution should reach Tawsel so the discrepancy does not remain open forever. Do not equate loss resolution with physical branch receipt, or damage settlement with automatically available inventory. Financial consequences remain ERP-owned. |
| D-46 | 8 | Partial delivery means item quantities inside a shipment: for example, two of three pieces are accepted. The ERP merchant policy determines whether splitting is allowed; Tawsel uses that supplied permission and item details. | Stable item references, integral/other units, quantities, B2C entry, and report counts need a final contract. Do not add merchant-account administration to Tawsel. |
| D-47 | 9 | Calculate partial COD directly from supplied shipment details. Example: 3 items at 100 each plus 50 shipping; accepting 2 requires 250 collection and leaves one 100-value item for return. No pricing request back to ERP is wanted. | Supersedes the online ERP quote proposal. Supplied prices/fees are authoritative inputs, not permission for Tawsel to invent discounts/taxes. Fee on total refusal, post-dispatch price changes, and prepaid edge cases require final rules. Local capture can use these details offline but remains pending until Tawsel accepts it. |
| D-48 | 10 | Every unaccepted part after partial delivery returns. No later customer visit is scheduled for that rejected remainder. | Supersedes the proposed remainder deferral. It is not yet clear whether the wording also retracts earlier whole-shipment deferral for an unavailable customer; ask R5-01 instead of assuming either interpretation. |

### Round 4 consequences — historical, updated by section 17

The next answer round changes the refusal-fee default, B2C returns, call counting, shipment grouping, and over-limit admission. Do not treat older open questions or rejected defaults as current requirements.

- **Billing:** remove paid-access/activation and pricing screens from current UI scope and implementation-foundation expectations. Keep independent account identity, authorization, and task ownership, which are needed without a billing layer. A future business decision will initiate monetization work after the basic app trial.
- **Pre-start removal:** one ordinary ERP operation should carry the source actor, task/assignment identity, and relevant revision. Tawsel validates and updates committed state, monitoring indicators, audit/idempotency result, and outbound intent atomically. No mandatory user-entered reason or second receipt acknowledgment. Retry and concurrent-start safeguards are invisible in the normal flow; show a clear conflict only when one actually occurs. Do not erase attempts or return history when work is assigned again.
- **Prepared work:** show upcoming work clearly, then refresh the on-board list and request optimization after receipt is accepted. Engine failure must not undo the receipt; expose the last valid route and unresolved planning status accurately. Never apply an obsolete optimizer result over newer accepted work.
- **Return destination:** select return items belonging to the same source branch for one handover. If the driver holds returns from different origins, show separate source-branch groups rather than silently redirecting everything to a nearby branch. A branch visit is not a delivered shipment or a GPS observation.
- **ERP-native receipt:** the driver-side request is surfaced under the driver in the ERP/mock. The ERP receiver records accepted items/quantities and sends an authorized confirmation through its server-side connector. Agree requested/received sets and use idempotency and expected revisions to prevent double receipt. ERP source changes and required outgoing intent commit together locally; Tawsel records its own confirmation locally. Commands/events expose received/processed or pending synchronization as appropriate; no global transaction is promised.
- **Discrepancies:** ERP may resolve lost or damaged items. Specify a narrow operational resolution command/event carrying the authorized disposition and source correlation. Keep physical receipt and disposition distinct; a lost item is not fabricated as a branch return, and damaged received goods may remain unavailable for re-dispatch. Do not implement employee financial settlement, inventory valuation, or insurance logic in Tawsel.
- **Partial outcome transaction:** validate split permission, selected quantities against outstanding quantities, authoritative price snapshot, full collection of the computed amount, current assignment, and expected revisions. Record the delivered and return-required portions, attempt/outcome, progress, action result, and outbox together. Duplicate/reordered commands must not re-deliver units, re-charge shipping, or incorrectly reset the original shipment.
- **Amount example:** the confirmed example is `2 * 100 + 50 = 250`. The returned unit is excluded from goods due. The fee for zero accepted units is unresolved, so do not generalize the formula to collect 50 on a failed delivery without approval. Do not revive online ERP quoting or permit a free-form reduced amount. The ERP supplies final unit amounts/fees; refund, tax, and discount decisions stay there.
- **Deferral:** unaccepted partial remainders are excluded from customer-revisit planning. Preserve the earlier whole-shipment deferral decision as unresolved against the latest wording until R5-01 is answered. Do not claim calls-versus-visits or a strict three-attempt reset rule is already approved.
- **UI mapping:** screens 1/2 need simple source-branch return and item-quantity selection with the computed amount; screens 3/6 reflect ordinary ERP assignment/removal and real planning results; screen 4 needs account/session behavior without billing UI; screens 5/8 distinguish prepared, held, delivered full/partial, return-required, confirmed return, and ERP-resolved discrepancies; screen 9 handles actual pending/rejected/concurrent actions. Staff receipt and discrepancy settlement remain ERP-native and appear in the mock harness. Preserve the selected Stitch layouts and components; add only the necessary dialogs/states.
- **Verification changes:** cover ordinary pre-start reassignment without extra acknowledgment, withdrawal/start races, receipt-triggered obsolete optimization, source-branch enforcement, ERP-native partial receipt, lost-versus-physically-returned resolution, duplicate quantity outcomes, formula examples, and all rejected partial remainders returning. Retain rollback, worker/receiver crash, replay, isolation, schema matching, and freshness evidence. No application/browser/Engine tests have been run in this planning update.

## 16. Round 5 questionnaire — answers received

Preserved as questionnaire history. Section 17 records the answers and overrides; sections 29/30 contain the final clarification and answer. In particular, count calls rather than visits, collect shipping on refusal, remove B2C returns, do not group shipments, and reject excess instead of auto-splitting. The 24-hour target, pending-action logout restriction, and freshness targets are accepted.

| ID | Decision / question | Recommendation to discuss |
| --- | --- | --- |
| R5-01 | Does the prohibition on later delivery apply only to rejected portions after partial acceptance, while an untouched whole shipment may still be deferred when the customer is unavailable? | Keep whole-shipment deferral to an earliest date/time and automatic replanning; all rejected partial remainders return. Confirm whether the user instead wants to remove whole-shipment deferral too. |
| R5-02 | Does the 1/3 counter count phone calls or failed physical delivery attempts, and what happens at the third? | Count distinct recorded failed visits, not phone rings. Preserve the count across days in the same dispatch cycle. At the limit require an explicit disposition rather than claiming physical return. These count/reset/limit details remain unapproved. |
| R5-03 | If the customer refuses every item, should the driver collect the shipping fee or zero? | Default to zero on complete refusal unless the ERP explicitly supplies a refusal-charge amount under the agreed policy. No driver-entered fee negotiation. Confirm the actual business rule before adding that optional contract capability. |
| R5-04 | May shipment contents or collectable amounts change after departure? | Let ordinary edits refresh the plan before departure; keep the execution price/content snapshot stable during delivery. The ERP cannot silently change what the driver is supposed to hand over or collect. Address/contact corrections need their own permitted rules. |
| R5-05 | For B2C, is a location/address plus a short label sufficient mandatory input, with phone, COD, and item details optional? | Use fast entry with optional details. Show item quantities/prices only when the independent user enables a divisible shipment; do not require a product catalog for every stop. |
| R5-06 | Does one B2C round collect its work from one source or multiple pickup sources, and who confirms its returns without an ERP receiver? | Start with one selected pickup/return origin per round and a driver confirmation of actual handback, with no invented dispatcher/warehouse account. Multiple-source pickup is a genuine scope choice if required. |
| R5-07 | Who confirms or corrects an ambiguous location, including a driver receiving a better pin from the customer? | Use the shared review UI for an authorized branch user and allow the assigned driver to correct the execution pin when permitted; B2C user confirms their own pins. Keep the original address/source. Unresolved locations stay visibly pending while valid work can be planned. |
| R5-08 | Should multiple shipments for the same recipient at the same confirmed pin appear as one physical visit? | Group that visit while keeping each shipment's outcome and COD independent. Do not merge merely nearby locations or lose external order identity. Reflect shipment counts separately from visit counts. |
| R5-09 | If the driver's eligible work exceeds 50 distinct stops, should it be split into multiple rounds? | Retain the daily backlog and create rounds of no more than 50 stops; show later work clearly. Do not impose a 50-per-day limit or exceed the per-route limit silently. Selection/partition rules will follow the approved constraints. |
| R5-10 | Is the preferred login UX company code plus username/password for B2B, and mobile number/password for separate B2C accounts? | Use those straightforward entry paths with remembered company context. ERP remains the B2B provisioning authority. Do not assume that entering a company/role grants access or that phone ownership/recovery is verified; authentication bridge and recovery must be finalized afterward. |
| R5-11 | Is one configurable role per user plus direct exceptions sufficient, or are multiple simultaneous roles required? | Start with one configurable role and inherit/allow/deny user exceptions, with explicit user exceptions overriding inheritance. Capabilities are identical across assigned branches; sensitive actions can be separated from read access. Confirm rather than treating this recommendation as already agreed. |
| R5-12 | Is one working day, roughly 24 hours, the expected offline support window, or must longer outages be covered? | Target a full day of downloaded-trip access and queued full/partial outcomes, then reconcile. Re-optimization/new work require connectivity. The window guides testing and deduplication retention; it is not a timer that deletes pending actions. |
| R5-13 | May a user log out/change accounts while delivery actions are still unsynchronized? | For simplicity, block deliberate logout only while pending actions need synchronization and show one clear explanation/retry action. Closing/reopening the app is separate. This restriction is proposed, not approved; account data must remain isolated regardless of the choice. |
| R5-14 | Are targets of about three seconds in Tawsel and five seconds in a healthy ERP projection acceptable after Tawsel confirms a change? | Use these as measurable online targets with visible last refresh/confirmation and honest stale states. Offline capture is excluded until receipt. Choose the transport and concrete percentile/test conditions after the pilot size is known. |
| R5-15 | How many drivers and monitoring staff will use the pilot concurrently, and will devices include iPhones as well as Android? | Start with roughly five drivers and one or two monitoring users for the actual pilot, subject to user confirmation. Do not confuse that rollout size with a verified capacity limit. Test the browser/platforms actually used and agree mobile/desktop viewports. |


## 17. Round 5 answers — history, amended by section 19

This round supplies fifteen answers. General agreement is applied to the actual preceding recommendation, not to unasked business behavior. Round 6 continues discovery; no master plan or implementation phase is approved.

| ID | Source answer | Confirmed intent / amendment | Boundary or consequence |
| --- | --- | --- | --- |
| D-49 | 1 | No answer or inability to receive an untouched whole shipment may lead to deferral. Accepting only part and requesting the remainder later is a different case and is prohibited. | Resolves D-48's ambiguity. Keep earliest-date/time whole-shipment deferral; all rejected partial remainders follow the B2B return disposition. Do not reopen this distinction. |
| D-50 | 2 | The 1/3, 2/3, 3/3 indicator counts phone calls, not physical visits. | Reject the failed-visit recommendation. Which reported call outcomes count, how the app captures them, reset period, and the third-call decision are still open. A call does not prove arrival. |
| D-51 | 3 | Even when the customer accepts no goods, the driver should collect shipping. A rare refusal to pay shipping is possible; the merchant/brand bears its liability through ERP rules. | Replaces the proposed zero-fee default on full refusal. Record goods outcome, shipping reported collected, and explicit unpaid-shipping exception separately. Do not build merchant charging/settlement, infer refusal from no answer, or permit arbitrary reduced payment for accepted goods. |
| D-52 | 4 | Shipment contents/prices do not change after departure. | Keep an authoritative dispatched snapshot. Before-departure corrections and permitted execution-pin updates remain distinct; ordinary post-departure commercial edits must not silently overwrite driver data. |
| D-53 | 5 | Accept the proposed minimal independent-driver entry, with recipient phone made mandatory. During delivery, prominently show recipient name and phone; tap the phone to call and the WhatsApp icon to open direct chat. | Preserve fast entry and optional detail. Recommend tapping the name to open the existing stop-detail view. Phone normalization, accessible controls, long names, RTL/LTR isolation, and external-handler states need implementation/verification. No inference of call completion or message delivery. |
| D-54 | 6 | B2C has no return workflow. It helps a driver independently of an employer's system enter shipments, deliver, and optimize. | Remove the proposed B2C source-branch/receiver/self-confirmed-handback process. This answer does not explicitly revoke earlier simple outcomes/daily-report agreement; avoid either expanding B2C into B2B custody or silently deleting previously agreed basic reporting. Exact optional execution depth remains a narrow discovery item. |
| D-55 | 7 | Accept authorized branch-user and assigned-driver execution-pin correction, with B2C users handling their own locations. Preserve the original address and make unresolved locations visible while valid work can be planned. | Do not infer measured accuracy, GPS capture, or a commercial customer-master update. Server permissions and source/route revisions still apply. |
| D-56 | 8 | Treat two shipments as independent even for the same person/location. Do not introduce automatic grouping. | Reject R5-08's grouped-visit proposal. Each shipment retains its own identity, execution outcome, money, and counters. Routine optimization may place them consecutively but cannot combine their business state. |
| D-57 | 9 | Reject work above the 50 limit rather than automatically splitting it or making rejected shipments appear assigned to the driver. The driver works with the accepted assigned work only. | The count scope is not settled by this answer. R6-03 asks route versus all unfinished/prepared/return work. Recommend atomic rejection of an incoming over-limit assignment batch, with clear ERP acceptance/pending/rejection state and concurrent admission protection. Do not impose a 50-per-day limit or claim a global cross-database transaction. |
| D-58 | 10 | Accept company code plus username/password for B2B and phone/password for separate B2C login. | This selects login UX, not a trust protocol or proof of phone ownership. ERP B2B administration remains authoritative. Provisioning, password/reset mechanics, and authorized session exchange still require design. |
| D-59 | 11 | One configurable role per user. Roles are created with permissions in the ERP; assigning a role gives the user its permissions, and exceptional additions are made in the same user modal. | No multiple-role model or hardcoded role-name checks. Preserve inheritance rather than copying stale grants. Latest wording emphasizes additions; the earlier explicit per-user restriction request was not explicitly withdrawn. Resolve final effective-capability precedence without duplicating ERP administration in Tawsel. |
| D-60 | 12 | Accept roughly one workday / 24 hours as the target offline support window. | Downloaded work and queued actions remain durable within actual device limits. The target is not a deletion deadline, guaranteed map cache, permission to run the Engine offline, or a promise that unsent actions are visible to dispatch. Retention must cover retries beyond that target. |
| D-61 | 13 | Accept blocking deliberate logout/account switching while actions remain unsynchronized. | Show the pending work and recovery/retry path. Closing/reopening is separate. Define session expiry, lost device, permanently rejected evidence, and account isolation without deleting pending actions or trapping the user indefinitely in an unexplained error. |
| D-62 | 14 | Accept approximate online freshness targets of 3 seconds for Tawsel and 5 seconds for a healthy ERP projection after Tawsel confirmation. | Specify percentile/load/healthy-path conditions and prove them with commit/application timestamps and instrumentation. Exclude unsent offline capture from server-confirmed freshness. Visible last refresh/confirmation and stale recovery remain required. |
| D-63 | 15 | The small pilot can use the proposed approximate numbers, but more users may join; the application must be designed and tested with headroom. | Pilot count helps select a benchmark, not a hard account/capacity limit. Recommend larger measured load, efficient reads, and bounded Engine work. Hosting resources and actual supported phones remain unknown; Android versus iPhone was not answered. |

### Current report and outcome examples

These describe the agreed B2B commercial inputs and driver-reported execution, not an ERP cash settlement. The simple example has three units at 100 each and an outstanding shipping fee of 50. All amounts below use the same illustrative currency; the contract's currency/unit representation is still to be specified.

| Customer outcome | Goods accepted | Driver-reported collection | Goods disposition / reporting |
| --- | --- | --- | --- |
| Full delivery | 3 units | 350 | Fully delivered shipment. |
| Authorized partial delivery | 2 units | 250 | Partially delivered shipment; the remaining unit is return-required, not a later delivery. |
| Complete refusal, shipping paid | 0 units | 50 | Not delivered, with shipping collected; all units return-required. |
| Complete refusal, customer explicitly refuses shipping | 0 units | 0; 50 shipping remains unpaid | Not delivered; record the specific unpaid-fee exception. ERP handles merchant liability. |

Do not treat an unanswered phone as a refusal or as unpaid-fee evidence. Do not charge an already prepaid fee again. A partial or full delivery records the authorized amount without arbitrary underpayment. Reports must include the recorded shipping collected on unsuccessful shipments, so the earlier delivered-only money summary is insufficient. Keep reported collection, expected/unpaid fees, successful/partial/failed shipments, and processed stops distinct. This remains a basic per-driver report, not a cash balance, finance ledger, warehouse settlement, or proof of payment.

### Round 5 UI and contract effects — historical, amended by section 19

- Screens 1/2: keep recognizable map/list and stop-detail layouts; make recipient name, call, and WhatsApp controls consistently available. Add the approved partial/refusal-fee outcome states and a truthful call-counter interaction once R6-01/02 are answered. Distinguish a planned next stop from explicit heading/arrival, and branch activity from a GPS position.
- Screen 4: implement the accepted separate B2B/B2C login forms with authorized membership; no unchecked role selector. Account recovery and simultaneous device execution await decisions. Reflect pending-action logout restrictions without prototype security/background-sync claims.
- Screens 3/6: source assignments still originate in the ERP. Show actual accepted/prepared/held work, resolved versus missing pins, optimization results, and over-limit validation. Prevent fabricated assignment or optimization success when a request is rejected or Engine work fails.
- Screens 5/8: preserve basic per-driver workday reporting, distinguish full/partial/unsuccessful processing, and include shipping money from refusals. Remove B2C branch return/handover controls. B2B receipt and discrepancy settlement stay in the native ERP/mock view, and no rewards, financial settlement, or custody balance is added.
- Screen 9: pending local actions are different from accepted actions and ERP lag. Display received conflicts only to authorized reviewers; dispatch cannot inspect a phone's unsent evidence. Use the accepted offline/logout behavior and required replay/recovery; keep GPS Breadcrumbs and fake timer success out.
- HTTP/events/examples must support independent shipment outcomes and item quantities, fixed source revisions, refusal shipping and its narrow exception, explicit contact records, permission/branch restrictions, coherent current/next summaries, rejected admission, and pending/accepted/processed states. A fee-only collection is not a task-delivered event. Exact endpoint/schema names remain undecided.
- Durable outbox/inbox and the clearly branded mock ERP remain the integration baseline. Retest no partial commit/emitted event on rejection, simultaneous over-limit admission, retry of fee/quantity outcomes, crash/replay/order recovery, and branch/integration isolation during implementation. None of these scenarios has been run in discovery.

## 18. Round 6 questionnaire — answers received

Preserved as questionnaire history. Section 19 records the replies; sections 29/30 contain the final clarification and answer. In particular, the call counter and departure with unconfirmed returns are rejected, and the service-time default is ten minutes. Recommendations below are historical proposals unless the answer accepts them. The batch does not reopen multi-branch access, one-role inheritance, B2C billing deferral, whole-shipment versus partial-remainder deferral, required contact controls, login form choice, or the accepted offline/freshness targets.

| ID | Question / concrete scenario | Recommendation to discuss |
| --- | --- | --- |
| R6-01 | How should an unanswered call enter the counter when a phone/WhatsApp link cannot establish its result? | Provide a simple `No answer` action after the driver calls. Increment the driver-reported unanswered-call count through that action, not merely by opening the dialer. Keep customer-unavailable/reschedule separate. No access to the device call log and no automatic arrival inference. |
| R6-02 | What does reaching 3/3 mean, and when does the counter reset? | Count reported unanswered calls for the shipment within the workday. At the third, prompt for an explicit later time or, in B2B, a return decision. Do not automatically mark physical return or enforce artificial minutes between calls. Keep prior contact history when a new workday begins. Confirm whether three should instead apply across the entire dispatch cycle. |
| R6-03 | Is 50 the limit for remaining stops in one route, or for all unfinished work associated with a driver, including prepared shipments, future deferrals, and held returns? Example: 45 remaining deliveries and a new batch of 8. | Retain the original per-route limit, including planned branch-service stops. Reject the entire new batch if accepting it would exceed that route's limit; do not silently accept some, split routes, or retain rejected work as driver-owned. Completed work is not remaining route work. Confirm the intended treatment of prepared/future/return work rather than imposing a daily or possession cap. |
| R6-04 | If a workday crosses midnight, which daily report should contain those deliveries, and how should unfinished work appear next time? | Group reports by an explicit workday from start to `End day`, even across midnight, with actual Egypt-local timestamps. Carry forward still-held unfinished shipments without ERP resubmission, respecting earliest deferral dates. Closing the day does not mark them delivered or returned. |
| R6-05 | The driver physically hands returns to the source branch, but the receiver/ERP confirmation is delayed or connectivity is unavailable. May delivery resume? | Let unrelated delivery resume from an explicitly supplied branch planning origin while the receipt stays visibly pending. Do not claim confirmed branch possession or make those items eligible for re-dispatch until authoritative receipt arrives. Clarify if physical business practice instead requires waiting; do not add a compulsory second driver confirmation by assumption. |
| R6-06 | If automatic optimization is unavailable, may the driver choose another ready assigned stop and continue? | Preserve recordable outcomes and the last valid remaining order, show re-optimization pending, and allow explicit choice of an eligible assigned stop. With no successful initial route, offer an honest manually ordered list if accepted. Keep an already selected heading/arrived target until the driver explicitly changes or resolves it. |
| R6-07 | Which vehicles will actually be used: motorcycles, cars, bicycles, or several? | ERP supplies the B2B driver/vehicle routing profile; the independent user selects a mode before planning. Use only a verified available Engine profile. No full vehicle-maintenance or fleet-assignment module. Profile/version suitability still requires adapter verification. |
| R6-08 | Should route optimization include returning to the branch or another endpoint after the last delivery? | Default to ending at the last customer. Include a branch visit when explicitly required/selected; let B2C optionally select a fixed endpoint. A planned return never claims that the driver actually arrived or handed over goods. |
| R6-09 | How much time should planning allow at each customer, beyond travel? | Start with a five-minute default service duration, optionally overridden by an authorized supplied duration or a simple user setting. Do not require the driver to enter a duration for every stop. Treat it as a planning estimate, not measured dwell time or a guaranteed ETA. |
| R6-10 | Who may fix an accidentally confirmed delivery or wrong accepted quantity after confirmation? | In B2B, an authorized ERP user submits an explicit operational correction that Tawsel validates and records without erasing history; the driver can report the mistake. B2C users correct their own records. Do not blindly undo dependent receipt/re-dispatch actions or allow arbitrary COD editing. |
| R6-11 | Should an independent B2C user self-register, and should forgotten-password recovery use email or the phone number? | Allow simple self-registration with the agreed phone/password login, plus a verified recovery email. B2B reset remains under company administration via the agreed identity mechanism. SMS-based ownership/recovery is an alternative if specifically needed; no provider or sending cost is selected here. |
| R6-12 | May the same driver execute deliveries on two phones at once? | Prefer one active execution device/session with an explicit replacement/recovery path, while preserving old unsent evidence for reconciliation. Do not pretend an offline old device can be instantly erased or that its pending outcomes may be dropped. This is an account execution rule, not a physical-person uniqueness claim across separate B2B/B2C accounts. |
| R6-13 | Are actual driver devices Android only, or do they include iPhones? | Plan responsive PWA support for the actual Chrome-on-Android and Safari-on-iPhone targets. Test the agreed devices/viewports and report missing checks honestly. No native-app or reliable background-GPS promise. |
| R6-14 | What CPU, RAM, disk, hosting environment, and Engine co-location are available for the pilot, if known? | Use actual hardware to choose repeatable load and operational tests, with headroom beyond the first few users. The historical Windows setup is not a production capacity result. If hosting is undecided, document that gap and defer a sized deployment recommendation until profiling; do not make the product account limit equal the pilot size. |

Remaining backlog items will be asked or resolved with justified engineering recommendations in subsequent discovery. Finishing this batch alone does not approve the master plan.


## 19. Round 6 decisions and amendments — history, amended by section 21

The user supplies fourteen answers and asks to continue if questions remain. The request for an explanation about simultaneous phones is not an approval of a one-device restriction. Discovery continues; no master plan or implementation phase is approved.

| ID | Source answer | Confirmed intent / amendment | Boundary and consequence |
| --- | --- | --- | --- |
| D-64 | 1, 2 | Cancel the call counter entirely, including the three-call rule and its reset/threshold questions. Keep a simple `No answer` outcome. | Supersedes D-20/D-50 counter intent and rejects R6-01/02 counter controls. Remove their UI, limit configuration, and dedicated capture workflow. Ordinary action/outcome history remains a backend responsibility without added clerical steps. |
| D-65 | 1 | After no answer the driver moves on and intends to return the shipment. If the customer later responds, the user suggests making return work available again and re-optimizing, or delivering another day. | Historical proposal, subsequently accepted with the bounded conditions in D-79. Do not confuse still-held return-required work with confirmed branch stock. Recommend a narrow whole-shipment retry action before branch receipt, preserving history. Previously rejected partial remainders remain excluded; no silent new custody transfer or duplicate shipping charge. |
| D-66 | 3 | Accept the per-route remaining-stop limit, including a planned branch visit, and rejecting the entire addition batch when it would exceed 50. | Resolves D-57 count scope. No partial assignment, automatic split, phantom driver backlog, or 50-per-day cap. Engine input and command validation must agree; concurrent additions, reactivation, and branch interruption need invariants. |
| D-67 | 4 | Accept reporting by workday, even across midnight, with actual Egypt-local timestamps. Still-held unfinished work carries forward without ERP resubmission, respecting deferrals. | Day closure does not imply delivery, return, or settlement. Preserve original event timestamps and history rather than resetting tasks. Target working hours/end-time constraints are a separate question. |
| D-68 | 5 | Branch receipt confirmation must happen before the driver leaves. | Reject the proposed resume-with-pending-receipt behavior. If the relevant remote confirmation is unavailable, the handover/resume flow waits and explains why. Scope for confirmed subsets/unresolved discrepancies is still to clarify; no default bypass or invented offline confirmation. |
| D-69 | 6 | Accept continuation through the last valid route or a manually selected eligible stop when optimization is unavailable, including an honest manual first route. This behavior must be recorded/indicated. | Keep outcomes usable, show manual/re-optimization-pending status, and prevent stale optimizer results from undoing later explicit choices. No fake optimized success and no automatic change to the current handled stop. |
| D-70 | 7 | Support the three vehicle modes already available in the Engine: car, motorcycle, bicycle. | Map application enums through adapters to verified actual provider profiles; no full fleet management. Repository configuration presence is not proof of production profile suitability or performance. |
| D-71 | 8 | Accept ending at the last customer by default, an explicit required branch visit, and an optional B2C fixed endpoint. | An endpoint is a planning input. Do not infer arrival, automatic return, or completed handover from a planned route. |
| D-72 | 9 | Use a ten-minute average/default customer service time. | Replaces five minutes. Represent ten minutes as 600 seconds in an eventual normalized adapter contract. It is an estimate, not measured dwell time or an ETA guarantee; no per-stop mandatory entry. |
| D-73 | 10 | Accept authorized ERP-user correction of B2B execution outcomes, with Tawsel validation/history, and self-correction in B2C. | Keep original outcome plus explicit correction; propagate revisions/events. Do not allow raw deletion, arbitrary price editing, or blind reversal of already dependent receipt/re-dispatch operations. |
| D-74 | 11 | Use email for the registration/recovery question. | Email recovery is accepted in context; whether email also replaces the earlier B2C phone login is ambiguous and asked narrowly in R7-06. Do not infer a mandatory SMS service or silently redesign authentication. |
| D-75 | 12 | The user asks what difference multiple devices make if work is online. | Explain simultaneous stale reads and delayed offline actions. No device-count rule is approved. Regardless of the UX choice, authorization/revision/idempotency checks must prevent contradictory outcomes and preserve received conflicting evidence. |
| D-76 | 13 | Accept the Android/Chrome and iPhone/Safari PWA support/test recommendation. | Actual physical devices, browser versions, and visual checks remain unverified. This does not authorize native app work, GPS, or a promise of reliable background operation. |
| D-77 | 14 | The pilot will use Hostinger KVM 2 with Dokploy. | Public plan specs were checked against Hostinger documentation. Engine co-location, other workloads, panel version, actual usage, and deployment capacity remain unverified. Do not assume a clean server or require an upgrade without evidence. |

### Consistency consequences and required verification changes

- **No-answer and retry proposal:** cancellation of the counter is settled. The action should record an execution outcome, remove the task from eligible delivery work as intended, recompute coherent current/next/progress, and emit committed event intent atomically. If R7-02 permits reactivation, it must be an explicit validated transition with history, assignment/quantity constraints, and fresh route revisions. A task can become pending again without erasing its earlier unsuccessful outcome. Event consumers need replacement/projection and transition semantics that support this change; a failed event is not necessarily the final lifetime state.
- **Return gate:** driver-side continuation must rely on authoritative received confirmation for the agreed handover scope. A local ERP click or outgoing intent alone is not a successful Tawsel confirmation. Durable ERP intent, idempotent commands, recovery reads, and clear pending states allow the user to retry safely without adding a second manual reconciliation job. Clarify partial receipt in R7-03. Server commit followed by a lost HTTP response must recover by reading accepted state rather than recording receipt twice.
- **Manual fallback:** keep accepting valid execution independently of Engine availability. Explicit manual choices update relevant revisions/current state without claiming optimization success. Resume optimization only against a fresh snapshot and keep the current target unless explicitly changed. Failed/obsolete jobs must not regress the displayed list or resurrect return-required work.
- **Capacity:** enforce the accepted count during additions and when applying results, with transactional concurrency checks. A held shipment is not erased because a retry would exceed route capacity. Planned branch activities need clear route membership/count semantics; interruption cannot make real physical outcomes unrecordable. Keep this in the state/invariant design rather than creating a 50-per-day business restriction.
- **Workday and service time:** all examples and fixtures use ten-minute estimates and workday-aware report grouping. Fifty ten-minute stops imply 500 minutes of estimated customer service alone. Preserve earliest dates and show expected duration honestly; unfinished work does not imply a failed customer attempt. Late synchronization and corrections may update a prior workday with visible timestamps/revisions.
- **Multi-device example:** two online phones can both read `With driver` at revision N. One submits delivery while the other submits return from that old view. Validate the expected revision and legal transition; never overwrite the accepted outcome blindly. An old offline queue introduces the same issue later with additional assignment/session changes. A one-device execution policy can reduce UX conflicts but cannot replace server concurrency controls. The policy remains open.
- **Deployment evidence:** plan read-only sizing of the full Egypt dataset and three profiles, geocoder, application DB, workers, map assets, and co-located services before capacity claims. The historical STACK report lists roughly 7.7 GB of OSRM data and 13.4 GB of geocoder storage; those are historical disk figures, not live RAM or VPS observations. Preserve existing volumes; ordinary releases must not rerun imports/preprocessing. Dokploy's host port 3000 and VROOM's current host mapping require deployment reconciliation, ideally private Engine access with no unnecessary public host port. No server configuration was changed.

### Current Stitch and deliverable effects

- Screens 1/2: remove all 1/3 counters and their special call controls; retain call/chat links, simple no-answer, approved outcomes and branch-return interruption. Add a `Ready to retry` action only after its pending scope is approved. Show manual/pending optimization status accurately and keep explicit current/next semantics.
- Screen 3: preserve the shared dispatcher map/list monitoring direction, with coherent revisions, workday filters, actual return confirmation status, corrections, and any approved reactivation. Assignment/receipt administration stays in native ERP surfaces.
- Screen 4: maintain authorized company entry and separate B2C account entry. Email recovery is accepted; identifier and multi-device behavior still await decisions. No role selector authorization, billing gate, or unsupported security/background-sync copy.
- Screens 5/8: workday reporting can cross midnight; unfinished work carries forward, return-required and received quantities remain distinct, and previous outcomes survive later retry/correction. Display ten-minute-based estimates as estimates. No B2C return/custody workflow.
- Screen 6: use the accepted 50-stop validation, vehicle choice/profile mapping, ten-minute defaults, explicit endpoints, honest optimizer results, and manual fallback. The selected reference remains the layout authority; generated auto-publish behavior is not business authority.
- Screen 7: preserve the accepted manual pin review/correction and valid-work planning flow; no GPS, measured accuracy, match percentages, or unsupported StreetView capability is added.
- Screen 9: cover duplicate/concurrent actions, stale device queues, pending return confirmations and recovered replies, obsolete optimizer jobs, corrections, and reactivation if approved. It must not fabricate successful synchronization or expose unreceived phone evidence to dispatch.
- Carry these changes into eventual DESIGN.md, docs/ui-spec.md, HTTP/events/examples, tracking/consistency, integration/operations, mock scenarios, visual review, and implementation-status. These future deliverables are not claimed complete by this discovery log. No phase count or executable implementation prompts are created yet.

## 20. Round 7 questionnaire — answers received; see section 21

This batch resolves the new return/retry boundary and remaining session/pilot behavior. It does not revive the cancelled counter or reopen settled branch, role, service-time, profile, or receipt-wait choices.

| ID | Question / scenario | Recommendation to discuss |
| --- | --- | --- |
| R7-01 | After the explanation of simultaneous online edits and delayed offline actions, should one driver execute on one phone or several? | Prefer one active execution device with a clear replacement/recovery flow. Preserve old queued evidence and show conflicts where needed. Multiple phones are possible if the user needs them, but do not promise that being online removes stale-read or conflicting-command cases. |
| R7-02 | A no-answer shipment is marked return-required but remains with its driver when the customer responds. Who can make it deliverable again, and until what point? | Let the same assigned driver explicitly mark an untouched whole shipment `Ready to retry`, without dispatcher approval, before confirmed source-branch receipt. Re-optimize eligible work with capacity/revision checks and preserve the prior outcome. After receipt, only the ordinary ERP re-dispatch path applies. Do not reopen rejected partial quantities, lost/damaged goods, or money already settled by an outcome. Whole-shipment deferral remains available. Confirm these boundaries rather than treating the user's suggestion as unrestricted reversal. |
| R7-03 | The driver offers five returns and ERP confirms three. Given the requirement to wait for confirmation, may the driver leave after those three are confirmed while the other two remain unresolved? | Let confirmation of the received subset satisfy that subset's handover, keeping the other goods visibly with the driver or under the ERP's explicit discrepancy process. If the company requires all offered items to be received or formally resolved before leaving, specify that stronger gate. Never fabricate receipt of the missing two. |
| R7-04 | The app has a downloaded but not-yet-started list; connectivity fails while ERP staff might remove a shipment before departure. May the driver begin from that cached list? | To preserve practical offline use without extra staff procedures, allow a clearly pending local start against the downloaded assignment; the server revalidates it on reconnect and retains conflicting reported actions for authorized review. This permits a stale list to have been used physically. The alternative is a brief connection requirement for every new start, while already-started work can continue offline. No online-start gate is silently imposed. |
| R7-05 | Should optimization respect a hard workday finish time, or only show an estimated finish and let the driver end the day with unfinished work? | Initially show estimated finish using ten-minute service defaults, travel and earliest deferrals, without enforcing a shift-end cutoff. Work whose earliest date is in the future remains scheduled for that date. If a hard finish time is required, leave infeasible work explicitly pending rather than dropping or promising it. The 50-stop limit is not a promise that 50 stops fit into a day. |
| R7-06 | Does `by email` mean B2C password recovery only, or should email/password also replace phone/password for account login/registration? | Preserve the accepted phone login and use a verified email for recovery, unless the user intends the wider change. Recipient phone remains a separate mandatory shipment field regardless of account identifier. |
| R7-07 | For B2B, must the driver use the same company identity/password as in ERP, or may company-created Tawsel access have a separate password? | Prefer one company identity/credential authority with an independent Tawsel session, so routine execution does not call the ERP for every action. ERP remains the user/role administration surface. Exact login provider/provisioning/availability design and compatibility with future ERP connectors must be specified; do not copy passwords between application databases or assume an iframe/deep link authorizes access. |
| R7-08 | Will the first real-user pilot use independent B2C entry with B2B tested against the mock, or must a real branch run B2B before the shipping ERP exists? | Use real B2C pilot work and separately labelled B2B mock scenarios initially. If a real branch needs the mock surface for operational trials, explicitly agree the necessary intake/user/receipt capability and its limitations; do not quietly turn the harness into a complete shipping ERP. |
| R7-09 | Is the KVM 2 server dedicated to Tawsel or already running other applications, and will all Engine services share it? | Keep Dokploy and existing services/data, inventory actual resource use, and test the intended co-location before deciding whether to separate services or resize. Prefer building/importing Engine data outside routine application releases. Public plan specs alone do not establish usable capacity or justify an automatic paid upgrade. |

Still to finish through later focused questions or justified engineering recommendations: final authorization/identity propagation and revocation, detailed contracts/state constraints, map/tile deployment and retention, recovery/backup targets, screen/action coverage, and measured operational/visual acceptance. The master plan remains gated by completion of material discovery and product-owner agreement.

## 21. Round 7 decisions and amendments — history, amended by section 24

These nine answers were supplied directly in the continuation task, following a complete reading of the chat HTML, STACK-CONTEXT.md, TAWSEL-ENGINE-CONTEXT.md, and this log. They supersede conflicting earlier recommendations. The user asks to continue discovery, not to implement the application or finalize the master plan yet.

| ID | Source answer | Confirmed intent / amendment | Boundary and consequence |
| --- | --- | --- | --- |
| D-78 | 1 | If one phone starts a round, a second phone opening it should see that the round is already running. The user prefers this behavior unless it adds undue problems. | Show the same authoritative active round and prevent duplicate starts, including simultaneous start attempts. Do not interpret the answer as unrestricted concurrent execution or as a ban on signing into a second phone. R8-01 asks only about execution continuation/transfer. Server validation remains necessary when a device has stale/offline state. |
| D-79 | 2 | Accept the proposed bounded retry: the same driver may explicitly reactivate an untouched whole return-required shipment still held by that driver before confirmed source-branch receipt, triggering re-optimization without dispatcher approval. | Preserve prior outcomes and source/assignment identity; validate current revisions and capacity. Confirmed branch returns use ERP re-dispatch. Rejected partial remainders, lost/damaged goods and duplicate financial collection are not revived. This resolves R7-02/D-65. |
| D-80 | 3 | Do not impose a batch-completion limit/gate on return handover. Items with confirmed return are finalized; items in other statuses are dealt with in ERP according to their real situation. | No requirement to settle every offered item before continuation. Preserve item-level confirmation and the prior distinction between actual receipt and unresolved/other disposition. Do not invent receipt, silently clear held work, remove the independent route-capacity limit, or add a compulsory ERP discrepancy step for every return. |
| D-81 | 4 | The driver cannot begin a new round until synchronization succeeds. Internet is required before beginning the round and departing for its first stop. | Reject the pending/offline-first-start proposal in R7-04. Start requires current authorized assignments, synchronized relevant pending work and server acceptance, not just a connectivity flag. Preserve offline continuation for an already-started round and recovery when a successful start response is lost. Do not require connectivity before every later customer stop. |
| D-82 | 5 | Show expected finish only, with no hard shift-end cutoff. Comparing expected versus actual is explicitly important. | Add V1 reporting acceptance criteria for timing comparisons. R8-02 proposes per-stop and whole-round comparisons with preserved initial and revised forecasts. Use action-reported times with provenance and visible missing data; no GPS measurement, automatic driver ranking or predictive-learning scope is approved. |
| D-83 | 6 | Email is for recovery only. | Resolve R7-06/D-74: B2C login remains phone/password with verified recovery email. Recipient phone is independently required on shipments. No mandatory SMS flow follows from this choice. |
| D-84 | 7 | Accept one shared B2B company identity/credential authority with an independent Tawsel session; user/role administration remains in ERP. | Resolve the product choice in R7-07; select the exact trusted identity/provisioning design later. Do not copy password hashes between databases or make routine driver commands synchronously depend on ERP availability. Revocation and safe session lifetimes remain to specify. |
| D-85 | 8 | Accept real B2C pilot work alongside separate B2B mock ERP scenarios. | No production B2B branch intake expansion is requested. The mock demonstrates the genuine integration boundary and remains clearly labelled; a real shipping ERP/connector is separate future work. |
| D-86 | 9 | The owner wants to try the complete setup personally, identify bottlenecks and decide resource increases from the observed results. | Retain KVM 2/Dokploy as the trial target and plan a repeatable owner-run diagnosis/load workflow. Do not require an upgrade in advance or claim capacity. The answer does not explicitly inventory existing workloads or confirm exact Engine co-location; defer read-only inventory to deployment preparation rather than repeatedly asking the same question. No server changes, purchases or tests were performed. |

## 22. Round 7 specification and evidence consequences

- **One active round across views:** starting twice must not create two active copies. Concurrent start requests need an atomic invariant; a second device reads the existing round. R8-01 is a product decision about execution transfer, not a substitute for revision/idempotency validation. Any later transfer must preserve unsent evidence and handle stale former-device commands explicitly; no remote-erasure promise.
- **Online start:** synchronize the relevant queue and assignments, then validate/start against authoritative revisions. Withdrawal or reassignment racing with start is resolved server-side. A lost successful response is recovered through idempotency/current-state reads. Do not show a successful start merely because the phone is online. Reopening an already-confirmed round remains different from creating a new round. An application-server-connected manual start during Engine failure remains available under the accepted fallback.
- **Partial branch receipt:** each confirmed set can finish independently, with no all-items barrier. Remaining work stays in its actual confirmed state until an authorized ERP update arrives. A failed or unsent receipt command is not success. ERP status/intent and Tawsel acceptance are separately durable; authoritative recovery reads repair lost replies. A loss/damage resolution is not fabricated receipt or automatically reusable inventory.
- **Retry:** reactivation must race safely with receipt, other outcomes and capacity changes. Preserve attempt history and quantities; refresh current/next indicators coherently. Prevent an older queued retry or optimizer result from resurrecting goods already received by the branch. Do not delete held work if it cannot enter the current route because of capacity.
- **Expected versus actual:** comparison is operational V1 reporting, not historical prediction or cross-company learning. Recommend retaining the forecast at round start and identified later route forecasts instead of overwriting the baseline. Associate stop forecasts with stable stop/attempt and route revisions. Added/removed stops, branch interruptions and re-optimization must be visible in the comparison, so changed workload is not silently treated as pure lateness. Driver-recorded heading/arrival/outcome times are observations reported by a person, not measured travel/dwell data. Late synchronization uses capture-time provenance and clock-quality rules; server receive time is not substituted for arrival time. Missing actuals, uncompleted/cancelled work and suspicious clocks remain explicit. Do not add pause buttons, GPS, automated worker scoring or a ranking feature by assumption. R8-02 selects report depth.
- **Identity and pilot:** keep separate B2B/B2C accounts and the pending-action logout rule. Specify one company identity authority with separate application sessions, capability propagation and revocation without building duplicate ERP role administration. The first pilot has genuine B2C intake; B2B mock operations use the same released contracts a real connector will consume.
- **UI mapping:** screen 4 needs the confirmed login/recovery paths and any approved device-transfer flow; screens 1/2 need the accepted retry and source-branch partial receipt behavior; screens 3/6 show online-start/assignment truth and accepted revisions; screens 5/8 gain approved expected/actual reporting; screen 9 retains actual pending/conflict evidence and start/receipt recovery. Preserve the source exports. Granular timing presentation and transfer controls are still proposals pending R8 answers.
- **Future verification:** cover concurrent starts, start/withdrawal races, lost start responses, offline continuation after confirmed start, retry/receipt races, independent partial receipts, and expected/actual reports through changed routes and delayed actions. Existing rollback, outbox/inbox crash recovery, duplicates, replay, isolation, freshness and visual checks remain required. These are planned scenarios, not executed tests.
- **Deployment:** provide the owner with repeatable checks and understandable measurements on the actual trial host, separating application/API latency, Engine job duration, event lag and resource usage. Inventory existing services before configuring deployment, preserve data and the Dokploy panel, and keep Engine builds/imports out of normal app releases. Exact co-location and usable capacity remain unverified.

## 23. Round 8 questionnaire — answers received; see section 24

These are material product choices still needed for implementation. Do not reopen the settled online start, phone/email recovery, retry eligibility, partial-return continuation, pilot split, role multiplicity or branch rules. Routine framework, transport and adapter decisions should be recommended from evidence rather than delegated to the product owner as a technical questionnaire.

| ID | Question / scenario | Recommendation to discuss |
| --- | --- | --- |
| R8-01 | The second phone sees the same running round. If the original phone fails, how should execution continue on the second? | Let both view; normally execute on the phone that started it, with an explicit online `Continue on this phone` transfer. The old device cannot submit new authoritative execution after transfer without revalidation; preserve delayed evidence for review. Do not require approval from an unavailable old phone. Simultaneous execution remains an alternative if actually required. This is a proposed execution rule, not yet accepted. |
| R8-02 | Should expected/actual reporting cover the whole round only, or each stop as well? | Show both: arrival and completion where recorded, plus whole-round elapsed time. Keep the forecast at start and revised forecasts identifiable after re-optimization; do not overwrite the original comparison. Display missing actuals honestly and collect from existing start/heading/arrived/outcome actions, without extra timing buttons or GPS. Compare round duration on matching start/end boundaries; an early end with unfinished work is not a full-route completion comparison. |
| R8-03 | Is optional item-level partial delivery needed in the initial B2C experience, beyond simple outcomes, an optional collection amount and the daily report? | Prefer the simpler B2C experience initially, retaining item-level splitting in B2B. This would narrow the earlier optional B2C item/detail proposal and must be explicitly accepted; until answered, do not silently remove earlier scope. B2C never gains a branch-return/custody workflow. |
| R8-04 | May an ERP administrator explicitly remove one inherited permission from a particular user as well as add exceptions? Example: the role allows an action, but one user must not have it. | Use inherit/allow/deny exceptions in the same ERP user editor, with the explicit user choice overriding role inheritance within that user's allowed resources. Tenant, branch, integration and assignment limits remain enforced. One role and identical effective capabilities across assigned branches stay settled. |
| R8-05 | Who resolves a received offline outcome that conflicts with a newer accepted correction or assignment? | In B2B, use a separately grantable conflict-review/correction permission for authorized ERP staff; the driver sees pending review and does not silently overwrite accepted state. Preserve both records and let Tawsel validate the authorized resolution. B2C users resolve their own conflicting records through a clear choice. Only received evidence is visible to staff; this does not create a warehouse or financial settlement workflow. |
| R8-06 | Are shipments either fully prepaid or fully collected on delivery, or is there a real deposit/part-prepaid case too? | Initially support the confirmed zero-due prepaid and full authorized due cases. If deposits are required, ERP must supply exact remaining amounts and any item/fee allocations needed for permitted partial delivery; Tawsel does not infer those allocations or refund money. Do not introduce an unsupported deposit workflow or arbitrary reduced payment. |
| R8-07 | For permitted partial delivery, are quantities whole pieces only, or must fractional quantities such as weights be supported? | Use whole pieces in V1, matching the accepted two-of-three example. Weight can remain descriptive shipment information if needed; fractional partial delivery requires explicit rules and is not assumed. Stable line IDs and validation are engineering responsibilities. |
| R8-08 | Beyond the accepted earliest deferral date/time, are explicit urgent-priority shipments needed for the first version? | Initially optimize using the agreed route/profile and timing constraints without a new priority control. If real urgent work must be prioritized, specify its behavior before adding it. Never change an already selected current target automatically or promise a hard arrival deadline merely from an urgent label. |

## 24. Round 8 decisions and amendments — history, amended by section 27

The user supplies eight answers and asks what remains. Do not restart discovery or turn routine technical choices into further product questionnaires. The new staff restriction supersedes conflicting older correction recommendations; related product boundaries are narrowly addressed in Round 9.

| ID | Source answer | Confirmed intent / amendment | Boundary and consequence |
| --- | --- | --- | --- |
| D-87 | 1 | Accept the proposed second-device behavior: both phones can view the same round; execution transfers through an explicit online `Continue on this phone` action without approval from an unavailable original phone. | Resolves R8-01/D-78. Only one execution owner at a time. Preserve old queued evidence and revalidate it; prevent duplicate starts and delayed former-owner writes from silently overriding current state. This does not transfer the shipment to another driver/account. |
| D-88 | 2 | Accept per-stop and whole-round expected/actual comparisons, retaining initial forecasts and identifiable revised forecasts after re-optimization. | Resolves R8-02/D-82. Use existing recorded arrival/completion actions, show missing actuals, and avoid extra timing controls. Changed workload and unfinished rounds must remain distinguishable from lateness. No GPS, driver ranking or learning pipeline is added. |
| D-89 | 3 | Accept the simpler initial B2C experience: simple delivery results, optional collection amount and daily reporting, without item-level partial delivery. | Explicitly narrows earlier optional B2C pieces/prices/splitting. Keep required recipient phone, fast entry and independent accounts. B2B whole-piece partial delivery remains; no B2C branch returns, warehouse custody or billing is introduced. |
| D-90 | 4 | Accept user exceptions that can remove an inherited permission as well as add one, using inherit/allow/deny in the ERP user editor. | Explicit user choice overrides role inheritance within permitted resources. One role and identical effective capabilities across allowed branches remain settled. Permissions never bypass a prohibited state transition or authorize a cross-tenant request. |
| D-91 | 5 | Once shipments have departed, ERP staff cannot change their state or other details; they are the driver's responsibility. | Reject the proposed staff post-departure conflict/outcome correction and supersede D-73 in that period. Do not add an admin override to restore it. Prevent staff changes to dispatched execution, preserving driver-originated actions and their authorization. R9-01 reconciles this with previously approved actual branch receipt/loss-damage handling; R9-02 asks how the driver corrects an accidental record. This answer does not itself approve driver self-correction, disable identity administration, or erase delayed conflicting evidence. |
| D-92 | 6 | Describes deposits as an edge case and accepts the preceding practical recommendation. | Retain source-authoritative due amounts and no arbitrary reduced collection. The answer does not establish actual deposit frequency, allocation/refund rules, or a separate deposit feature. If such source work is accepted, ERP must provide exact outstanding amounts and sufficient item/fee allocation for allowed partial delivery. Do not invent an even/proportional allocation or interpret a missing allocation as zero. |
| D-93 | 7 | Use whole numbers/pieces, with no fractional quantities. | In the context of R8-07, this rejects weight/fraction-based partial delivery; it does not repeal the accepted two-of-three-piece B2B case. Validate whole-piece quantities and stable source lines. |
| D-94 | 8 | Expresses interest in urgent shipments: yes, they can be needed. | Do not retain the recommendation to omit urgency as a settled decision. R9-03/04 define who may set it and its scheduling effect. A label alone does not define a deadline, permission to override the current target, or verified optimizer semantics. |

## 25. Round 8 consequences and remaining engineering work

- **Authority:** general staff permissions must not permit post-departure execution edits, even when a role grants broad pre-start administration. Define caller, lifecycle and ownership checks together. The previously suggested conflict example in which staff freely change a dispatched outcome is no longer an approved workflow. Preserve meaningful concurrency coverage through simultaneous starts, pre-start withdrawal, execution transfer, driver commands, retry/receipt and delayed duplicate actions. Branch confirmation/disposition remains a pending narrowly scoped reconciliation, not a silent staff override.
- **Execution transfer:** use an authoritative transfer generation or equivalent current execution ownership, with idempotent takeover and stale-owner validation. Receiving a delayed action is distinct from applying it. A disconnected old phone cannot be instantly changed; its UI may remain stale and its unsent evidence must survive for the authorized resolution policy. Show current ownership/freshness truthfully without demanding approval from the lost phone.
- **Reporting:** preserve initial forecasts and identified re-optimization snapshots against stable stops/attempts. Whole-round and per-stop actuals must use matching comparison boundaries and action-time provenance, not HTTP arrival time. Show unavailable times and changed scope; no fabricated duration for a skipped/unvisited stop or early-ended unfinished route. R9-06 asks about report export, not about reopening the approved comparisons.
- **B2C and quantities:** remove item-level partial-entry/confirmation UI from current B2C scope while retaining agreed simple outcomes, optional collection and reporting. Keep B2B piece-based partial delivery, price snapshots, fee rules and rejected remainder disposition. No fractional delivery or dedicated deposit/settlement screens are approved. Source contracts must explicitly describe the outstanding collectable amount and any supported per-piece prepaid allocation; unsupported ambiguity is surfaced instead of guessed.
- **Urgency:** requested behavior must be specified before choosing an adapter implementation. Preserve current heading/arrived work and earliest deferrals. Do not assume a provider's priority field controls visit ordering or provides a hard arrival guarantee; verify deployed-version semantics during adapter design and use an appropriate strategy for the confirmed product rule. Admission limits and partial/unassigned planning results remain explicit.
- **UI:** screens 1/2 need approved device ownership, B2B-only partial controls, driver-authorized actions and any approved urgency/correction behavior; screens 3/6 must enforce staff read-only behavior for departed execution, with explicit pending boundaries for receipt; screens 5/8 need approved forecast/actual reporting; screen 9 preserves actual received conflicts without the rejected staff execution-edit workflow. Preserve all selected visual references. Account recovery, maps, auth/session mechanics, retention, backups, deployment and acceptance still need supported engineering specification before an agreed master plan.
- **Evidence:** this update changes only the discovery log and verifies its document structure. No code, contracts, Engine/runtime configuration, server capacity or browser/device behavior was implemented or tested. All new UI/transition details awaiting Round 9 remain proposals.

## 26. Round 9 questionnaire — answers received; see section 27

This focused batch clarifies the consequence of the latest authority rule, urgent work, and remaining intake/report outputs. Source/format suggestions here are proposals, not guarantees of supported map-link parsing or implemented export. Do not reopen settled account, offline-start, route-limit, B2C partial-delivery, device-transfer or permission-inheritance choices.

| ID | Question / scenario | Recommendation to discuss |
| --- | --- | --- |
| R9-01 | With staff barred from editing dispatched shipments, does their previously agreed confirmation of an actual driver-requested branch return remain available? | Keep native ERP confirmation limited to actual receipt of the returned goods and related ERP-owned disposition, while prohibiting staff remote changes to delivery outcomes or current held shipment details. Re-dispatch follows confirmed branch receipt. This narrow reconciliation needs the user's answer; no broad staff correction privilege is restored. |
| R9-02 | If a B2B driver accidentally records delivery or the wrong accepted piece count, may that driver correct their own record? | Provide a simple correction in shipment details during the open workday, before dependent branch receipt/re-dispatch, preserving the original record and validating quantities/amounts. Correct a recording mistake only; do not invent a refund, reverse a real completed handover or allow arbitrary underpayment. Do not impose a mandatory reason on ordinary work. Ask whether the user instead wants submitted outcomes to be final. |
| R9-03 | Who marks a shipment urgent, and may urgency change after departure? | ERP supplies B2B urgency before departure; after departure only the assigned driver may change it. B2C users control their own urgency. This follows the new ownership rule without granting staff a live route override. |
| R9-04 | Does eligible urgent work always precede ordinary remaining work, or is urgency only a preference balanced against total distance? | After the already selected current stop, serve eligible urgent work before ordinary work, optimizing within that group and respecting earliest dates/times. Make clear that this can increase travel; it is not a hard arrival-time promise. The alternative is a softer preference that can leave an urgent job later. |
| R9-05 | How does a B2C driver usually receive a destination: a written address, a shared map link, or a manually selected pin? | Offer address entry and a manual confirmation pin, plus pasting supported shared-map links where extraction is reliable. Ask which input is most common to prioritize the flow. Preserve original input and show unsupported/unresolved links for manual pin confirmation; do not claim every Google Maps/WhatsApp message contains extractable coordinates. No GPS collection is added. |
| R9-06 | Is viewing the daily/timing report in the app sufficient, or should the driver/authorized viewer also download it? | Provide the in-app report plus an Excel export of the same authorized rows, filters, units and forecast/actual distinctions. This is a proposed output, not a new analytics, accounting, settlement or cross-company report module. |

## 27. Round 9 decisions and amendments — completed by section 30

The user supplies six answers and asks whether anything remains. These decisions close the preceding receipt/correction, urgency-order, intake and report questions. The precise timing of ERP urgency edits was subsequently resolved by D-101 in section 30. Routine engineering choices should now move toward assessment and an evidence-backed master-plan draft rather than more speculative product questionnaires.

| ID | Source answer | Confirmed intent / amendment | Boundary and consequence |
| --- | --- | --- | --- |
| D-95 | 1 | Confirm the proposed distinction: staff cannot remotely change dispatched execution, but still confirm actual driver-requested source-branch receipt in ERP; ERP handles loss/damage and can re-dispatch after confirmed return. | Resolves R9-01/D-91 boundary without restoring general staff outcome correction. Authorize specific receipt/disposition operations with real state/revision checks; no arbitrary overwrite or assumption that loss means receipt/available stock. Preserve independent partial receipts and the absence of a whole-batch departure gate. |
| D-96 | 2 | Accept B2B driver correction of an accidental result or piece count from shipment details, while the workday remains open and no dependent branch receipt or re-dispatch has occurred. | Preserve original and corrected records, revalidate quantities/amounts/assignment, and propagate coherent progress/events. This corrects a recording mistake; it does not create a refund, price edit, free-form underpayment or undo a real dependent handover. Do not add mandatory clerical steps for ordinary actions. |
| D-97 | 3 | Urgency can be set by either ERP or the driver, including when the customer calls the driver asking to be served sooner. | Driver changes during execution are explicit. The answer does not explicitly locate ERP edits before/after departure; R10-01 asks whether urgency is an exception to the earlier staff lock. Do not silently grant a live ERP override or silently narrow the user's both-sides intent. B2C users control their own tasks. |
| D-98 | 4 | Accept the proposed ordering: finish the explicitly current target, then eligible urgent work before ordinary work, optimizing within the urgent group and honoring earliest dates/times. | Strong ordering, not merely a vague preference or a promise of a fixed arrival time. No automatic replacement of the protected current stop. Ineligible future/prepared/location-unresolved work does not become executable merely because it is urgent. Provider implementation/feasibility must be verified later. |
| D-99 | 5 | The independent driver enters the location themselves, using a written address or a dropped pin. | Prioritize manual address entry and explicit pin selection/confirmation. This is not permission for GPS collection and does not explicitly approve shared-map URL import. Do not add a link-expansion/import dependency to core V1 based on the preceding assistant suggestion alone. Existing missing/ambiguous-location handling remains. |
| D-100 | 6 | Accept in-app reporting plus Excel download of the same authorized data, including results, recorded amounts and expected/actual timing under the selected workday/filters. | Export is a product deliverable, not implemented evidence. Apply tenant/branch/driver access at export time, preserve the same units/time and status semantics, and keep spreadsheets safe for user-supplied text. No finance ledger, settlement, incentive scoring or cross-company report is introduced. |

## 28. Round 9 consequences and next assessment

- **Authority and corrections:** make actor, lifecycle, assignment and operation constraints explicit. ERP can confirm an actual return/disposition but cannot use that operation to edit customer delivery outcomes. Driver self-correction must race safely with branch receipt, workday closure and re-dispatch; no blind revert of dependent facts. Preserve audit, idempotency, amount/quantity invariants, forecasts, counter coherence and outbound intent in the authoritative transaction. The separate urgency-time question must not broaden any other permission.
- **Urgency:** retain protected current work and earliest availability. The chosen urgent-first ordering requires verification against the deployed optimizer; a provider's job-priority field must not be assumed to guarantee sequence order. Specify asynchronous source revisions, repeated priority changes and honest manual fallback. Do not infer feasibility from optimization acceptance or hide unassigned work. Fixed endpoints, branch interruptions and the accepted 50-stop limit still apply.
- **Intake:** manual written addresses and pin confirmation are primary B2C entry. Preserve source address, unresolved-location visibility and authorized driver correction. No automatic GPS reading or map-link parser is added merely because it appeared in an earlier recommendation.
- **Reports/export:** the forecast/actual report and Excel file use the same authorized snapshot/filter semantics. Identify initial versus revised predictions, action-derived actuals, missing times, route/workload changes, workdays crossing midnight and later accepted corrections. Export user-supplied names/addresses as text without executable spreadsheet formulas; no export-specific expansion of data access. This is specification work, not generation of an example workbook now.
- **Assessment before a draft:** inspect the current repository/instructions and actual nine Stitch image/HTML/metadata references, record the observed commit and any differences from the historical inspection. Select and justify frontend/backend/database, identity/provisioning, map renderer/tile assets, durable worker, monitoring transport, PWA storage/update and deployment choices using relevant primary sources where needed. Carry accepted requirements into a coherent state/action model and screen/API/event coverage; recommend routine defaults instead of asking the user to choose implementation internals.
- **Operational readiness:** retain owner-led KVM 2/Dokploy testing and a clearly labelled mock ERP. Exact co-location, running versions, capacity, device evidence, backup destination and restoration/recovery targets still require engineering/operational specification. Do not claim these were measured or block ordinary product discovery on a hypothetical upgrade. Material unresolved launch constraints must be labelled before deployment, not concealed in an approved plan.
- **Current evidence:** only this discovery log was edited. Local HEAD was read as `3d6291697fb0baeb69215237bf1d09dbf6d1d9cd`; this alone is not a fresh full repository or UI audit. The previous visual/repository evidence remains attributed to its original inspection. No application/Engine/server tests or deployment changes were performed.

## 29. Final product clarification — answered; see section 30

| ID | Question / scenario | Recommendation to discuss |
| --- | --- | --- |
| R10-01 | For a shipment already out with its driver, may ERP still change the urgent flag, or is ERP urgency restricted to before departure while the driver can change it afterward? | Preserve the earlier ownership rule by allowing ERP before departure and the assigned driver during execution. If the user wants ERP to mark urgency while the driver is out, approve that flag as a narrow exception; all other staff restrictions and current-stop protection remain unchanged. Neither alternative is inferred from the words both sides alone. |

Do not declare an approved master plan, final implementation phases or full launch readiness from these answers. After this clarification, continue repository/design/technical assessment and draft the master plan for the owner's review; ask another product question only if concrete evidence reveals a material unresolved behavior.


## 30. Round 10 decision and planning handoff

| ID | Source answer | Confirmed intent | Consequence |
| --- | --- | --- | --- |
| D-101 | User selects option 1, then asks to continue | ERP may set or change urgency before departure; after departure only the assigned driver may change urgency. | Completes D-97 and R10-01. Urgency is not an exception to the general staff lock. Preserve the protected current stop, earliest availability, urgent-first ordering and automatic history. |

Product discovery is consolidated sufficiently for a reviewable master-plan draft. This does not mean the owner has approved the draft, its proposed engineering defaults, final contracts, implementation phases, or a deployment.

The 21 September assessment rechecked the local repository, existing Engine configuration and all nine original UI images plus HTML text/structure, embedded styles/scripts and metadata. Asset hashes matched the manifest. Primary documentation was consulted for optimizer semantics, authentication, PWA storage, maps and deployment. No Engine, browser-interaction, device, server-capacity or application test was run. Original configuration/assets and unrelated work were preserved.

Deliverables from this planning step:
- `master-plan.md`: draft scope, architecture recommendations, authority/state model, nine-screen coverage, contracts, consistency, offline/device behavior, reports, operational defaults and acceptance evidence.
- `docs/planning/repository-and-ui-assessment.md`: current evidence, prototype corrections and source-supported technical findings.
- This decision log: D-101 and the current planning status.

The owner reviews the concrete master plan next. The established workflow is to agree that plan before dividing implementation into phases and creating complete Codex prompts. No phase count, executable phase prompts, application implementation or production change is created by this handoff. Proposed engineering defaults remain clearly distinguishable from confirmed product decisions.


## 31. Owner review amendment — visual authority and component stack

| ID | Owner clarification | Confirmed intent / review status | Consequence |
| --- | --- | --- | --- |
| D-102 | Screens are a visual reference for frontend design/layout; buttons and operations come from the plan and answered discovery. Additions and removals are expected while preserving the design/layout system. | Confirmed hierarchy: business decisions and agreed requirements govern behavior; exports govern the visual reference only. | Do not implement the exported action inventory blindly or limit the application to nine fixed routes. Remove unsupported controls, add missing agreed operations/screens, and adapt layout coherently. Preserve provenance and recognizable design language. |
| D-103 | Use shadcn + Smooth UI; fetch https://smoothui.dev/llms-full.txt. | Explicit component-library selection: shadcn/ui + Smooth UI. | Unify theme tokens and component conventions around Tawsel. Choose components to serve the required behavior; inspect actual APIs/dependencies, RTL, focus, reduced motion and device performance. This does not approve a generic library template or the entire proposed stack. |
| D-104 | Asks why the technology stack was not discussed with them. | The assistant's stack was a proposal, but it should have been presented for clear discussion. Owner preferences for remaining frontend framework/backend/database/identity choices are now requested and pending. | Preserve confirmed architecture/product decisions and D-103. Do not mark React/Vite, Fastify, PostgreSQL or Keycloak as owner-approved until that discussion resolves them; do not create implementation phases yet. |

The requested Smooth UI full-text resource was fetched completely to a temporary research file (705,721 bytes; SHA-256 `c2be985ccc9b9082864ef0fd980b96d121844e05e5d2eee516321883a0ce33c0`). Relevant installation, framework compatibility, component and accessibility sections were reviewed, along with official shadcn Vite/RTL guidance. The current Smooth UI Vite guide lists React 19+, Tailwind CSS 4+ and Motion 12+; exact mutually supported versions remain implementation work after the framework choice. No full-catalog installation or blanket component-API compatibility is assumed.

master-plan.md revision 2 now makes this visual/behavior hierarchy explicit, records the selected component libraries, updates UI/design/phase acceptance expectations and leaves the rest of the stack under discussion. The supporting repository/UI assessment records the amendment without altering the original exports or rewriting their historical evidence.


## 32. Owner review decisions — Vitest, simple UX and stack acceptance

| ID | Source instruction | Confirmed intent | Consequence |
| --- | --- | --- | --- |
| D-105 | There should be Vitest files testing the important connected pieces. | Require meaningful runnable Vitest tests for important integrated behavior, not merely isolated trivial functions or a testing-tool mention. | Add tests alongside implemented features, exercising real API/domain/database/progress/outbox relationships and the mock ERP boundary where appropriate. Use isolated PostgreSQL for transactional/concurrency guarantees; distinguish controlled external fixtures from live-service evidence. Keep Playwright for complete browser/PWA/visual checks. No empty test files or invented pass results. |
| D-106 | UX must be extremely easy/simple, without many buttons, excessive text or many operations on the same page; make the purpose, requirement, missing input and awaited state clear. Splitting into modals/pages is acceptable when it separates tasks clearly. | Driver simplicity and clarity are explicit acceptance criteria. Use focused pages, an obvious main action for the stage and progressive disclosure; contextual controls remain discoverable. | Use focused modals/sheets for short choices and pages for longer tasks; avoid nested dialogs, repeated confirmations and needless navigation. Keep blockers/waiting explanations visible and concise. Preserve approved explicit actions/authority rules while reducing clutter. Review this with the owner early; visual fidelity or a passing API test alone is insufficient. |
| D-107 | The stack is good / accepted. | Accept the presented React + TypeScript + Vite, Node.js + Fastify, PostgreSQL, OIDC with Keycloak and MapLibre + PMTiles direction, alongside shadcn/ui + Smooth UI and the existing Engine/deployment decisions. | Closes D-104's stack question. Exact mutually supported versions/configuration remain engineering work. Do not reopen the choice without new concrete evidence. This reply is stack acceptance and specified plan amendments, not an explicit approval of every remaining operational proposal or authorization to implement the application now. |

master-plan.md revision 3 includes a concrete Vitest scenario/file map, same-phase test delivery expectations, isolated-database/crash/concurrency evidence requirements, and a driver-UX section covering main actions, progressive disclosure, focused modal/page boundaries and owner walkthrough criteria. It records the stack as selected and removes the stale pending-stack statements from current summaries. No implementation phase prompts, application code, packages or test scaffolding were created; tests will be real deliverables attached to the functionality they exercise.

## 33. Phase preparation authorized and delivered

| ID | Source instruction | Confirmed intent | Consequence |
| --- | --- | --- | --- |
| D-108 | After asking for the next step, the owner says “fine start take all your time.” | Authorize the proposed preparation of sequential implementation phases and complete, self-contained Codex prompts based on the reviewed plan and latest amendments. | Prepare the phase package now without another confirmation. This step does not execute an application phase, publish a release or imply measured acceptance of proposed operational targets. |

Delivered at that step, now superseded by section 34: [eleven sequential phase prompts and usage guide](docs/phases-archive-v1/README.md), [original coverage matrix](docs/phases-archive-v1/coverage-matrix.md) and the initial implementation ledger. The current ledger is [implementation-status.md](docs/implementation-status.md). The first package required source/prerequisite checks, tests alongside implementation and separate database/browser/operational evidence, but its task boundaries were too broad for the owner's preferred execution process.

The order is foundation/contracts/design; representative UI review; identity/database; intake/maps/planning; execution/monitoring; returns/corrections; durable integration/mock ERP; complete online journeys; offline/device recovery; reporting/Excel; operational verification and handoff. Forecast capture begins with planning/execution so later reports compare stored predictions rather than reconstructed ones. Atomic audit/idempotency/outbound intent is established with authoritative changes, before the later network-delivery worker.

master-plan.md revision 4 and current summaries now reflect phase-preparation authorization. All eleven application phases remain not started. Historical sections retain their original review status and are superseded by this section where necessary. Original Engine configuration/data, Stitch exports and unrelated work remain unchanged; package verification is documentation evidence only.

## 34. Smaller self-contained implementation phases

| ID | Source instruction | Confirmed intent | Consequence |
| --- | --- | --- | --- |
| D-109 | Owner challenges the 11 broad prompts, says not to assume the receiving agent understands the prior chat, and rejects one-shot implementation that depends on the agent's state; wants each phase to contain what must be implemented without omitted behavior. | Split large tasks into smaller reviewable increments. Embed the relevant business rules, concrete cases, boundaries, prerequisites, outputs and tests; line count alone is not the criterion and low-level code need not be prescribed. | Replace the first division now, preserve business decisions, provide traceability through all answers and enforce ordered implementation/checkpoints. Each task stops after its assigned phase; meaningful tests accompany each feature. |

The current [42-phase package](docs/phases/README.md) separates foundation/design, access/identity/provisioning, independent/company intake, location/adapters/planning/policy, start/current/outcome/scheduling/workday/device ownership, receipt/branch/correction/monitoring, sender/receiver/native ERP, focused online UI, local capture/replay/account-update recovery, reporting/export and separate operational evidence tasks. Ordinary delivery, exception/correction UI and branch/day UI are separate phases.

Every prompt contains a concrete outcome, direct dependencies and actual prerequisite verification, relevant sources/decision references, embedded current rules, three ordered checkpoints, six observable acceptance scenarios, focused verification, deliverables/exclusions and an explicit stopping point. The [65-requirement catalog](docs/phases/coverage-matrix.md) also maps contracts, all visual sources/extensions, named Vitest files, data creation order, documents and acceptance A–P. The [decision map](docs/phases/decision-map.md) includes every D-01–D-109 and highlights superseded answers. A [review checklist](docs/phases/review-checklist.md) helps assess the actual output.

The original package is preserved in docs/phases-archive-v1 with superseded notices; no application phase has executed. Master-plan revision 5 and current summaries point to the replacement. Documentation validation establishes internal structure/traceability only; it does not prove future implementation will pass its acceptance tests. Preserve actual omissions, contradictions or unavailable evidence rather than claiming completeness from the number of files.

## 35. Per-phase model guidance and reusable ERP handoff

| ID | Source instruction | Confirmed intent | Consequence |
| --- | --- | --- | --- |
| D-110 | Owner asks which Codex model to use in every phase. | Each execution prompt must specify a model recommendation. | Add model, reasoning effort and a phase-specific rationale to all 42 prompts, plus a consolidated guide. Exact assignments are engineering recommendations based on current available models; record actual settings and retain all tests. This does not switch the current task model. |
| D-111 | Owner asks whether completing the phases produces important files for planning the real ERP, fears an isolated Engine that cannot integrate externally, and asks which phase handles this. | Make the existing public integration and final handoff commitments concrete, reusable and verifiable. | P02 starts contracts/planning/mapping; P08/P10/P21/P22/P25 complete boundary details; P26/P27 prove separate-database public-only receiver/source integration and create a repeatable quickstart/conformance suite; P42 validates final versions, assembles the ERP planning bundle and records evidence/remaining real-connector work. No automatic compatibility with an unknown future ERP is claimed. |

Package revision 3 retains all 42 phase boundaries and existing product decisions. The [model guide](docs/phases/model-selection.md) distinguishes official model descriptions from the assistant's workload recommendations. The [ERP handoff deliverables plan](docs/planning/erp-handoff-deliverables.md) names future outputs, phase ownership, mapping contents and reproducible external proof. No application, final as-built handoff or real ERP connector was created during this amendment; those files must be produced and tested by their assigned phases. Master-plan revision 6 and the execution ledger reflect the amendment.

## 36. ERP-agnostic integration boundary and connector ownership

| ID | Source clarification | Decision and rationale | Consequence |
| --- | --- | --- | --- |
| D-112 | Tawsel must integrate with shipping companies' existing ERPs even when Tawsel does not own them and cannot access their source code, database or internal schema. | Tawsel's compatibility target is its released HTTP API and webhook/event protocol, including authentication, stable external IDs, versioning, idempotency, revisions and recovery. ERP-specific translation belongs in a connector/adapter because keeping that knowledge outside Tawsel core preserves a stable delivery-execution product boundary. | ERP source-code access is not required, direct ERP database access/cross-database joins are not the preferred architecture, and Tawsel core must not depend on ERP-specific schemas. The ERP vendor/agency, Tawsel or both may implement the connector. Integration feasibility depends on an assessment of the ERP's API, webhooks, integration modules, authentication, stable IDs, import/export and vendor-supported mechanisms; an ERP with no suitable surface may require vendor cooperation or an ERP-side adapter. The mock ERP is a conformance/reference implementation, not proof that every ERP integrates identically or automatically. This clarification amends existing contract and handoff work and creates no new roadmap phase. |

This decision sharpens D-111 without changing ownership, the Engine definition or the 42-phase sequence. Phase 02 makes the public foundation explicitly ERP-agnostic; P08/P10/P25/P26/P27/P42 already implement the correct boundary and remain unchanged.
