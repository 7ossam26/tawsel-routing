{
  "name": "tawsel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tawsel delivery application workspace around the retained routing Engine",
  "engines": {
    "node": ">=24.11.0 <25",
    "npm": ">=11.1.0 <12"
  },
  "packageManager": "npm@11.1.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "audit": "npm audit --audit-level=high",
    "build": "npm run build -w @tawsel/shared && npm run build -w @tawsel/api && npm run build -w @tawsel/web",
    "check": "npm run audit && npm run lint && npm run typecheck && npm run test:ci && npm run build",
    "dev": "npm run build -w @tawsel/shared && concurrently --kill-others-on-fail --names api,web --prefix-colors green,blue \"npm run dev -w @tawsel/api\" \"npm run dev -w @tawsel/web\"",
    "lint": "eslint .",
    "typecheck": "npm run build -w @tawsel/shared && npm run typecheck -w @tawsel/api && npm run typecheck -w @tawsel/web",
    "test": "npm run build -w @tawsel/shared && vitest",
    "test:fast": "npm run build -w @tawsel/shared && vitest run --project fast",
    "test:integration": "npm run build -w @tawsel/shared && vitest run --project integration",
    "test:ci": "npm run build -w @tawsel/shared && vitest run"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/node": "24.13.6",
    "concurrently": "9.2.4",
    "eslint": "10.0.3",
    "globals": "17.12.0",
    "typescript": "6.0.2",
    "typescript-eslint": "8.70.1",
    "vitest": "5.0.1"
  }
}
