# Requirement-driven action coverage

P28 connected implementation: A09–A13, A32, A34 and the P28 portion of A39 are now wired in the production shell at `/day` and `/prepare`. Daily categories preserve ready/unresolved/prepared/held/deferred meaning; planning exposes current choices and real job/partial/failure state; complete manual fallback is explicit; readiness and start use the canonical server checks; an unknown start preserves the exact action for status/retry; active work continues and another-phone work offers takeover without a second start. [Actual Keycloak/Chromium/PostgreSQL evidence and limits](phase-28-evidence.md). No new schema/operation, live Engine profile, native ERP administration or later execution/offline UI is claimed.

P27 native implementation: A06/A07 preparation and explicit receipt, A08 normal predeparture removal, A29 driver/source-branch physical subsets and separate disposition, A43 minimal trusted branch/role/user/driver/exception forms, E01/E02/E03/E10 receiver projections and B03 durable source status are connected to public APIs. [Browser interactions/captures and limits](phase-27-evidence.md). The deliberately small form exposes one piece line/EGP, one capability per role and one branch per user; broader canonical fields remain available through the typed adapter. It does not claim every planned UI affordance or full driver journey screen. No fault injection control is exposed.

P23 API update: A26/A27 now have bounded driver correction, current-owner outcome adoption and explicit availability/result reads. Originals remain in history; blocked evidence remains review-required. [API/demo](corrections.md), [ordered proof](phase-23-evidence.md). Full 02/09 UI and offline replay remain P30/P34; no render/usability claim is made.

P15 backend update: A10/A11 and online start now have `round.prepareStart`, `round.start`, `round.getCurrent`, `round.getStartResult` and `round.started` intent. The new readiness/current/result reads fit the existing preparation/continue/sync feedback; they introduce no dispatcher approval or pickup step. [Exact API examples/limits](round-start.md). Connected UI remains P28, with P34 journal integration; no new UI render/browser/device/owner-review claim.

P14 backend update: A10/A11/B01 and the manual planning action now have validated ready/partial history and revisioned `planning.setManualOrder` with retained valid order on failure. E05 accepts source-scoped ready/partial/manual notices. [Current policy/API](route-policy.md). Connected UI remains P28; no new render/browser/owner-review evidence.

P13 backend update: A10/A11/B01 now have verified revisioned settings, durable jobs and stored candidate/forecast APIs. E05 has a source-scoped pending draft notice (`state=draft`, `policyValidated=false`); authoritative `round.started` is still P15. UI journeys remain P28; no new screen, browser/render/owner review is claimed. [Exact behavior and limits](planning-jobs.md), [evidence](phase-13-evidence.md).

P10 backend update: A06–A08 now have verified source APIs and durable event intent; their native ERP UI remains P27. intake.getTask and intake.listTasks expose scoped upcoming/held/readiness data for those consumers; intake.getBatchResult distinguishes committed acceptance/rejection from unknown pending. No new UI, browser/render result or owner approval is claimed in P10. Departure race proof remains P15.


P07 implementation update, 22 September 2026: A01–A04 now have connected local identity/session journeys, with [real-service evidence](phase-07-evidence.md) and [HTTP/issuer ownership](identity.md). Bootstrap adds the 148th catalog entry. The remaining rows retain their assigned implementation phases; queue-aware exit is still P35, and owner review remains pending.

P03 specification, 22 September 2026; **designed**, not implemented UI. [UI specification](ui-spec.md) owns route/state conventions; [canonical operations](../contracts/operations.json) own IDs, capabilities, lifecycle and backend owner phase. URL paths here are proposed **browser routes**, never published API endpoints. P04 supplies representative fixtures only; the UI phase column below is the connected implementation owner. A range means later offline or feature follow-through, not permission to run those phases now.

Every catalog entry is covered explicitly, including events and internal/external work that must not become driver buttons. Role labels describe resource scope, not hard-coded role grants. Enforce the catalog capability family or its documented own-driver alternative **and** tenant/branch/own-driver/source/device/lifecycle bounds. As the canonical coverage states, `execution.own` is the own-work alternative to staff `monitor.read`, `planning.manage` and `location.review`; it does not grant access to other drivers. Report/export/correction still use their named scoped capabilities. P06/P08 finalize enforcement. Staff never gain execution through monitor/planning access. External operations live in native ERP/connector/operator tooling; this specification does not build an ERP.

## Actions, reads and focused overlays

| ID | Role / eligible state | Page or overlay | Action, required input and recovery/waiting | Canonical operations | UI phase | Requirements |
| --- | --- | --- | --- | --- | --- | --- |
| A01 | Signed out company user | `/login/company` | Bootstrap CSRF silently, enter company code, continue to trusted issuer username/password; unknown code has inline correction, unavailable context offers retry. Company choice does not grant access. | `session.bootstrap`, `session.resolveCompany`, `session.beginLogin`, `session.completeLogin` | P07 | R-05 R-06 |
| A02 | Signed out independent driver | `/login/independent`, `/register`, `/verify-email` | Phone/password login through A01 issuer flow; create separate account with recovery email, verify before activation per P07 policy. No SMS claim. Invalid fields retain values; verification waiting has resend/recovery guidance only as supported by issuer. | `account.registerIndependent`, `account.verifyRecoveryEmail` | P07 | R-05 R-07 |
| A03 | Signed out or expired session | `/recover`, `/recover/complete` | Request email recovery, neutral acknowledgement; expired proof returns to recovery. Do not reveal account existence. Company recovery follows company issuer. | `account.beginRecovery`, `account.completeRecovery` | P07 | R-06 R-07 |
| A04 | Authenticated own account | `/account`, authorized shell | Read own account/context and authorized branches; auto-use sole context, explicit selector for several. Session refresh is background; pending queue survives expiry. Logout/switch uses A41 guards. | `session.getContext`, `account.getStatus`, `session.refresh`, `session.logout` | P07 P35 | R-03 R-04 R-41 |
| A05 | Independent driver, own pre-execution intake | `/tasks`, `/tasks/new`, `/tasks/:taskId/edit` | **P09 verified locally:** list/open/create/revise name, phone, written address, optional collection/instructions through real APIs/PostgreSQL. Address-only input is visibly blocked and draft/action identity survives failure/back navigation. The confirmed-coordinate contract exists, while real pin UI remains P11/P28. Revision/departure guards reject unsafe edits; no item rows/branch fields. | `task.listIndependent`, `task.getIndependent`, `task.createIndependent`, `task.reviseIndependent` | P09 verified; P28 later journey integration | R-05 R-14 R-15 R-57 R-63 R-65 |
| A06 | Native ERP, authorized source before departure | External native task/preparation view; Tawsel `/day` upcoming read-only | Submit generic source snapshot then prepare; required allocation/identity/contact/location policy errors stay on native form. Await Tawsel acceptance; preparation never implies held stock. | `intake.getTask`, `intake.listTasks`, `intake.submitSnapshot`, `intake.prepare` | P27 (API P10) | R-09 R-10 R-50 |
| A07 | Native ERP assignment actor, predeparture | External native assignment/batch result | Definitive received assignment for ERP-selected driver; batch preview names resulting remaining-stop count. Recover uncertain result with same identity; reject whole over-limit batch, never partial silent backlog. | `assignment.receiveBatch`, `intake.getBatchResult` | P27 (API P10) | R-08 R-10 R-13 |
| A08 | Native ERP assignment actor, predeparture | External native assignment view | Withdraw/reassign normally without mandatory reason/driver checklist. Start race may deny departed edit; show locked state. Priority update only before departure. | `assignment.withdraw`, `assignment.reassignBeforeDeparture`, `intake.setUrgencyBeforeDeparture` | P27 (API P10) | R-11 R-12 R-25 |
| A09 | Scoped staff predeparture or assigned driver own allowed pin correction | `/tasks/:taskId/location`; mobile page | Read original address/provenance, search/select candidate or manually place pin, explicitly confirm. Missing pin explains blocker with direct edit; preserve original source. Staff loses edit after departure, driver correction remains. Confirmed result returns to initiating task/preview. | `location.list`, `location.getSnapshot`, `location.searchCandidates`, `location.confirmPin`, `map.getAssetConfiguration` | P11 P28 P30 | R-19 R-20 R-43 |
| A10 | Scoped planning actor; own driver or predeparture staff | `/prepare`, `/workspace/drivers/:driverId/prepare` | Choose confirmed first/manual origin, car/motorcycle/bicycle, service estimate (default 600s/customer), last-customer or permitted fixed/branch endpoint. Save draft without starting. Missing origin/pin/mode highlights specific field. | `routing.getVehicleProfiles`, `planning.saveDraft` | P28 (API P12 P13) | R-20 R-21 R-22 R-23 |
| A11 | Same planning scope, eligible work | Same preview page | Preview/replan; show job queued/running/partial/failed/obsolete and affected tasks. Keep accepted receipt/outcome and current target. Read stored plan/forecast; retry current inputs or use explicit manual route. No provider timer success or dispatcher publication button. | `planning.requestPreview`, `planning.requestReplan`, `planning.getJob`, `planning.getPlan` | P28 (API P13) | R-13 R-25 R-28 |
| A12 | Own driver or predeparture planner, valid eligible stops | Preview manual-order section | Select complete eligible manual sequence with move-up/down plus drag alternative. Keep current/urgent/earliest/endpoint constraints; unreachable urgent work stays explicit. No unlabelled straight-line “road route.” | `planning.setManualOrder` | P28 (API P14) | R-13 R-25 R-28 |
| A13 | Own driver; synchronized ready plan, no active round | `/day` ready card; `/prepare` if changes needed | “ابدأ الجولة” is single start; inputs already reviewed, no extra approval. Missing sync/storage/pin/origin has direct recovery; connection required. Await authoritative start before active/download. Capture first forecast and departure lock. | `round.prepareStart`, `round.start`, `round.getCurrent` | P28 (API P15) | R-12 R-27 R-38 |
| A14 | Own active-round owner; no current or heading | `/rounds/:roundId`, `/stops/:attemptId` | “اتجه للعميل” explicitly selects eligible stop; an explicit change replaces heading. Do not auto-select after result or replan. Arrived customer must resolve before change. | `current.selectHeading`, `current.getActivity` | P16 focused real component; P29 full UI | R-26 |
| A15 | Own active-round owner; heading to identified customer/branch | Same stage card | “وصلت” records explicit arrival. No GPS or contact inference; phone no-answer may resolve without arrival. P22 extends activity payload for branch target; retain distinct branch activity identity. | `current.recordArrival` | P29 P31 (API P16 P22) | R-20 R-26 R-35 |
| A16 | Own driver; compatible unresolved attempt | `/rounds/current` stage card; focused exception views later | Arrived ordinary work exposes one direct full delivery-plus-exact-collection confirmation. B2C uses optional/no collection independently. Simple no-answer remains secondary before arrival and invents no movement/fees/counters. P30 owns partial/refusal/correction choices. | `outcome.recordFull`, `outcome.recordNoAnswer`, `outcome.getRound` | P29 verified common path (API P17); P30 exceptions | R-15 R-17 R-18 R-30 R-57 |
| A17 | Own B2B driver; split permission and compatible pieces | `/stops/:attemptId/partial` page replacing sheet | Select delivered whole pieces per stable line; show delivered/remaining and exact goods+shipping. Three ×100 +50: two pieces =250 EGP; one remains return-required, never retryable partial remainder. No B2C split. | `outcome.recordPartial` | P30 (API P17) | R-16 R-17 R-18 |
| A18 | Own driver; unresolved compatible attempt | Result sheet refusal section | Refusal with shipping paid or explicit refusal-to-pay shipping; calculated collection/unpaid shipping visible. No arbitrary underpayment/refund. B2C shows simple refusal, no merchant liability form. | `outcome.recordRefusal` | P29 P30 (API P17) | R-17 R-18 |
| A19 | Own driver; eligible unresolved attempt, arrival optional | Result sheet or contextual “لا يوجد رد” | Simple no-answer; no call evidence/count/reason essay. B2B whole goods remain held/return-required, physical origin unchanged without arrival. Result doesn't automatically retry. | `outcome.recordNoAnswer` | P29 (API P17) | R-20 R-30 |
| A20 | Own driver; untouched whole task eligible for deferral | `/stops/:attemptId#defer` sheet | Choose earliest date/time; show “متاح من …” in Cairo time, not appointment promise. Missing time recovery in field. Partial remainder not offered; arrival/current constraints remain. | `task.deferWhole`, `task.activateDeferred`, `task.getEligibility` | P30 (API P18) | R-24 |
| A21 | Same owning driver; untouched whole return-required goods, before receipt/disposal | Held task detail, focused retry sheet | Explicit “أعد المحاولة”; new attempt with history, admission and prior collection validation. Over-limit stays held with cause, no lost record. No retry for partial rejected remainder/loss/damage. | `task.retryWhole` | P30 (API P18) | R-13 R-18 R-31 |
| A22 | Assigned driver after departure; own B2C as eligible | Task options urgency sheet | Set/clear urgency; explain current continues and eligible urgent work follows. Future earliest/pin blocker stays visible. Staff control absent after departure. Offline intent marks route pending. | `task.setDriverUrgency` | P30 (API P18) | R-12 R-25 |
| A23 | Own driver, active round | `/rounds/:roundId/summary#end` sheet | End round with explicit current resolution/pause and held-work summary. Preserve unfinished work; no warehouse/cash confirmation. Unknown response stays pending, not a second end. | `round.end`, `workday.getSummary`, `workday.getCarryForward` | P31 (API P19) | R-29 |
| A24 | Own driver, open workday | `/day/summary#end` sheet | End day names carryover and correction cutoff; resolve/explicitly pause current. Also ends active round. Offline end pending blocks a new start until synchronized. No midnight auto-close. | `workday.end` | P31 P35 (API P19) | R-29 R-37 |
| A25 | Same driver on another phone; online | `/day#takeover`, existing round read-only | Read existing owner context then “انقل التنفيذ لهذا الهاتف” sheet with consequence. One final confirm transfers same round, never driver assignment. Fetch and save latest confirmed snapshot before enabling commands. Offline unavailable; old-phone unsent work cannot be promised recovered. | `device.getContext`, `device.takeOver`, `device.getSnapshot` | P30 (API P20) | R-38 |
| A26 | Owning driver; open day before dependent receipt/redispatch | `/outcomes/:outcomeId/correct` page | Original/effective result, new quantities/amount and difference. “حفظ التصحيح” appends once. Closed day/dependent receipt explains read-only history; no refund/source-price edit. | `outcome.correct`, `outcome.getCorrectionAvailability`, `correction.getResult` | P30 (API P23) | R-37 |
| A27 | Current owning driver; compatible former-phone evidence | `/sync/:actionId/review` page | Inspect captured facts versus accepted state; adopt through same correction bounds only. Incompatible evidence remains review/rejected with reason; staff cannot adopt. | `evidence.adoptCompatible` | P30 P34 (API P23) | R-37 R-38 R-40 |
| A28 | Own B2B driver; held return-required pieces | `/returns`, `/returns/:requestId` | Group by source branch, select offered pieces, request handover; read offered/received/unresolved subsets. “بانتظار تأكيد الفرع” after request, never received stock. Partial receipt names exact accepted subset; other goods remain held/disputed. | `return.listSourceBranchGroups`, `return.requestHandover`, `return.getRequest`, `return.getResult`, `return.checkConfirmation` | P31 (API P21) | R-32 R-33 |
| A29 | Native source-branch ERP actor with receipt/disposition grants | External native return request/detail | Confirm only physically received requested subset; loss/damage separate action and explanation. Await Tawsel acceptance and show remaining dispute. No Tawsel staff override or driver self-receipt. | `return.confirmSubsetReceipt`, `return.recordDisposition`, `return.listPending`, `return.getNativeRequest`, `return.getNativeResult` | P27 (API P21) | R-33 R-34 |
| A30 | Own B2B driver; current customer absent or heading, not unresolved arrived | `/rounds/:roundId#branch` sheet then `/branch-activity/:activityId` | Choose source branch; pause visible customer sequence in same round. Resume after authoritative receipt for claimed handed-back subset; unclaimed/unresolved others do not gate whole batch. P22 API/transaction evidence verifies the full 50-stop segment; connected branch UI remains P31. | `branch.interruptRound`, `branch.recordArrival`, `branch.resumeRound` | P31 (API P22) | R-13 R-35 |
| A31 | Native ERP assignment actor; actual branch-received portion | External native redispatch | Create new cycle/assignment linked to receipt. No old-cycle overwrite, direct driver transfer or redispatch of merely requested/lost goods. | `dispatch.listCycles`, `dispatch.createFromReceipt` | P27 (API P22) | R-36 |
| A32 | Scoped staff observer or own driver with read grants | `/workspace`, `/workspace/drivers/:driverId`; driver `/day` and active round | Coherent day/round/selected driver current, next, counts, held/prepared, ownership and freshness. Conditional reads; stale banner on failure, no regression from late response. Source-hidden details redacted. | `monitoring.getDriverSnapshot`, `monitoring.getTripSnapshot`, `monitoring.getTaskHistory`, `monitoring.getWorkdayHistory`, `monitoring.getAction` | P28 P29 P31 P32 (API P24) | R-43 R-44 R-45 |
| A33 | Scoped integration observer | External native execution view | Read recipient-scoped projection; mixed-trip private contacts/totals absent. Server view cannot know unsent driver evidence. | `integration.getExecutionProjection`, `integration.getTripProjection`, `integration.getTaskHistory`, `integration.getWorkdayHistory`, `integration.getMonitoringAction` | P27 (API P24) | R-43 R-44 R-51 |
| A34 | Own authenticated command/evidence owner | Inline pending status and `/sync/:actionId` | Resolve timeout/unknown response with same action identity. Read result versus durable evidence receipt; keep captured/received/accepted/rejected facts distinct. | `round.getStartResult`, `current.getResult`, `outcome.getResult`, `task.getEligibilityAction`, `closure.getResult`, `action.getResult`, `sync.getEvidenceReceipt`, `sync.listConflicts` | P28 P29 P32 P34 (API P05 P15 P34) | R-08 R-40 |
| A35 | Former device/current replay, owning account only | `/sync` and restricted same-account recovery | Submit original ordered actions or durably receive old-owner evidence; no stale overwrite. Missing predecessor waits, revoked access doesn't grant execution. Retain rejected evidence for permitted review. | `sync.submitActions`, `evidence.receiveFormerDevice` | P34 (API P20 P34) | R-38 R-40 R-41 |
| A36 | Own driver/scoped report viewer | `/reports/workdays/:workdayId`, round timing section | Select authorized day/round/driver/branch/outcome filters; distinguish processed/full/partial/unsuccessful/held/received. Initial/latest forecasts match attempts and scope; missing arrival/uncertain clock not fabricated. | `report.getWorkday`, `report.getRoundTiming` | P36 | R-52 R-53 |
| A37 | Authorized report exporter | Report toolbar/export status | Request Excel for same filter/snapshot/timezone; waiting/failed/expired status and reauthorized download. No new “finance” page; user text stays text. | `report.requestExcelExport`, `report.getExportStatus`, `report.downloadExport` | P37 | R-43 R-54 |
| A38 | Authorized own recipient detail | Stop contact row | Call/WhatsApp/open destination navigation; local external links only, no automatic send, call counter, heading or arrival. Missing phone disables link with correction/source guidance; missing pin links A09. | `ui.callRecipient`, `ui.messageRecipient`, `ui.openNavigation` | P29 | R-20 R-26 R-30 R-57 |
| A39 | Authorized viewer of underlying resource | All list/map/detail/filter/overlay surfaces | Select driver/branch context, filter, inspect, expand, back, copy coordinate, toggle password, select candidate or tab. Unsaved input retained; no save from navigation. Reads use explicit rows above. | `ui.filterAndInspect`, `ui.prepareDraft` | P04 fixtures; P07 P11 P28 P32 connected | R-55 R-57 |
| A40 | Own confirmed started round, storage available | Start/download status, `/account/storage`, inline save notice | Download authorized details/geometry; request persistence; journal permitted offline actions atomically with pending projection. Storage failure says not saved. Map availability separate; no offline first start. | `ui.downloadStartedWork`, `ui.requestPersistentStorage`, `ui.captureOfflineAction` | P33 | R-27 R-39 |
| A41 | Owning queued account; pending/expired/update available | `/sync`, `/account`, session/update sheet | Retry original identities, same-account login, safe logout/account switch, safe update only at pending-work boundary. Block unsynchronized exit with count and direct sync; never clear queue or merge accounts. | `ui.retrySynchronization`, `ui.reauthenticateSameAccount`, `ui.switchAccount`, `ui.applySafeUpdate` | P34 P35 | R-40 R-41 R-42 |
| A42 | Integration administrator, scoped operator | External integration setup/runbook; `/operations/integrations/:integrationId` status | Bind/configure source and version; rotate/disable credential with overlap/recovery. Never reveal secrets in routine UI. Native ERP/company admin source owns provisioning. | `integration.bindSource`, `integration.rotateCredential`, `integration.disableSource`, `integration.getConfiguration` | P08 operator; P27 mock | R-02 R-43 R-51 |
| A43 | Native ERP identity administrator | External native branches/users/roles view | Provision/disable branch and trusted-subject user; one role, inherit/allow/deny overrides, branch memberships, driver/profile reference. Status separates issuer reconciliation from acceptance. No password-hash copying. | `branch.provision`, `branch.disable`, `role.defineCapabilities`, `user.provision`, `user.setRole`, `user.setCapabilityExceptions`, `user.setBranchMemberships`, `user.disable`, `driver.provisionReference`, `provisioning.getStatus` | P27 (API P08) | R-02 R-03 R-04 R-06 |
| A44 | Scoped integration operator | `/operations/integrations/:integrationId` | Configure allowed callback, rotate signing key, inspect pending/received/failed delivery, controlled retry/replay. Show durable ERP receipt separately from application; never a driver “sync approved” button. | `integration.configureWebhook`, `integration.rotateSigningKey`, `integration.getDeliveryStatus`, `integration.getDeliveryDetail`, `integration.retryDelivery`, `integration.replayEvents` | P25 operator; P32 status | R-46 R-47 R-49 |
| A45 | Scoped receiver/connector operator | External reconciliation tooling; integration status | Recover gaps from scoped checkpoint/snapshot; report/read applied checkpoint separately from receipt. Unsupported/expired replay has actionable reconciliation, not success. | `integration.getReconciliationSnapshot`, `integration.reportAppliedCheckpoint`, `integration.getAppliedCheckpoint` | P26 operator; P32 status | R-48 R-49 |
| A46 | Owner with diagnostics.read | `/operations/health` | Inspect database/worker/Engine/integration separately and measured capacity/freshness conditions. Unavailable measurement stays unknown. No generic GPS/42ms dashboard. | `diagnostics.getHealth`, `diagnostics.getCapacityAndFreshness` | P38 | R-61 |
| A47 | Developer/local operator | Existing workspace health; no driver business page | Verify workspace only; response explicitly not Engine readiness. | `workspace.getHealth` | P01 existing | R-60 R-64 |
| A48 | Own active-round device owner | Contextual origin correction API; full location/planning UI P28/P30 | Explicit manual planning-origin correction with current origin revision; does not select/arrive or alter recipient destination. Reload on conflict. | `current.correctOrigin` | API P16; UI P28 P30 | R-20 R-26 |

## Events and automatic work — observable effects, no extra buttons

| ID | Role / eligible state | Page or overlay | Action, required input and recovery/waiting | Canonical operations | UI phase | Requirements |
| --- | --- | --- | --- | --- | --- | --- |
| E01 | Scoped provisioning recipient | A43 external status | Refresh accepted identity configuration, preserving source revision and disabled history. | `provisioning.changed` | P27 (producer P08) | R-03 R-04 |
| E02 | Own/scoped intake recipient | A05/A06 task detail | P09 persists own `task.independentCreated`/`task.independentRevised` facts in intake history; no execution/custody inference or ERP recipient is invented. P10 now records B2B `task.snapshotAccepted` durable source intent; its native consumer UI remains P27. | `task.snapshotAccepted`, `task.independentCreated`, `task.independentRevised` | P09 verified for independent events; P10/P27/P28 later | R-09 R-15 |
| E03 | Scoped assignment recipient | A07/A08 upcoming/received groups | Prepared versus received/withdrawn/reassigned; only accepted received work counts as held. | `assignment.prepared`, `assignment.received`, `assignment.withdrawn`, `assignment.reassigned` | P27 P28 | R-10 R-11 |
| E04 | Scoped task recipient | A09/A22 task and preview | Accepted urgency/pin changes with history; replan pending separately. | `task.urgencyChanged`, `task.driverUrgencyChanged`, `location.pinConfirmed` | P11 P28 P30 | R-19 R-25 |
| E05 | Scoped execution recipient | A11/A13 plan/round | Published route and authoritative start/first forecast; preview is not departure. | `plan.revisionPublished`, `round.started` | P28 | R-27 R-28 |
| E06 | Scoped execution recipient | A14–A19 current/detail | Explicit heading/arrival/result reflected from accepted snapshot; no movement from navigation or ordering. | `current.headingSelected`, `current.arrivalRecorded`, `outcome.recorded` | P29 | R-17 R-26 |
| E07 | Scoped task recipient | A20/A21 held/eligible groups | Earliest change/new admitted attempt; prior attempt retained. | `task.deferred`, `task.retryAdmitted`, `task.deferredActivated` | P30 | R-24 R-31 |
| E08 | Scoped workday recipient | A23/A24 summary | Ended round/day, explicit carryover; no cash clearance. | `round.ended`, `workday.ended` | P31 | R-29 |
| E09 | Scoped evidence recipient | A25/A34/A35 sync | New execution generation versus evidence received; receipt not acceptance. | `device.executionTransferred`, `evidence.received` | P30 P34 | R-38 R-40 |
| E10 | Scoped return recipient | A28/A29 branch status | Offered, actually received and disposed quantities stay distinct. | `return.requested`, `return.subsetReceived`, `return.dispositionRecorded` | P27 P31 | R-32 R-33 |
| E11 | Scoped branch recipient | A30/A31 branch and day | Retained paused sequence/resume and new dispatch cycle, no second active round. | `branch.roundInterrupted`, `branch.arrivalRecorded`, `branch.roundResumed`, `dispatch.createdFromReceipt` | P27 P31 | R-35 R-36 |
| E12 | Scoped outcome/evidence recipient | A26/A27 history/report | Appended correction/adoption result including rejected review; don't delete original event. | `outcome.corrected`, `evidence.adoptionResolved` | P30 P34 P36 | R-37 R-40 |
| E13 | Scoped observer/integration | A32/A45 monitoring | Replacement snapshot nonregression and separately reported ERP application. Cannot skip required missing business transitions. | `progress.snapshot`, `integration.applicationReported` | P32 | R-44 R-48 |
| B01 | Internal worker; valid planning intent | A11 job status (no direct public control) | OSRM/VROOM computation and current-fingerprint publication; partial/failure/obsolete visible. | `routing.computeRoadRoute`, `routing.optimize`, `planning.publishRevision` | P28 (workers P12 P13) | R-28 |
| B02 | External receiver, authenticated event | A45 external inbox/application status | Verify signed bytes, durable receive then independent apply; failure/gap stays recoverable. | `consumer.receiveSignedEvent`, `consumer.applyInboxEvent`, `consumer.getStatus` | P26 external | R-47 R-48 R-49 |
| B03 | External native source worker | A06–A08/A29/A43 source status | Durable native command intent delivery/status, not Tawsel internal persistence or an invented endpoint. | `source.deliverCommandIntent`, `source.getCommandStatus` | P27 external | R-50 R-51 |

No feature payload/schema, wire enum, event or capability changes are required by this mapping. Reuse Phase 02 common schemas/examples/generated client unchanged. Each backend owner completes its feature schemas before handlers; each UI owner binds to those real contracts. The action map is not an alternate wire schema and does not promote any business operation from designed to implemented.

P11 runtime coverage: location.list, location.getSnapshot, location.searchCandidates, location.confirmPin and map.getAssetConfiguration now connect to the focused review surface. B2C entry connects through its saved task. Durable location.pinConfirmed intent has no delivered-event claim. Full route preparation remains P28. See [P11 evidence](phase-11-evidence.md).

P12 API evidence (23 September 2026): A10 can consume authenticated routing.getVehicleProfiles metadata with liveVerification=not-checked. B01's routing.computeRoadRoute and routing.optimize adapters are controlled-HTTP verified; no worker/publication or planning UI is implemented yet. A10 preparation UI remains P28, B01 durable scheduling P13 and complete route policy P14. See [P12 evidence](phase-12-evidence.md).


P16 (24 September 2026): A14/A15 and the recipient/contact portion of A38 are exercised by the small real `/rounds/current` component; exact evidence in [Phase 16](phase-16-evidence.md). Complete outcome/collection/branch/offline UI remains assigned later. Manual physical-origin correction is a verified API, not a new map UI claim.

P17 (24 September 2026): A16–A19 and E06 have verified outcome APIs/intent; A16 can read effective own-round outcome/progress and A34 recovers retained outcome actions. [Evidence](phase-17-evidence.md) and [exact examples/demo](outcomes.md). The focused result/collection UI remains P29–30; this API phase adds no UI completion or browser/owner-review claim. All 159 operations are mapped.

P18 (24 September 2026): A20–A22/A34 and E04/E07 now have verified eligibility/activation/retry/urgency reads, commands and intent. All 164 operations are mapped. [Contextual server inputs](eligibility.md), [evidence](phase-18-evidence.md). No new UI or browser findings; progressive driver controls remain P29–31.
P20 (24 September 2026): A25/A26/A27/A34 may use verified device.getContext, device.takeOver, device.getSnapshot, scoped action.getResult, evidence.receiveFormerDevice and sync.getEvidenceReceipt APIs. The extra snapshot operation belongs to A25's mandatory download before enabling execution. E09 has verified durable account-only notification intents; delivery and UI consumption remain later work. Adoption stays designed for P23; full takeover/review/queue/logout UI remains P30/P33–35. No UI component or browser interaction was changed in P20. [Evidence](phase-20-evidence.md), [protocol](device-ownership.md). All 168 catalog operations are mapped.

P24 (24 September 2026): A32/A33 now have coherent conditional snapshots and scoped history/action APIs. The additional direct action read and ERP task/trip/workday reads are inspection operations, not new execution controls. All 183 operations are mapped. [Evidence](phase-24-evidence.md), [API/client/demo](monitoring.md). No UI component/render/browser claim; P32 owns polling and stale-state interaction.

P25 (24 September 2026): A44 now has six verified scoped sender APIs, signed transport, retained error/retry/replay and explicit projection-unknown state. Operator UI and P32 monitoring presentation remain future; no new browser interaction/render evidence is claimed. All 184 operations are mapped. [Evidence](phase-25-evidence.md).
