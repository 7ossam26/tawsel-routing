# Phase 27 evidence — 24 September 2026

Started from clean `37387c4` (phase 26). No applicable AGENTS.md was found in the workspace or its ancestors. Read the requested phase, current decision amendments through D-112, assigned requirement rows, master-plan authority/session/integration/UI sections, implementation ledger and actual provisioning/intake/return/dispatch/receiver/client artifacts. Existing dependency pins are retained. Runtime identifies GPT-6; exact picker variant and reasoning effort are not exposed. Requested Astra/xhigh is recorded separately, without claiming a switch; consulted `docs/phases/model-selection.md`.

Primary documentation rechecked during final review: [openid-client code/PKCE/discovery/refresh/introspection](https://github.com/panva/openid-client) and [Fastify cookie compatibility/options](https://github.com/fastify/fastify-cookie). Existing workspace versions `openid-client 6.8.8` and `@fastify/cookie 11.0.2` are reused with Fastify 5; no floating dependency or stack substitution. Actual local issuer is Keycloak 26.7.4. Browser/UI pins remain React 19.3.0, Vite 8.3.0 and the existing shared component/theme packages. Runtime checks use Node 24.19.0, npm 11.6.2 and PostgreSQL 18.4.

## Prerequisites

`node --env-file=.env.database.local node_modules/vitest/vitest.mjs run --maxWorkers=2 --project integration apps/api/test/integration/provisioning-actor.test.ts apps/api/test/integration/b2b-intake-admission.test.ts apps/api/test/integration/partial-return-correction.test.ts apps/api/test/integration/dispatch-cycle.test.ts apps/mock-erp/test/integration/receiver.test.ts` — **5 files, 94 tests passed**, 323.45s, `.local/phase-27-prerequisites-node24.log`. Real isolated PostgreSQL and previous API/receiver guarantees. No prerequisite defect found. Initial system Node 25 run was interrupted after prolonged lack of output; its pass/fail is unknown. Subsequent commands use Node **24.19.0** at `C:\Users\jo\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin`, prepended to PATH. Initial source code was prepared while the prerequisite check ran; dependent native screens started only after its passing result.

## Checkpoint A — passed before B

Added receiver-local migration `0003_source.sql`, source records/history, immutable command envelopes, local optimistic revisions, verified-native-actor audit slot, FIFO leased attempts and durable pending/accepted/rejected/review-required results. Public generated-client adapters send outside transactions. An HTTP status without a valid matching durable result remains unknown/pending, including authentication failures after possible earlier acceptance. Same IDs survive retries; no command is silently replaced. The CLI supports source-submit, source-status and source-worker. It uses only its own database and public contract/client.

`node --env-file=.env.database.local node_modules/vitest/vitest.mjs run --maxWorkers=1 --project integration apps/mock-erp/test/integration/mock-source-outbox.test.ts` — initial **3 tests passed**, 37.19s (`.local/phase-27-a.log`). Extended actual child exits after source commit/before send and after public HTTP acceptance/before local completion: **3 tests passed**, 45.77s (`.local/phase-27-a-process.log`). Other assertions cover actual transaction rollback, concurrent workers, immutable/stale/conflicting identities and 51-item whole-batch rejection with no accepted held rows. Typecheck passed. Fixture scope/subject reservation uses the real operator API; this checkpoint does not claim native OIDC/browser evidence. No unresolved dependency prevents B.

## Checkpoint B — passed before C

Native React/Vite Arabic RTL screens reuse the existing shared controls/theme/Cairo assets. Separate `erp-reference` OIDC code/PKCE, encrypted server tokens, issuer introspection/refresh, server-configured native staff subjects, CSRF and rate limits guard forms. Public Tawsel commands keep explicit service authority; the native verified subject is retained locally. UI covers trusted provisioning, inherit/allow/deny, snapshot/prepare/physical receipt assignment, predeparture removal, driver/source-branch subset receipt, separate loss/damage and fresh receipt-funded cycle. Native code has no Tawsel database/module dependency. The native listener is loopback/test-only and rejects production mode. No fault controls are placed in user flows.

`node --env-file=.env.database.local node_modules/vitest/vitest.mjs run --maxWorkers=1 --project integration apps/mock-erp/test/integration/native-session.test.ts` — **1 passed**, 11.21s, `.local/phase-27-b-auth.log`. Signed issuer protocol fixture, real independent databases/API, one-use callback, absent/forged actor/CSRF denial, issuer revocation and effective deny→inherit checked on Tawsel. Typechecks/build passed after fixing explicit pagination response/null types.

`node node_modules/@playwright/test/cli.js test -c playwright.source.config.ts` — **1 passed**, 30.4s total / 17.6s test, `.local/phase-27-b-browser-6.log`. Actual local **Keycloak 26.7.4**, Chromium, two application sessions, two separate databases and child source/receiver workers. Native admin created branch/role/user/driver and changed deny/inherit; live Tawsel session reflected both. Native forms created two shipments, prepared them (no receipt/eligibility), explicitly confirmed actual receipt, then the separate driver session used published clients to manually plan/start, report partial delivery/no-answer and request returns. Native departed controls were disabled with an explanation; subset receipt retained 1 unresolved piece on the first shipment and 3 on the second; a fresh one-piece dispatch retained the original cycle. Signed receiver checkpoints advanced to applied. This is a manual public planning path, not live Engine routing evidence.

Actual captures: `output/playwright/phase-27-native-admin.png`, `phase-27-departed.png`, `phase-27-subset-mobile.png`, `phase-27-subset-desktop.png`; interaction and schema-valid results in `.local/phase-27-browser-evidence.json`. Mobile 390×844 and desktop 1366×768, reduced motion, explicit input/checkbox gates, readable RTL and no horizontal overflow. Owner/physical-device review remains outstanding.

Earlier browser attempts exposed: disposable users missing required email (fixed fixture); existing ignored ERP client secret differed from the actual issuer client (reconciled local config by reading the existing credential, no issuer rotation); exact select labelling (explicit accessible name added); combined read/write rate bucket exhausted by fast acceptance polling (separate entry 20/minute, writes 60/minute, reads 600/minute). Failed logs `.local/phase-27-b-browser.log` through `-5.log`; no passing stub substituted. The secret mismatch is the small prerequisite configuration repair discovered by the actual browser check. No unresolved dependency prevents C.

## Checkpoint C — passed before final gates

`npm run receiver:package` then `node --env-file=.env.database.local --import tsx scripts/source-demo.ts` — **passed**, logs `.local/phase-27-package-2.log`, `.local/phase-27-standalone-2.log`; redacted structured capture `.local/phase-27-standalone-evidence.json`. The independently installed copy at `C:\Users\jo\AppData\Local\Temp\tawsel-p27-consumer-1dXyfw` contains compiled public client/contracts, consumer, migrations, UI and portable checkers. Its npm install reported zero vulnerabilities. Module probes for `@tawsel/api` and `@tawsel/shared` fail as expected; consumer environment has only its own database and scoped public credentials. Internal operator setup is separate and labelled.

Actual Keycloak driver login, separate API/consumer processes and separate restricted databases prove native provisioning → two snapshots → preparation with no custody → explicit receipt assignment → public manual plan/start → partial delivery/no-answer → native subset receipt → separate remaining-piece loss → one-piece new cycle/preparation/receipt. Delivered 1, reported 15000 minor, actually received 1; the other shipment retains 3 unresolved return pieces. A departed withdrawal is durably rejected. The source change committed during API downtime survives the consumer restart and retries action `5d1efaa0-6199-4ea0-bb1f-2301c537f17a` unchanged. Both API and receiver processes restart twice; recovery waits for normal backoff/leases, without database repairs. Signed receiver conformance reports **24 unique events and 10 negative checks**, with received/applied state and projection counters verified over real HTTP.

The first standalone attempt timed out at the genuine OIDC callback because a Playwright virtual proxy could not complete the cross-origin navigation. Replaced only the harness proxy with a real local HTTP proxy; no identity stub was substituted. Failed run `.local/phase-27-standalone.log`. The portable checker and staged setup are documented in `docs/erp/consumer-quickstart.md`; ownership, exact reference mappings and ordered real-ERP slices are in `source-protocol.md`, `field-and-status-mapping.md` and `ERP-PLANNING-INPUT.md`. No unresolved dependency prevents the final gates. Phase 28 is not executed.

## Final review and repository gates

Source status now reads records, commands and truncation count in one repeatable-read snapshot so a concurrent completion cannot expose contradictory status within a response. Actual accepted/rejected source results were added to canonical examples; negative cases reject phantom acceptance and a status contradicting its durable receipt. Native status notices now follow the command result instead of continuing to say waiting after acceptance. Production/nonloopback native mode is tested as forbidden.

- `npm run audit`, `npm run lint`, `npm run contracts:lint`, `npm run contracts:check`, `npm run typecheck` and `VITE_TAWSEL_API_BASE_URL=http://127.0.0.1:3001 npm run build` passed under Node 24.19.0. Audit: **zero vulnerabilities**. Canonical inventory: **26 schemas / 247 valid / 150 invalid examples / 185 operations**. Logs `.local/phase-27-final-*.log`, `phase-27-lint-2.log`, `phase-27-typecheck.log`. Existing callback-302 and MapLibre large-bundle warnings remain.
- Final native browser rerun: `node node_modules/@playwright/test/cli.js test -c playwright.source.config.ts` — **1 passed**, 1.4m total / 29.3s test, `.local/phase-27-final-browser.log`. Captures refreshed and mobile/desktop RTL inspected. Added explicit private harness shutdown so Windows disposes its own databases and users before Playwright terminates the process tree.
- Review regression: same browser command — **1 passed**, 52.5s total / 33.7s test, `.local/phase-27-browser-review-final.log`. Valid external reference `demo/one` now keeps its complete local revision key; revising a new-cycle source snapshot replaces the form's cached snapshot instead of reopening old values. The browser reopens that edit and verifies the latest recipient. Every save waits for the visible accepted notice; outbound counters and received/applied checkpoints are visible together. `output/playwright/phase-27-integration-status.png` records this state. Unknown terminal rejection copy no longer promises an automatic retry. Lint, all typechecks and the native production build passed again after these fixes (`phase-27-lint-final.log`, `phase-27-types-final.log`, `phase-27-native-build-final.log`).
- `python scripts/check-ui-spec.py` passed: 18 original hashes preserved, 64 action/effect rows cover 185 operations, 33 designed states and contrast/negative checks. `git diff --check` passed. This specification check does not replace actual browser or owner acceptance.
- Initial lint included generated `ui-dist` JS; excluded this build output like the existing `dist` exclusion, then lint passed. Initial specification checker still allowed only P07–26 promotion; added the two actually verified P27 operations, then it passed. No runtime invariant was relaxed.
- An extra final copied-bundle rerun, launched alongside the full suite, timed out at the receiver's 15-second startup deadline (`.local/phase-27-final-standalone.log`). The complete earlier C proof above remains separate. Startup failure cleanup was tightened and the standalone orchestration receives a 30-second startup allowance; the rerun result is recorded below when complete.
- Removed **13 verified inactive test databases and 13 disposable P27 issuer users** left by earlier Windows-aborted browser/standalone attempts, matching reserved issuer subjects and consumer scope before deletion. All unmatched/active databases and the pre-existing `tawsel_test_c14b51b674f643efb422785702a3ebc3` fixture were preserved. No application/Engine database was changed. `.local/phase-27-cleanup.log`.
- First full `npm run test:ci -- --maxWorkers=2`: **41 files passed / 1 failed; 866 tests passed / 1 timed out**, 1371.16s. The only failure was the existing P20 real database/multi-migration upgrade at its default 10000ms limit (19.498s observed, during competing demo workload). Its scoped timeout is now 30000ms, matching the neighboring process/demo allowance; every preservation/takeover assertion is unchanged. No product timing guarantee is relaxed. A complete quiet rerun follows the final standalone proof below.

Final standalone rerun passed after `node scripts/build-receiver-bundle.mjs` then `node --env-file=.env.database.local --import tsx scripts/source-demo.ts`: `.local/phase-27-package-final-2.log`, `.local/phase-27-standalone-final-2.log`. Independent installed directory `C:\Users\jo\AppData\Local\Temp\tawsel-p27-consumer-FcbTBd`; **24 unique signed events / 10 negative checks / delivered 1 / reported 15000 / received 1 / two restarts each side**. Action `34310066-d2f6-4b90-9619-b5cd7c4e25fc` survives downtime unchanged. Native/code review changes are included in this bundle. Disposable databases/user and temporary conformance/API credential files were cleaned up; the installed public artifacts remain. No database recovery repair was used.

Final full suite rerun **passed: 42 files / 867 tests**, 1063.04s: `npm run test:ci -- --maxWorkers=2 --reporter=verbose`, `.local/phase-27-suite-final-2.log`. The previously timed-out upgrade passed in 8.5s with all assertions intact. This includes the source's real transaction/HTTP/process checks, native session/revocation checks, canonical examples and public dependency boundary. No Phase 28, commit, push or publication.

Final browser recovery review **passed: 1 test**, 52.5s total / 34.6s test (`.local/phase-27-browser-recovery.log`). The server really commits before Playwright discards its HTTP response; native recovery reuses the original stored ID, creates no second command, clears the browser journal and displays its accepted result. The notice tracks that recovered ID, while definite local validation/conflict responses release the uncommitted browser journal for a reviewed correction. Network failure and invalid-form copy are Arabic. Lint, native/UI/script typechecks and native build passed again (`phase-27-lint-recovery.log`, `phase-27-types-recovery.log`, `phase-27-build-recovery.log`). No new backend change follows the passing full suite. The final bundle refresh includes these verified UI assets and the final evidence.

The actual local Keycloak service started for the checks was stopped afterward. Nine obsolete temporary P27 credential files were removed; compiled standalone artifacts and redacted captures remain. Unmatched older marked fixture databases were preserved rather than guessed to be disposable. No claim of zero pre-existing fixtures or production readiness is made.

## Changed artifacts and Phase 28 handoff

- `apps/mock-erp/migrations/0003_source.sql`, `0004_native_sessions.sql`, `src/source.ts`, `native-auth.ts`, `native.ts`, `app.ts`, `main.ts`, `config.ts`; `ui/`, Vite/TypeScript/package wiring. Existing P26 inbox/projection/reconciliation remains intact.
- `apps/mock-erp/test/integration/mock-source-outbox.test.ts`, `native-session.test.ts`, source/process fixtures and boundary checks; `tests/source-browser/`, `playwright.source.config.ts`, `scripts/source-browser-server.ts`; source API/process helpers.
- `tests/erp-conformance/source.ts`, `source-driver.ts`, `scripts/source-demo.ts`, bundle builder and npm entry points. The portable consumer runs independently; operator-only setup is not shipped.
- `contracts/source.schema.json`, OpenAPI/operation catalog/captured examples, `packages/api-client/src/source.ts`, generated client/reference/coverage, ownership checks and source documentation.
- ERP quickstart, protocol, worked field/status mapping, planning input/index, implementation ledger, requirement/action coverage, current guides and actual PNG captures.

Phase 28 may rely on the private mock's native provisioning/snapshot/preparation/physical receipt/removal/subset/disposition/new-cycle source flows, durable immutable command retries and public source status, P26 signed inbox/projection/checkpoint recovery, and the exact two-task portable/native proofs. Full driver preparation screens are still P28 work. The native form is deliberately single-line/EGP with a small role/branch editor; the public adapters retain broader canonical payloads. No real ERP schema/identity choice, production release/TLS/SMTP, physical-device/owner acceptance, live Engine routing, measured freshness/load, stock valuation or settlement is claimed.
