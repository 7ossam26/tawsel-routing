# Phase 38 — Owner diagnostics and measured freshness/capacity

26 September 2026. Starting HEAD `488dbd1` (Phase 37), clean working tree. No applicable AGENTS.md found. Runtime identifies GPT-6; exact picker suffix/reasoning is not exposed, so the recommended `gpt-6-astra / high` is not asserted as observed. Model selection guide consulted. No delegation, deployment, commit or push.

## Prerequisites

Read master-plan sections 14/17, latest discovery amendments through D-112, phase index/model guide/decision map, R-44–48/R-61/R-65, implementation status, P37 evidence and actual monitoring, outbox, receiver, source, replay, export and browser code. The first prerequisite run failed to import the already pinned ExcelJS package (missing local installation); `npm install --ignore-scripts --package-lock-only=false` restored 107 packages without changing the lockfile. Its npm shim still used Node 25; all following test commands explicitly invoke bundled Node 24.19.0. No package upgrade was introduced.

The rerun passed monitoring, outbox/inbox recovery, native source and export (21 tests); outbox sender passed 13/14, with its upgrade inventory failing because migration 0028 was added while the suite was running. This ordering error is recorded: the migration assertion was updated and the prerequisite/focused checks rerun before checkpoint B. The initial replay filename selected no file; the actual `offline-device-takeover.test.ts` is included in the corrective run.

## Checkpoint A — Observability

Added payload-free request/action correlation, bounded process timing (HTTP/query/transaction/pool/COMMIT/Engine/export), a dedicated operator-only diagnostic API, queue/lease ages, recent worker loop observations, database pressure/size, process/host/disk metrics, and action-to-event/checkpoint trace. Liveness remains independent from database readiness. Engine availability and backup restore readiness explicitly remain unverified until separately probed/rehearsed. Receiver acknowledgement never becomes an application claim. Migration 0028 is additive; earlier migrations and Engine assets are unchanged.

Focused result: API typecheck passed. Explicit Node 24 `node --env-file-if-exists=.env.database.local node_modules/vitest/vitest.mjs run --project integration apps/api/test/integration/diagnostics.test.ts apps/api/test/integration/outbox-sender.test.ts apps/api/test/integration/offline-device-takeover.test.ts` passed 3 files / 29 tests in 85.77 seconds before checkpoint B. Dependencies: no target host credentials/inventory were supplied; production capacity, live Engine suitability and restore readiness cannot be inferred from local tests.

## Checkpoint B — Measurement harness

Implemented deterministic alternating headings through actual HTTP/CSRF/current-activity transactions, real MonitoringPage/one-second polling/Chromium render capture, actual signed outbox and separate restricted receiver databases/projection COMMITs. Setup/identity remain labelled fixtures. Async-local COMMIT brackets avoid treating the pre-COMMIT receipt timestamp as exact commit time. Browser min-round-trip offset/end-drift probes, database clock observations, raw samples, bounded failure accounting and nearest-rank p50/p95/p99 are recorded. Native sender retry, receiver outage, frozen Chromium and explicitly simulated unsent dispatch hold remain separate conditions.

Focused accounting tests: **2/2 passed**; scripts and web typechecks passed. Smoke produced 4 visible / 10 applied healthy samples and correctly returned **insufficient-samples**. The first baseline and ramp exposed a harness missing-map-route defect (108/120 HTTP 404s), so they are retained as `*-missing-map-route.json` partial evidence, not full healthy-view runs. The actual existing map asset route was registered and the baseline/ramps repeated. No fixture map/Engine response was substituted. Corrected run summaries and final checkpoint results are in [performance](verification/performance.md).

Independent live probe: `node --import tsx scripts/engine-demo.ts --live` exited **1**, all nine configured profile/operation probes unavailable; Docker inventory unavailable. Historical Engine versions/benchmarks are not new observations. No map import, container start or mount edit occurred. This prevents a live Engine/target capacity claim but does not prevent stored manual-round execution/freshness measurement. The owner runbook/terminal view and explicit saturation investigation follow this evidence; target-host work remains Phase 39.

The map-enabled pre-optimization baseline completed with 100 visible/250 applied healthy samples, no HTTP errors, p95 upper bounds 1928.11/412.88 ms and 866 tile-range requests. The following ramp stopped after its first progress log; later its process was absent and PostgreSQL refused connections. No final report or completed-ramp success is inferred. The exact interruption cause is unknown. The dedicated PostgreSQL was restarted with the existing lifecycle script. Harness review added a five-second frame-wait bound and per-wave durable progress/owned-database journals; interrupted evidence is retained separately.

## Checkpoint C — Owner diagnosis and measured change

Added the read-only terminal operations view and exact action trace runbook. The baseline showed repeated map reconstruction: every replaced snapshot/current selection recreated WebGL, markers and tiles even when geographic content was unchanged. Changed map lifecycle to depend on target/road content; current/selected/disabled marker state updates existing elements and callbacks use current props. No polling period, business rule, provider response or map data changed.

The same five-driver/two-observer/50-wave benchmark after map reuse produced 100/250 complete healthy observations, p95 upper bounds **1095.23 ms visible / 393.03 ms applied**, with tile-range requests **866 → 18**. Browser capture confirmed the same canvas remained and real marker selection responded, with no desktop overflow. This is one local before/after comparison, not a causal confidence interval or a capacity claim; ERP p99 varied upward. Two additional intentional HTTP 401s are the ordinary-ERP diagnostics-isolation check, outside the measured workload. Final ramp/stress, schema/regression checks and limits follow in the performance report/final verification below.

Final ten-driver/five-observer run: 100 visible/200 applied healthy observations, p95 upper bounds **1066.29/1397.69 ms**, zero failed workload samples. Fifteen-driver/ten-observer run: 100/150 healthy observations, p95 **1335.20/6309.48 ms**; the ERP target is **missed**, not hidden. Explicit ten-connection/six-second pool exhaustion produced 29 waiting requests, 15 failed commands, ten failed dependent view observations and one worker-loop error. The natural workload and injected failure populations remain separate. Mouse/keyboard marker selection, retained canvas, idle conditional freshness and desktop/mobile overflow checks passed in the final ramp/stress browser runs. Actual mobile map/list/history capture was visually inspected; no physical-device or owner approval claim.

One intermediate ramp failed a post-load marker assertion because it assumed task creation-array order matched database display order. Corrected the harness to compare selected list and marker ordinal, retained its progress/log, then reran. This is a test assumption repair, not a relaxed map-selection invariant. Map review also retained a bounded ten-second retry after actual map failure; healthy polls still reuse the map.

## Final verification and review

The default four-worker `npm run check` passed audit (two existing moderate ExcelJS/uuid findings), lint, OpenAPI lint, canonical generation consistency and all workspace/script typechecks. Its integration phase produced failures across diagnostics and existing monitoring/reporting/device/outcome/outbox tests on this shared Windows host; it was interrupted, so **the full suite is not passed**. A first serial diagnostics rerun passed 8/9, with the Engine-outage test preserving accepted/replayed state but receiving database status unavailable. PostgreSQL logged a real statement timeout in the database metadata/size query. This was investigated rather than weakening the assertion or lengthening the production deadline.

Actual `EXPLAIN ANALYZE` on the retained interrupted-ramp fixture measured 1628.77 ms wall time / 1526.14 ms execution for the first database status/size query, versus about 311 ms warm execution; activity/lock subplans were below one millisecond. The filesystem size traversal must not erase valid SQL readiness. The optional size query now has a savepoint: timeout/permission failure leaves `bytes: null` and preserves successful readiness/queue evidence. Other query failures still return unavailable/null observations. The 1.5-second budget is unchanged. Canonical nullable size and CLI wording match this behavior. These final diagnostic/retry refinements follow the reported load runs; historical percentiles are not relabelled as new measurements.

After that repair, explicit Node 24 `node --env-file-if-exists=.env.database.local node_modules/vitest/vitest.mjs run apps/api/test/integration/diagnostics.test.ts apps/api/test/fast/performance-accounting.test.ts packages/shared/test/fast/contract-foundation.test.ts --maxWorkers=1 --testTimeout=30000` passed **3 files / 457 tests**, 52.65 s. Authorization, actual listening HTTP/CLI, redaction, independent blocking, real Engine-outage durability, export gauges, measurement accounting and canonical assertions all passed. The earlier concurrent focused invocation had passed 459/460 with one diagnostic timeout; that failed run is retained, not counted as a pass.

Read-only inventory matched the early interrupted ramp by ten unique Phase 38 fixture issuers and migration timestamp `2026-09-26T02:20:53.086Z`, then linked its ten receiver scopes by tenant. Cleanup verified exact database markers/owners and no active sessions before dropping only those eleven databases and ten dedicated roles, without FORCE. Other databases were preserved. Successful harness runs close their own randomly named databases. Inventory, query plans, cleanup record and command logs remain under ignored `.local/performance/`.

Final `node --env-file=.env.database.local --import tsx scripts/performance-load.ts smoke` exited **0**. It ran five drivers/two actual Chromium observers, real APIs/database/signed receiver, all three separate fault conditions, retained-canvas mouse/keyboard behavior and 390px mobile overflow checks. An intercepted map-style request was actually aborted; the fallback appeared and the map returned through its bounded retry. HTTP count 186 included two intentional ERP-token 401s, zero client timeouts and zero worker-loop errors. Idle revision stayed 3 while the successful refresh time advanced. Four healthy view/ten ERP samples remain **insufficient-samples**, not a target pass. [Final smoke raw evidence](verification/performance/smoke.json) is separate from historical baseline/ramp/stress; desktop baseline and mobile stress screenshots were visually reviewed.

Reproduction commands used supported Node 24 with its directory first on PATH: `node --env-file=.env.database.local --import tsx scripts/performance-load.ts baseline`, then `ramp`, then `stress` (never concurrently), and `node --import tsx scripts/performance-report.ts`. The report generator recalculates each summary from raw samples before copying evidence and SHA-256 hashes. Recursive evidence-key scanning found no authorization/cookie/token/password/secret or recipient payload fields. No package-lock, Engine dataset/mount or original design export change was made.

Serial regression command: `node --env-file-if-exists=.env.database.local node_modules/vitest/vitest.mjs run --project integration apps/api/test/integration/monitoring-snapshot.test.ts apps/api/test/integration/outbox-sender.test.ts apps/api/test/integration/offline-device-takeover.test.ts apps/api/test/integration/report-export.test.ts apps/api/test/integration/command-transaction.test.ts --maxWorkers=1 --testTimeout=30000` passed **5 files / 62 tests**, 212.32 s. This includes actual independent-connection locking/atomicity and post-commit replay, not mocked transactions.

The remaining specific failures from the interrupted concurrent run were rerun using `node --env-file-if-exists=.env.database.local node_modules/vitest/vitest.mjs run --project integration apps/api/test/integration/reporting-query.test.ts apps/api/test/integration/retry-deferral-urgency.test.ts apps/api/test/integration/device-ownership.test.ts apps/api/test/integration/outcome-progress-outbox.test.ts --testNamePattern='actual branch interruption|future deferral stays visible|another account and forged account|real two-session HTTP/client|partial resolves arrived current' --maxWorkers=1 --testTimeout=30000`: **4 files / 5 selected tests passed, 54 skipped**, 41.45 s. Every failure printed before interruption is now covered by a passing serial check. This does not establish a pass for the uncompleted full suite, nor make host contention disappear.

Changed implementation areas: `apps/api/src/diagnostics/`, shared transaction/Engine/export timing, API bootstrap and worker scripts; additive migration 0028; owner client/CLI and public-only ERP isolation checker; actual browser load/accounting/report scripts; MonitoringPage measurement attributes and retained-map lifecycle. Canonical schema/examples/OpenAPI/operations, generated client/reference/coverage, migration inventory tests, ERP planning/mapping/quickstart, operations/monitoring/reporting and phase/status documents are updated. The concrete Phase 39 handoff is the runbook's final section; it may rely on these local artifacts and verified boundaries, not a target capacity or working Engine claim.

Final gates passed sequentially: `npm run lint`, `npm run contracts:lint`, `npm run contracts:check`, `npm run typecheck`, `npm run build`, invoking `C:\Program Files\nodejs\node_modules\npm\bin\npm-cli.js` with bundled Node 24 and that runtime first on PATH. Canonical inventory: **31 schemas / 268 valid / 167 invalid examples / 187 operations**. API/shared/operator client/web/mock ERP production builds passed; PWA precache generated **17 assets / 2,434,816 bytes** without API/map runtime caching, and production fixture isolation passed. Vite retained a >500 kB chunk warning (main 600.76 kB, MapLibre 1,057.62 kB uncompressed); this is recorded, not a fresh mobile startup benchmark. Final `git diff --check` passed. No deployment, commit or push.
