# Phase 18 evidence — 24 September 2026

Started from clean HEAD `8fdc331` (`phase 17`). No applicable AGENTS.md found in repository or ancestor directories. Original Engine datasets/mounts and Stitch exports are untouched. Runtime identifies GPT-6; the exact Codex picker variant/reasoning effort is not exposed, so the requested `gpt-6-astra` / `high` setting cannot be attested. Consulted `phases/model-selection.md`; no claim that prompt text changes the picker.

## Prerequisites

Read the phase prompt, phase README/status, master-plan sections 7/9, current discovery amendments/decision map (including D-112), assigned coverage rows, and actual P14 planning, P16 current/ownership and P17 outcomes/quantities/collection code. `npm run db:local:start` used the existing isolated loopback PostgreSQL 18.6 service. `npm run test:integration -- apps/api/test/integration/planning-publication.test.ts apps/api/test/integration/current-activity.test.ts apps/api/test/integration/outcome-progress-outbox.test.ts`: **3 files, 59 tests passed**, 46.43s. Each fixture uses a fresh disposable database. No missing material prerequisite.

## Checkpoint A — completed before B

Added additive migration `0015_retry_deferral_urgency.sql`: latest attempt uniqueness, single explicit successor, retained identity/history, driver execution options, immutable eligibility history and a receipt/disposition dependency hook for P21/P22. Closed `eligibility.schema.json` defines deferral, retry, explicit activation and urgency, revision expectations, allowed-action blockers and recovery. `policy.ts` / `state.ts` derive decisions from actual source/cycle/outcome quantities and ownership. Existing resolved attempts are never rewritten.

`npm run test:fast -- apps/api/test/fast/eligibility-policy.test.ts`: **3 passed**. `npm run test:integration -- apps/api/test/integration/retry-deferral-urgency.test.ts`: **1 passed**, 2.36s. The latter migrates real PostgreSQL, records two delivered / one rejected using the P17 command, proves all customer-revisit actions denied, and rejects an unsupported call-counter field. `npm run contracts:generate`: **16 schema files; 148 valid / 98 invalid existing examples; 164 catalog operations**, generated successfully (new command examples added at connected checkpoint).

Explicit semantics: deferral suspends unresolved whole work until explicit activation at/after earliest time with capacity; return-required work remains return-required until explicit retry. Current customer cannot be deferred silently; urgency may change without replacing current. B2C uses simple whole-task outcomes, without a goods/custody ledger. Receipt/disposal command producers remain P21/P22; dependency rows here are an enforcement hook, not a claim of physical receipt implementation. Checkpoint B/C and final verification pending at this entry.

## Checkpoint B — completed before C

Added kernel-backed eligibility commands/routes/client and driver/workday/assignment/task locking. Retry creates one successor and active admission; deferral pauses reservation; activation restores it only after current eligibility/capacity checks. Durable history, audit, outbox and replan commit together. Planning reads only latest attempts and respects deferred state independently of wall-clock passage. Current target/origin and first forecast remain untouched. Outcome reads retain all attempt history and cumulative reported fees while counting each latest resolved task once. Pin changes cannot reactivate deferred/resolved work.

`npm run test:integration -- apps/api/test/integration/retry-deferral-urgency.test.ts`: **7 passed**, 12.59s. Includes real independent-connection duplicate races, two retries competing for stop 50 after 49 public intake/admissions, full rollback at domain/progress/audit/outbox/result checkpoints, fee-preserving successor delivery, visible future urgent work, protected current, invalid manual selection and explicit activation after a labelled elapsed-time database fixture. `npm run typecheck`: passed. Existing P17 suite plus A: **15 passed**, 18.24s before added B cases.

Failed checks retained: first B run had 5 failures (legacy P13 immutable-attempt trigger blocked updating the new latest marker; manual-order rejection assertion used the wrong API shape; race barriers then timed out because retry never reached domain completion). Replaced only that trigger in migration 0015 with identity/history retention allowing a one-way latest-marker transition. Corrected the test to assert existing `invalid_manual_order` rejection. Follow-up 6/7-case runs isolated the assertion mismatch; final 7-case run passed. No prior migration edited. Generator helper initially referenced a nonexistent client index; client modules are standalone files, so package files were updated directly. C remains pending.

## Checkpoint C — focused verification completed

`npm run test:eligibility`: **2 files, 48 tests passed**, 42.54s (13 retry/deferral/urgency cases plus 35 planning-publication cases). Added five successive retries without a cap, one lifetime shipping charge, B2C simple retry, partial/receipt/loss/damage/redispatch denial, source staff urgency denial versus driver acceptance, stale task/device rejection, CSRF/cross-tenant/revocation recovery checks, pin non-reactivation and two real delayed-provider tests for driver urgency/earliest changes. Provider HTTP responses are controlled fixtures; PostgreSQL transactions and publication are real. Dependency facts and elapsed-time boundary changes are labelled database fixtures, not implemented receipt or clock services.

`node --env-file-if-exists=.env.database.local --import tsx scripts/eligibility-demo.ts`: passed actual loopback HTTP start → refused/50 shipping → lost committed retry response → same-ID status/replay → full/300 goods, plus future urgent second task and denied activation. Typed clients and real database produced `.local/phase-18-demo.json`. `npm run test:erp:eligibility -- .local/phase-18-demo.json` and `npm run test:erp:eligibility`: passed public-only consumer assertions. Events carry original source/cycle/revisions and only the owning integration's durable intent. Transport remains future.

`npm run test:contracts`: **282 passed**; generation now reports **16 schemas, 162 valid and 107 invalid examples, 164 operations**. New examples include actual demo request/result/status/read/event shapes and nine invalid-field/revision/date fixtures. `python -X utf8 scripts/check-ui-spec.py check`: passed original-source hashes, all operation mappings, Arabic state/contrast and negative checks. No UI changed; no new browser/render/usability claim.

C failures retained: first 12-case run had one invalid pin fixture (missing canonical `confirmed` and resource reference); corrected the fixture, then 47 and final 48 tests passed. First contract check had nine missing expected-keyword annotations plus generated drift after a Windows `UNKNOWN` file-open error during generation; annotations added and generation rerun successfully. First UI map check found the new action-recovery read unmapped; mapped it to A34 and reran successfully. Typecheck passed after new client/demo. Full regression and final handoff pending at this entry.

## Final review and regression

Initial full `npm run check` with `VITE_TAWSEL_API_BASE_URL=http://127.0.0.1:3001`: **27 files / 590 tests passed**, 143.99s test duration; audit **0 vulnerabilities**, ESLint, OpenAPI lint, generated-contract check, all TypeScript checks, production API/web builds and production fixture isolation passed. Existing warnings: OIDC redirect operation lacks a 2xx response (expected redirect), and MapLibre's existing bundle exceeds 500 kB. No new dependency versions were selected.

Final review found a useful source/driver earliest distinction: the event now reports the effective maximum of source and driver earliest time, matching its read/result. Added real API cases for this, preservation of unmodified predeparture ERP urgency, and repeated unpaid refusals followed by payment (one outstanding fee, then zero). `npm run test:integration -- apps/api/test/integration/retry-deferral-urgency.test.ts`: **15 passed**, 50.24s while the full suite also ran. No failed review test; this was a source-level review finding. Full final rerun recorded after completion below.

Versions: Node **24.11.1**, npm **11.6.2**, PostgreSQL **18.6**, pg **8.23.0**, Fastify **5.12.5**, TypeScript **6.0.2**, Vitest **5.0.1**, Playwright **1.63.0** (retained pinned stack). `git -c core.whitespace=cr-at-eol diff --check`: passed. Database inventory before application migration: zero personal/company tasks, rounds, execution attempts or outcomes; no disposable test databases remained. Application migration is additive; no Engine imports/services/datasets are touched.

Remaining limits: no live Engine provider verification (manual demo plus controlled planning HTTP fixtures), no real ERP/physical device test, no new UI/browser flow, no physical receipt/disposal API, no closure/carryover, takeover, correction or signed event delivery. Original source hashes/UI specification checks are document evidence only. Dependency row fixtures establish retry denial, not a receipt implementation or its future race coverage. No commit, push, publication or Phase 19 execution.

Handoff: Phase 19 may rely on migration 0015's latest/successor constraints and immutable history; `Eligibility` commands/read/recovery; `task_execution_options`; latest-attempt planning snapshots with explicit future exclusion and capacity admission; P17 cumulative reported-collection history; `EligibilityClient`/canonical schema/examples; the real HTTP demonstration and PostgreSQL race/fault tests. It must add closure/carry-forward, closure-aware action affordances and cross-round reporting. P21/P22 must atomically append receipt/disposal dependencies under the same locks and cover both race orders; P22 must initialize overrides for new source dispatch cycles. No success in those later phases is implied.

Changed implementation paths: `db/migrations/0015_retry_deferral_urgency.sql`; `apps/api/src/eligibility/{models,policy,state,service,routes}.ts`; registration in `apps/api/src/app.ts`; latest scheduling in `planning/queue.ts`; non-reactivating `locations/service.ts`; cumulative-history `outcomes/service.ts`. Tests: new fast policy and `retry-deferral-urgency.test.ts`, extended `planning-publication.test.ts`, migration inventory/retained-upgrade expectations in `database-lifecycle.test.ts` and `planning-jobs.test.ts`.

Changed contract/consumer paths: `contracts/eligibility.schema.json`, `outcomes.schema.json`, `openapi.yaml`, `operations.json`, `examples/{valid,invalid}.json`; generated `packages/api-client/src/schema.d.ts`, `docs/reference/public-contract.md`, `docs/contract-coverage.md`; `packages/api-client/src/eligibility.ts` and package file inventory; `scripts/eligibility-demo.ts`; `tests/erp-conformance/{eligibility,outcomes}.ts`; package scripts, generation/coverage checks. Documentation: this evidence/status, `docs/eligibility.md`, all required ERP planning/mapping/quickstart files, current phase README/coverage/action map, master/discovery status and affected outcome/route-policy/client/example references. Prior SQL migrations, original Stitch exports and Engine mounts/data remain unchanged.

Final frozen implementation check: `npm run check` with `VITE_TAWSEL_API_BASE_URL=http://127.0.0.1:3001` **passed**, **27 files / 592 tests**, 120.81s test duration, 0 audit vulnerabilities, lint/contracts/types/API and web build/production fixture isolation. `npm run db:migrate` applied only `0015_retry_deferral_urgency.sql` to the local application database; post-run inventory still has zero application tasks/rounds/attempts/outcomes and no disposable databases. `npm run eligibility:demo`, `npm run test:erp:eligibility -- .local/phase-18-demo.json`, `npm run test:erp:outcomes`, and UI specification checks passed again. Final OpenAPI summaries/grouping were clarified after runtime verification; generation/OpenAPI lint and all **282 contract tests** were rerun successfully. No runtime change followed the 592-test run.
