# Phase 36 — Effective workday reports and forecast comparison

25 September 2026. Starting clean HEAD `f95c1ea` (`phase 35`). No AGENTS.md in the repository or ancestors. Runtime identifies Codex/GPT-6; the exact picker suffix and reasoning setting are not exposed. Requested recommendation `gpt-6-astra / xhigh` is not asserted as observed; the model-selection guide was consulted. No dependency change, commit, push, publication or Phase 37 execution.

## Prerequisites

Read the master plan (including section 15), discovery through D-112, current phase/decision/model guides, assigned requirement rows, implementation ledger, source report action map and original 03/05/08 images. Inspected actual planning jobs/manual/worker forecast persistence, first-start references, admissions, arrival/action observation, effective outcome/correction/receipt history, workday closure, local capture and ordered replay/auth recovery.

Supported runtime explicitly selected using `$env:PATH='C:\Users\jo\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin;'+$env:PATH`: Node 24.19.0, npm 11.6.2. Existing pinned dependencies retained.

`node --env-file=.env.database.local node_modules/vitest/vitest.mjs run apps/api/test/integration/planning-jobs.test.ts apps/api/test/integration/start-assignment-race.test.ts apps/api/test/integration/outcome-progress-outbox.test.ts apps/api/test/integration/workday-carryover.test.ts apps/api/test/integration/partial-return-correction.test.ts apps/api/test/integration/offline-device-takeover.test.ts apps/api/test/integration/offline-session-update.test.ts apps/web/test/fast/local-action-capture.test.ts --maxWorkers=2 --testTimeout=30000`: **8 files / 144 passed**, 380.99s. `.local/phase-36-prerequisites.log`. Real isolated PostgreSQL and labelled issuer/provider/IndexedDB fixtures. No material missing prerequisite.

## Checkpoint A — passed before timing implementation

Closed canonical reporting contract, current `reports.read` scope in SQL, repeatable-read workday projection, effective corrections and preserved histories, distinct shipment/attempt counts, currency-separated exact string totals and once-per-cycle piece disposition. Outcome selection retains a separate unfiltered denominator. Content snapshot identity includes current access and filters; an expected stale snapshot fails with 409. No claim that local unsent actions are server knowledge. Initial API typecheck caught an invalid test-only plans input lookup; removed it.

`node --env-file=.env.database.local node_modules/vitest/vitest.mjs run --project integration apps/api/test/integration/reporting-query.test.ts --maxWorkers=1 --testTimeout=30000`: **8/8 passed**, 30.27s (`.local/phase-36-checkpoint-a-rerun.log`); API typecheck passed. Actual intake/start/results/correction/retry/return commands on isolated PostgreSQL verify both 18-shipment examples, same-address independence, prepaid/partial/refusal/unpaid totals, immutable original history, distinct retries, once-only subset receipt/disposition, current tenant/branch/capability filtering, public HTTP filter denial and a concurrent committed outcome during a repeatable-read report. Initial run **5 passed / 2 failed** because the test used `shippingPayment: paid` instead of the actual `collected` contract enum; corrected the fixture and added the subset check. Currency separation has explicitly labelled arithmetic evidence plus actual foreign-currency intake rejection. No unresolved dependency for B.

## Checkpoint B — passed before focused UI work

Timing integration tests exposed an actual small prerequisite gap: `command_evidence` holds rejected/review envelopes, not accepted envelopes. P34 metadata retains accepted device identity but not the start observation. Additive migration 0027 captures original observation and relevant source/assignment/pin assertions in the existing immutable metadata row in the command transaction. Older rows stay null; no historical observation is invented. Timing uses retained domain action observations plus metadata device identity, and the original resolution time survives later corrections. The first B run passed 9/11; the two failing duration assertions exposed this faulty evidence lookup. The repair passed the B rerun below before C.

`node --env-file=.env.database.local node_modules/vitest/vitest.mjs run --project integration apps/api/test/integration/reporting-query.test.ts --maxWorkers=1 --testTimeout=30000`: **12/12 passed**, 53.17s (`.local/phase-36-checkpoint-b-rerun.log`); API typecheck passed. A real controlled HTTP routing provider goes through intake → worker → immutable forecast → online start, rather than inserting report fixtures as production forecasts. Verifies baseline/latest identities, delayed receipt versus physical observation, phone-only missing arrival, uncertain clocks, matching device-bound duration, correction retaining physical completion, new workload, post-resolution forecast cutoff, early unfinished end, direct timing endpoint equality, actual Cairo offset/date changes with labelled action observations and separate branch interruption/service units. No unresolved dependency prevents C. The retained-schema upgrade/rollback and broad regressions remain final checks.

## Checkpoint C — passed

The connected `/reports` and explicit workday route use the existing account boundary, component tokens and Cairo/RTL family. Counts precede expandable rounds, attempts and original/correction history. Optional personal filters and collection stay simple; company piece/fee details remain scoped. Native disclosures work with Enter and the page retains URL filters on reload. Refresh clears obsolete totals before the next authorized read; denial never retains the old report. An account-scoped local-pending notice is separate from accepted totals.

`node node_modules/@playwright/test/cli.js test -c playwright.reporting.config.ts`: **1/1 complete scenario passed**, 27.9s test time (`.local/phase-36-browser-6.log`). Actual local Keycloak, Chromium, Fastify and isolated PostgreSQL; controlled HTTP routing provider through the real worker, public source setup and accepted execution/correction commands. Verifies mobile summary → first/latest workload → uncertain phone-only attempt → original/correction history → unfinished closure; desktop authorized shared round → partial filter → reload → foreign-day 404/hidden-branch 403 → live report-grant revocation with totals removed. 320/390px widths have no horizontal overflow; desktop is 1440px and reduced motion is requested. This is a connected Vite browser test, not installed/offline PWA evidence or a production performance measurement.

Five earlier browser attempts did not complete: two strict-text selectors matched the same explanation in several fields; the next exposed implicit select labels containing option text (fixed explicit accessible names); a concurrent run reached an account-status error before staff navigation (trace contains failed session-context reads; no passing auth claim for that attempt); the fifth reached correct denial but exact paragraph matching included the retry-button text. The final alert assertion checks the visible denial and absence of collection totals. No timing threshold was increased for the final browser pass.

Visual inspection of the actual captures found readable RTL navy summary/cards and clear unfinished/unknown amounts on mobile, plus separate company amounts and selected-versus-scope denominator on desktop. Personal fee/partial controls were removed and optional filters collapsed after the first inspection. Detailed timing stacks into labelled mobile rows; IDs remain in optional reference disclosures. Keyboard, reload, counting and denial assertions establish interaction evidence beyond screenshots. No human owner or physical-phone review is claimed.

Captures: [driver summary](../output/playwright/phase-36-driver-summary-mobile.png), [timing detail](../output/playwright/phase-36-timing-mobile.png), [corrected history](../output/playwright/phase-36-correction-mobile.png), [staff filters](../output/playwright/phase-36-staff-desktop.png), [revoked report](../output/playwright/phase-36-report-denied.png). Public JSON: `.local/phase-36-browser-evidence.json`.

## Final verification

`node --env-file=.env.database.local node_modules/vitest/vitest.mjs run --project integration apps/api/test/integration/reporting-query.test.ts apps/api/test/integration/command-transaction.test.ts apps/api/test/integration/database-lifecycle.test.ts --maxWorkers=2 --testTimeout=30000`: **3 files / 42 passed**, 118.88s (`.local/phase-36-provenance-regression.log`). Includes the final 14 reporting tests, real retained-schema upgrade preserving first forecasts, unavailable old start observation, rollback after the metadata write, and immutable accepted provenance. No engine/dataset migration is involved.

Root lint and all workspace/script types passed (`.local/phase-36-lint-final.log`, `.local/phase-36-typecheck-final.log`). Initial lint rejected a throw in a cleanup finally block; moved the cleanup-path validation before the try. Audit reports **zero vulnerabilities**. UI specification checker passes original hashes, all **186 operations**, local links, contrast/state cases and negative controls (`.local/phase-36-ui-spec-final.log`); its old phase allowlist was extended only for the three report reads. Export operations remain designed.

Broad regression command `node --env-file=.env.database.local node_modules/vitest/vitest.mjs run --maxWorkers=2 --testTimeout=30000` completed **53 files / 1,012 tests: 928 passed, 42 failed, 42 skipped**, 2,195.66s (`.local/phase-36-full-regression.log`). Six failures were the missing invalid-example keywords and old available-operation allowlists, repaired above. Of the 36 integration failures, 35 were **10-second setup-hook timeouts** and one was a database connection timeout. Two beforeAll hook failures skipped 42 kernel/session tests. This run is not labelled green.

The full skipped kernel/session suites subsequently passed **42/42**, 27.71s. Every remaining non-report integration failure was selected from the completed log and rerun, with **35 passed / 184 deliberately unselected**, 384.04s, across 12 files (`.local/phase-36-integration-rerun.log`). The additional matched case comes from the name pattern; these skips are selection, not setup failures. Exact file/name/argument selection is retained in `.local/phase-36-rerun-selection.json`; the actual dispatcher command was `node .local/phase36-rerun.mjs`. Both runs used real isolated databases and unchanged business assertions. For a fresh full run, use `npm run test:ci -- --maxWorkers=1`; the checked-in integration setup-hook allowance is now 60 seconds.

The CLI timeout flags did not override the inline project's hook default. `vitest.config.ts` now explicitly allows 60 seconds for creating/migrating isolated databases while retaining the existing business-test deadline. The report's retained-schema upgrade case creates a second database inside its body, so it also has an explicit 60-second fixture allowance. Its first extended rerun passed 13/14 with one setup timeout; after the hook change, another passed 13/14 with the extended upgrade hitting the original ten-second test bound. The final result is recorded below. No assertion, transaction or source fixture was replaced with a mock.

The final fast run passed **18 files / 591 tests** but had a worker-start timeout for the three-test intake file (`.local/phase-36-fast-final.log`, 250.21s). The subsequent full contract/intake rerun passed **2 files / 449 tests**, 40.85s (`.local/phase-36-fast-repair.log`), covering that missing worker and all repaired assertions. All 594 distinct fast tests have passing evidence across these runs. The canonical generation test now allows 30 seconds for two complete byte-identical generations; its earlier 5-second timeout is recorded in `.local/phase-36-contract-rerun.log`.

`npm run lint`, `npm run contracts:lint`, `npm run contracts:check`, `npm run typecheck` and `npm run build` all passed (`.local/phase-36-final-gates.log`). Existing OIDC callback 302-only lint and large MapLibre bundle warnings remain. Build uses explicit local `VITE_TAWSEL_API_BASE_URL=http://127.0.0.1:3001`, not a deployed URL. Production fixture isolation passes. The public-response checker passed the complete browser capture (`npm run test:erp:reporting`, `.local/phase-36-consumer.log`).

`npm run db:migrate` advanced the marked local application database through its previously unapplied **0025, 0026 and 0027** migrations (`.local/phase-36-local-migrate.log`). Isolated tests independently applied/verified their own schemas. No Engine/Nominatim/OSRM import or migration was run.

Final review extended the real upgrade case with actual pre-P36 heading/arrival commands: retained clocks may support duration, but missing version assertions produce `identity-unavailable` rather than an assumed pin match. It also changed the active-round finish explanation to “work incomplete” rather than falsely saying the round ended, and repaired retry after initial day discovery fails. The final browser includes an explicitly injected initial 503 discovery outage followed by a real authorized retry. The first post-copy browser rerun exceeded the default five-second expectation while still displaying its loading state; the connected-browser allowance is now 15 seconds, with no performance/SLO claim. A one-request outage fixture was initially consumed by StrictMode's aborted read, so the fixture now holds the outage until its error is visible and removes it before Retry.

Final post-review results:

| Exact check | Observed result |
| --- | --- |
| `node --env-file=.env.database.local node_modules/vitest/vitest.mjs run --project integration apps/api/test/integration/reporting-query.test.ts --maxWorkers=1` | **14/14 passed**, 104.24s, including extended legacy identity and repeatable-read concurrency. `.local/phase-36-report-complete.log`. |
| `node node_modules/@playwright/test/cli.js test -c playwright.reporting.config.ts` | **1/1 passed**, 21.2s test / 42.3s total. All mobile/desktop steps plus initial outage → real retry and open-versus-ended copy assertions. `.local/phase-36-browser-verified.log`. |
| `npm run test:erp:reporting` | Passed final actual public capture. `.local/phase-36-consumer-verified.log`. |
| `npm run lint`, `npm run contracts:lint`, `npm run contracts:check`, `npm run typecheck`, `npm run build -w @tawsel/api`, `npm run build -w @tawsel/web` | All exit 0 after the production code/schema review changes. `.local/phase-36-after-review-gates.log`. Final web build: **17 precached assets / 2,429,220 bytes**, production fixture isolation passed. |
| `python -X utf8 scripts/check-ui-spec.py check`, `git diff --check` | Passed. `.local/phase-36-ui-spec-complete.log`. Original reference/data/config paths have no diff; HEAD remains `f95c1ea`. |

The full 1,012-test suite was not repeated after these focused repairs. All its failed/skipped cases have passing rerun evidence above. No unfinished Phase 36 implementation dependency remains; external/device and Phase 37 limits below remain explicit.

## Changed artifacts and versions

- Query/HTTP/definitions: `apps/api/src/reporting/{models,queries,service,timing,routes}.ts`, app registration; `contracts/reporting.schema.json`, OpenAPI, operations, valid/invalid examples, generated contract coverage/reference/types and public reporting client/package.
- Small provenance repair: command kernel and additive migration 0027; retained migration expectations in lifecycle/planning/device/outbox/return integration tests.
- UI: `apps/web/src/reporting-page.tsx`, shared stylesheet, production/account/day/closure/monitoring navigation. Original Stitch exports and Engine mounts/datasets are unchanged.
- Evidence/demo: real reporting query fixture/tests, `scripts/reporting-browser-server.ts`, `playwright.reporting.config.ts`, browser test/captures, public-only consumer checker, npm entry points and ignored failure traces. Canonical validation allowlists cover only released P36 reads. `vitest.config.ts` allows real database setup time; only the retained-schema fixture and byte-identical generation case have expanded individual test allowances.
- Docs: reporting definitions/handoff, this record, implementation ledger, phase/coverage/UI action/spec references, ERP planning input/mapping/consumer quickstart.

Retained pins: Node 24.19.0/npm 11.6.2, TypeScript 6.0.2, React 19.3.0, Vite 8.3.0, Fastify 5.12.5, pg 8.23.0, AJV 8.20.0, Vitest 5.0.1 and Playwright 1.63.0. PostgreSQL uses the existing marked 18.x application/test cluster and Keycloak the existing local issuer on 8085. No package upgrade or map import.

## Verification limits

Multi-currency arithmetic is tested as a labelled forward-compatible fixture; current released source/outcome writers accept EGP only and foreign-currency intake must reject. Browser and external-service evidence are distinguished above. No physical-device, live Engine, commercial ERP, owner review or deployment claim. Report pages were tested online in Chromium, not as installed/offline reports. Phase 37 must implement export lifecycle and download reauthorization using the [same authorized object and definitions](reporting.md#exact-phase-37-handoff).
