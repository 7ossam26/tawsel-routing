# Phase 39 deployment evidence — 26 September 2026

Status: local preparation implemented; no target mutation or rollout. Owner explicitly selected local preparation because no server access is available. Starting revision `50c684b` (Phase 38), clean `main`. Runtime identifies Codex/GPT-6; exact picker model suffix/reasoning is not exposed, so the recommended Astra/high setting is not claimed as observed. [Release/recovery instructions](../deployment.md) describe the prepared path and target prerequisites.

## Prerequisites and environment

Read Phase 39, master-plan sections 4/17, current discovery/decision map and requirement rows, operations, P35 update/queue rules, P38 diagnostics, actual Engine Compose, database migrations/markers, identity/provider/theme, native mock, map serving and worker modules.

Windows host; shell default Node 22.17.0 is outside this workspace's support range. Tests use the bundled Node 24.19.0 by putting `C:/Users/ahmed/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin` first on PATH; npm 11.6.2. Existing PostgreSQL 18 is under `C:/Program Files/PostgreSQL/18/bin`. No Docker executable/daemon was found. No target hostname, credentials, inventory, domains, SMTP or backup destination was supplied.

The initial PostgreSQL helper failed because PostgreSQL could not interpret the Cyrillic workspace path. Its retained `postmaster.opts` named `T:/.local/postgres-18`; the drive mapping was absent. Restored that mapping using `subst T: <workspace>` and ran the existing `T:/scripts/postgres-local.ps1 start`. No application migrations or Engine changes were run against persistent data. Disposable marked databases are used by tests. The first prerequisite attempt failed with ECONNREFUSED before the cluster was started; it is not passing evidence.

Connected prerequisite rerun: 47/49 tests passed across six files; authentication (20), offline account/update recovery (9), sender/receiver recovery (4) and diagnostics (7) passed. The database inventory expectation omitted P38's real `worker_observations` table; repaired that small prerequisite defect. The new YAML validator needed YAML merge support for Compose anchors; repaired before repeating its test.

## Checkpoint A — configuration

Separate `deploy/compose.yaml` owns app/API/three workers, one explicit migration job, retained-assets job, web and custom issuer. It contains no Engine services, host ports, import commands or writable host mounts. API/planning join a required inventoried Engine network; web/issuer alone join ingress. Database remains an explicitly provisioned dedicated PostgreSQL 18 service with verified TLS, database ownership and purpose marker. No permissive database parser change was made.

Build recipes retain lockfile dependencies and require immutable base references. The issuer recipe compiles the actual recovery provider against the matching Keycloak distribution. Container builds and runtime networking remain unrun without Docker. Required target digest pins and host paths are placeholders, not invented observations.

Focused Checkpoint A/B check: `node --env-file-if-exists=.env.database.local node_modules/vitest/vitest.mjs run apps/api/test/fast/deployment-config.test.ts apps/api/test/integration/deployment-recovery.test.ts apps/api/test/integration/database-lifecycle.test.ts --maxWorkers=1 --testTimeout=30000` passed **3 files / 10 tests** (8.61 s). This verifies negative configuration controls, immutable asset retention/collision refusal, current release-evidence requirements, fresh install, concurrent migrations, failure rollback and original command evidence after reconnect.

## Checkpoint B — release and recovery

No new SQL migration or public payload version. Existing migration 0028 is the current boundary. A deliberate DDL + division-by-zero failure after the P37 baseline rolls back both new tables and the migration ledger to 27 entries; the old command result remains readable. Replacing the unapplied failed SQL with real 0028 and racing two runners applies exactly one migration. A fresh pool sees current checksums and byte-equivalent original evidence. This is real PostgreSQL behavior; it does not prove a container restart or a disaster restore.

Release preflight requires exact config hash, immutable new/previous images, target-bound recent inventory, a recent backup plus separate-failure-domain restore report and app/identity/secrets coverage. It validates operator attestations, not the contents of backups. Example evidence fails closed. Immutable asset retention keeps old hashed JS/CSS/font/Workbox files, refuses a different file at an existing name and never deletes files during a web rollback.

Root `npm run build` passed using `VITE_TAWSEL_API_BASE_URL=http://127.0.0.1:3001` explicitly as a local fixture; all five workspace builds passed, including production service-worker generation and fixture-isolation scan. `npm run typecheck` passed. Initial lint found a missing error cause in the new asset collision path; repaired. The existing large MapLibre/main chunk warning remains. These checks are not Docker image builds or host-capacity evidence.

## Checkpoint C — local services and final gates

Registry manifest lookups resolved all five tag/digest pairs retained in `deploy/base-images.json`: Node 24.19.0 bookworm-slim, Nginx 1.30.5 alpine, Keycloak 26.7.4, Temurin 25.0.4.1_1 JDK and PostgreSQL 18.4 bookworm. These are observed registry content digests; no container execution, architecture compatibility or vulnerability scan is inferred.

Downloaded the official standalone Compose 2.39.4 Windows executable into ignored `.local/deploy-tools` and checked its SHA-256 against the release checksum file: `6b3bccfabcdd172e1d9e15d011b54c9b5b13b93b1153148108f55e4349055955`. The first checksum-line lookup missed the checksum file's `*` filename prefix and refused execution; corrected the match, verified the checksum and then ran it. Both `-f deploy/compose.yaml config --quiet` and `-f deploy/mock-local.compose.yaml config --quiet` passed using labelled fixture image digests, local empty secret-file fixtures and parser-only CPU/memory values. These values are not target configuration. No Docker daemon or Engine was installed/started.

Nginx Windows 1.30.5 was fetched from the official HTTPS distribution. `npm run deployment:smoke` with `TAWSEL_NGINX_BINARY=T:/.local/deploy-tools/nginx-1.30.5/nginx.exe` passed **8 HTTP checks** against actual Nginx + Fastify + a disposable real PostgreSQL database: built shell, exact 206 range bytes, invalid range 416, previous hashed asset retention, missing asset 404, private diagnostics 404, no-cache worker, and proxied API JSON. `.local/phase-39-http.json` records the result; its 16-byte map fixture proves HTTP range behavior only. Initial harness failures exposed missing Windows Nginx temp-directory setup and a startup wait that stopped before readiness; repaired. Successful runs shut down their Nginx process and remove only their own temporary directory/database. No UI behavior or target TLS claim comes from this smoke.

`npm run test:deployment` passed **4 files / 20 tests**, 45.22 s, covering the release/config/identity negative controls, real migration lifecycle and retained old queue/update recovery. `node --env-file-if-exists=.env.database.local node_modules/vitest/vitest.mjs run apps/mock-erp/test/integration/receiver.test.ts --maxWorkers=1 --testTimeout=30000` passed **8/8**, 29.12 s. The managed mock readiness function refuses an uninitialized schema without creating one, then verifies complete checksums and exact recipient scope. Existing receipt/signature/duplicate tests still pass. `npm run deployment:check` passed; passing the unfilled `deploy/release-evidence.example.json` intentionally exited **1**, correctly refusing live evidence.

`npm run test:fast` passed **21 files / 602 tests** (27.97 s). `npm run lint`, `npm run typecheck`, `npm run contracts:lint` and `npm run contracts:check` passed. Canonical inventory remains **31 schemas / 268 valid / 167 invalid examples / 187 operations**; the existing redirect-only session callback has the expected OpenAPI 2XX warning. `npm run audit` passed the configured high threshold, reporting the existing **two moderate ExcelJS/uuid findings**; no dependency changes or forced downgrade were made. The entire integration suite was not rerun; connected prerequisite/deployment/mock files and the fast suite are the reported evidence.

The first `npm run recovery:demo` attempt could not start because `.local/identity/secrets.json` was absent. Restored the existing pinned local identity prerequisites using `identity-local.ps1 install`. The historical Windows PowerShell invocation lacked `Get-FileHash`; invoking that script in the available PowerShell 7 verified/downloaded its pinned archives. The JDK then failed to resolve native libraries under the Cyrillic workspace path. Extracting its already checksum-verified archive into a uniquely named ASCII temporary directory fixed Java startup. Compiled the actual recovery provider with that JDK against Keycloak 26.7.4, created its provider JAR, then launched local Keycloak on loopback 8085 using the existing generated local realms. This is local dev-mode identity/H2, not the production PostgreSQL issuer recipe. No target email or realm was changed.

## Remaining target facts and safe status

Final browser rerun: `npm run recovery:demo` passed **2/2 in 1.1 minutes** against real local Keycloak 26.7.4, isolated PostgreSQL, production-built PWA/service worker and Chromium. It exercised browser-process reopen with pending actions, aborted native IndexedDB upgrade, pending update/logout gates, same-account OIDC reauthentication, retained former-phone review and a storage-empty second phone context. Routing remains the labelled controlled provider fixture; this is not physical-phone or live Engine proof. Visually inspected the 390 px RTL captures: [pending update/blocked account exit](../../output/playwright/phase-39-pending-update.png), [retained partial-delivery input](../../output/playwright/phase-39-retained-input.png), [retained review state](../../output/playwright/phase-39-retained-review.png). Arabic context and actions remain visible without horizontal clipping. These reproduce existing UI behavior; no UI feature/layout was changed.

Final focused reruns after review: lint and all workspace/script typechecks passed; deployment config/identity tests passed **4/4** after strict boolean/line-ending handling; the modified mock ERP production build passed. Both final Compose files passed the actual parser with explicit resource-limit placeholders resolved solely to configuration fixtures. Final Nginx smoke again passed **8/8** after removing a redundant range header. No full integration-suite pass is claimed. Git whitespace verification passed. Existing P35 screenshots were restored after retaining new captures under P39 names.

The local identity restore is retained under ignored `.local/identity`; its archives were checked using the existing installer hashes. The temporary ASCII JDK path is recorded privately in `.local/deploy-tools/java-path.txt` so local reproduction can use it. The prior local helper scripts were not rewritten. Generated secrets, local database files, binaries and raw browser reports are excluded from the commit. Source/operations changes and the three inspected browser captures are the reviewable release-preparation artifacts.

Unrun: Docker image builds/starts and container clean/upgrade/restart, real Dokploy version/network/labels and cgroup limits, actual co-located workloads/Engine image and dataset inventory, verified map archive/license/coverage on target, target TLS/DNS and issuer redirects through that proxy, external SMTP/email receipt, independent backup/restore and measured RPO/RTO, physical-device/owner acceptance. No authorized target exists for those checks. The native private mock and production outbox have deliberately incompatible private-address policies; target-host mock connectivity requires a separately reviewed design. The Linux-only private staging mock Compose is configuration-validated, not container-run.

Changes: `deploy/` recipes/config/examples/pins, `.dockerignore`, deployment CLI/identity/assets/health/smoke scripts, real recovery/config/mock tests, the P38 table inventory fix, optional managed mock migration startup, package commands, operations/phase/status/coverage/ERP runbooks. No public payload/schema/operation, SQL migration, Engine Compose/dataset/mount or original design export changes. Phase 40 may use these local proofs and release gates but must supply independent restore evidence before live mutation. Commit/push are explicitly requested by the owner; no public launch or image publication is part of this task.
