# Tawsel implementation status

## Phase 28 — 24 September 2026

Implemented the connected Arabic RTL driver journey at `/day` and `/prepare`: coherent daily work grouped as ready/unresolved/prepared/held/deferred, explicit preparation choices, durable planning states, partial-plan blocking, honest Engine failure and complete manual fallback, current-plan readiness, stable same-ID start recovery, current-round continuation and explicit other-phone takeover. Personal creation and native ERP remain their existing source-owned entry points; no duplicate commercial administration surface was added.

Focused component coverage is **6 passed**. The actual local Keycloak 26.7.4/Chromium/HTTP/PostgreSQL proof passes both a personal stale-plan recovery/start and a company mock-B2B controlled Engine-503/manual-order/start journey, producing exactly two rounds and two accepted start actions. A real browser run exposed and drove fixes for unbound native fetch defaults, incomplete manual fallback membership and draft/server revision synchronization. [Ordered evidence, captures, reproducible commands and exact limits](phase-28-evidence.md), [UI action map](ui-actions.md), [ERP handoff](erp/ERP-PLANNING-INPUT.md).

Final equivalent gate: audit zero vulnerabilities; lint/contracts/types passed; **43 files / 873 tests passed**, 779.36s with the documented scoped 30-second allowance; all production builds and fixture isolation passed with the required explicit local API base. Two unrelated older additive-migration cases first hit the default 10-second limit, then passed in their two-file **59/59** rerun and in the complete accepted run. No schema, operation or migration changed. Live Engine profiles, commercial ERP, physical-device/owner review and production deployment remain unverified. The runtime identifies the GPT-5 family; exact picker suffix/effort is unavailable and is not asserted. Earlier phase entries below retain their historical boundaries.

## Phase 27 — 24 September 2026

Implemented the private labelled native ERP with independent OIDC staff sessions, Arabic RTL provisioning/task/return views, local source records and atomic outgoing command outbox (consumer migrations 0003–0004), immutable IDs/revisions and durable pending/accepted/rejected/review-required results. All execution uses the published client and authenticated public API. Received/applied projections remain distinct from command acceptance.

Checkpoints passed in order: prerequisite **94 tests / 5 files**; A **3 actual database/API/process tests**; B **1 connected session test plus 1 actual Keycloak/Chromium two-task browser flow**; C **independently installed public bundle, 24 signed events and 10 negative checks**, both API/consumer restarted twice with no database repair. One piece delivered/15000 reported minor, one physically received, one separately lost, three still unresolved on the other shipment, and a fresh receipt-funded one-piece cycle. The same pending action survives downtime; departed edits and over-capacity batches reject visibly. Final full suite **42 files / 867 tests passed**, 1063.04s with two workers; audit zero vulnerabilities, lint/contracts/typechecks and production builds passed. An initial 10-second multi-migration test timeout was given a scoped 30-second allowance and the entire suite rerun successfully. Canonical inventory: **26 schemas / 247 valid / 150 invalid examples / 185 operations**. [Ordered commands, failures and exact limits](phase-27-evidence.md), [quickstart](erp/consumer-quickstart.md), [source protocol](erp/source-protocol.md), [ERP planning](erp/ERP-PLANNING-INPUT.md).

A stale ignored ERP OIDC client secret was reconciled to the existing local issuer credential; no issuer rotation. Runtime reports GPT-6; requested Astra/xhigh is not asserted as the actual unavailable picker setting. Phase 28 may rely on the native source, scoped clients, receiver projection and portable two-way checkers. Live Engine routing, production TLS/email, physical-device/owner review, real ERP integration and final release packaging remain unverified. No Phase 28, commit, push or deployment; Engine/Stitch unchanged. Earlier phase entries below describe their historical scope.

Final native Chromium flow also verifies a real local commit with a lost browser acknowledgement, same-ID recovery with no extra command, slash-containing source IDs and fresh values when reopening a new-cycle edit. **1 passed / 52.5s**, with actual RTL captures under `output/playwright/phase-27-*`. The copied consumer proof passed again after review; the final bundle is `dist/erp-reference`. Temporary native test users/databases were disposed and the local issuer stopped; unmatched older fixtures were preserved.

## Phase 26 — 24 September 2026

Implemented the external reference mock receiver, separate database/restricted role and two independent receiver migrations, exact-byte durable inbox, atomic current projection/history/processed marker, signed duplicate/mismatch handling, scoped status and dependency/gap recovery. Tawsel migration 0025 adds independent current checkpoint caches and separately authenticated receiver progress reports. Current-state reconciliation preserves explicit missing history and accepts late required transitions without double-counting.

Ordered evidence: prerequisites **418 tests**; A **3**; B **7**; C **13 tests / 3 files**. The compiled distribution was installed outside the repository and its receiver/worker/public conformance checker ran against separate real processes/databases over HTTP: **19 unique events, 10 negative checks, 2 delivered pieces, 25000 reported minor units, 1 received piece, 5 applied reports**, both sides restarted. Final equivalent check gates passed: **40 files / 858 tests**, 913.53s with two workers; zero audit findings, lint/contracts/types, all production builds and fixture isolation. Earlier compile/contract assertion failures and fixes are recorded in the evidence. [Exact evidence](phase-26-evidence.md), [integration proof](verification/integration.md), [install/start/demo](erp/consumer-quickstart.md).

Runtime identifies GPT-6; exact picker variant/effort unavailable, requested Astra/xhigh recorded separately. Phase 27 may rely on the verified public receiver/inbox/projection/replay/report interfaces and standalone artifact paths; native source operations/forms/outbox remain unimplemented. No next phase, commit, push, publication or production deployment. Existing local application database is not migrated by this task; all database assertions use isolated real databases. Engine/Stitch assets remain unchanged.

## Phase 25 — 24 September 2026

Implemented durable signed outbox delivery, migration 0024, recipient aggregate streams, immutable event bytes, fenced leases/attempts, key rotation, destination controls, bounded jittered retries, six scoped operational APIs, public client/signature vector and reusable consumer proof. Checkpoints passed in order: A **3 tests**, B **11 tests**, C **13 tests**. The real PostgreSQL/HTTP demonstration delivered **19 unique events and 1 duplicate** after response loss, including committed intake/outcomes/correction/returns. Sender receipt is verified; projection remains unknown and the receiver is explicitly process-memory. Final equivalent check gates passed: **37 files / 831 tests**, 827.36s with two workers, zero audit findings, lint/contracts/types and all production builds/fixture isolation. [Exact commands, earlier failures and local migration caveat](phase-25-evidence.md), [reproduction and P26 handoff](outbox-delivery.md).

Runtime identifies GPT-6; exact picker variant/effort unavailable, requested Astra/xhigh recorded separately. No next phase, commit, push, publication or production deployment. Engine/Stitch assets remain unchanged.

## Phase 24 — 24 September 2026

Implemented coherent conditional monitoring/history APIs, migration 0023 with scoped indexes/revision ledger/write timing, public client and source grant, real PostgreSQL consistency/isolation tests, and loopback HTTP demo. Checkpoints A/B/C passed in order: prerequisites **120 tests**, A **367 contract tests**, B **2 connected tests**, C **32 connected tests**. Final `npm run check` passed **34 files / 800 tests**, 356.46s, plus audit (0 vulnerabilities), lint/contracts/types and production builds. Migration **0023** applied; repeated migration current; demo and copied public conformance consumer passed. No disposable test databases/application fixtures remain. Failed interim runs and all bounds are recorded in [phase evidence](phase-24-evidence.md). [Reproduce and exact Phase 25 handoff](monitoring.md). Runtime identifies GPT-6; exact picker suffix/effort unavailable; requested Astra/high recorded without claiming a switch.

Scope: server-received evidence only, no presence inference or delivered/applied transport claim. UI/polling belongs to P32; measured freshness to P38. Shared-driver source binding is explicitly a setup fixture, not a new administration workflow. No Phase 25 execution, commit, push or publication.

Updated: 24 September 2026. Package revision 3 under D-109–D-111, with the ERP-agnostic boundary clarified by D-112.
Planning HEAD: `3d6291697fb0baeb69215237bf1d09dbf6d1d9cd`.

## Current state

24 September 2026 — Phase 23 bounded driver correction and compatible former-device outcome adoption are implemented locally. Ordered A/B/C checks, real PostgreSQL races/rollback, effective quantities/collection and loopback HTTP loss/restart demonstration passed; final `npm run check` passed **33 files / 778 tests**, 303.74s, plus audit/lint/contracts/types and production builds. [Evidence](phase-23-evidence.md), [API/demo and exact Phase 24 handoff](corrections.md). No UI/device/native ERP/signed delivery claim. Exact picker suffix/effort unavailable; requested Astra/xhigh is recorded separately from the visible GPT-6 runtime.

24 September 2026 — Phase 22 branch interruption/arrival/resume and confirmed-return dispatch cycles are implemented with real PostgreSQL and public HTTP proof. Final `npm run check`: **33 files / 736 tests passed**, 271.74s; all audit/lint/contracts/types/build gates passed. Migrations **0020–0021** applied locally. See [ordered evidence and exact P23 handoff](phase-22-evidence.md), [reproducible API/demo](branch-interruption.md) and [ERP mapping](erp/field-and-status-mapping.md). Preceding summaries remain historical evidence.

24 September 2026 — Phase 21 source-branch offers, native actual subset receipt and separate loss/damage are implemented with real PostgreSQL/API verification. See [ordered evidence](phase-21-evidence.md), [reproducible protocol/demo and P22 handoff](returns.md), and [ERP mapping](erp/field-and-status-mapping.md). Final `npm run check`: **31 files / 703 tests passed**, 217.52s, all audit/lint/contracts/type/build gates passed; migration 0019 applied locally. The preceding Phase 20 summary remains historical evidence.

24 September 2026 — Phase 20 implemented from clean HEAD `fd6c090`. Online takeover, consistent execution fencing, mandatory confirmed snapshot, retained former-phone evidence and scoped notification intents are verified with real isolated PostgreSQL and two-session HTTP/client/API-restart checks. Final `npm run check`: **30 files / 665 tests passed**, 176.43s, with all other gates passing. Device/kernel checks **38 passed**, prerequisites **69 passed**, execution regression **95 passed**. [Evidence](phase-20-evidence.md), [protocol/demo/P21 handoff](device-ownership.md). No adoption/queue UI, next-phase execution, commit, push or deployment. Exact picker variant/effort unavailable; runtime identifies GPT-6, requested Astra/xhigh recorded without claiming a switch.

The owner requested smaller self-contained implementation tasks after reviewing the broad 11-phase package.
The [current package](phases/README.md) contains 42 sequential prompts, a 65-requirement catalog, complete D-01–D-112 traceability, per-phase model/reasoning recommendations, explicit ERP handoff deliverables and a review checklist.
**Phases 01, 02 and 04–27 are implemented and locally verified within their bounded scope; Phase 03 supplies the verified specification.** Phase 01 provides the web/API/shared workspace, Cairo typography and real-HTTP/configuration harness. Phase 02 adds validated common contracts and generated references. Phase 03 supplies the visual/action specification. Phase 04 supplies reusable RTL components and development fixtures. Phase 05 adds the PostgreSQL atomic command foundation; Phase 06 adds tenant/membership/capability/resource enforcement. Phase 07 now binds real Keycloak identities to those guards, with separate company/personal cookies, phone registration, verified-email recovery and a connected account UI. Production email/TLS, owner/device review, remaining domain handlers, deployed business API and real ERP interoperability remain unverified or unimplemented. Phase 08 adds authenticated ERP provisioning, source revisions and durable issuer reconciliation, with public-consumer and actual local Keycloak proof. P09 adds connected independent intake; P10 adds ERP source snapshots, receipt, atomic admission and normal predeparture updates. P11 adds execution-pin provenance, scoped confirmation, pending planning invalidation and real self-hosted Cairo maps; live private Nominatim remains unavailable. P12 adds validated private routing adapters and authenticated mode metadata; live Engine remains unavailable. P13 adds durable planning jobs, recoverable fenced leases, candidate drafts and immutable forecast/workload history. [P13 evidence](phase-13-evidence.md) distinguishes PostgreSQL/process/HTTP proof from unavailable live Engine routing. P14 adds current/urgent grouping, complete route validation and revisioned manual fallback; [P14 evidence](phase-14-evidence.md) records ordered checks. P15 adds online start/ownership/immutable first baseline and departure/admission guards; [evidence](phase-15-evidence.md). P16 adds explicit heading/arrival, physical origin and a focused connected browser component; [evidence](phase-16-evidence.md). P17 adds exact whole-piece outcomes, reported collection and atomic effective progress/source intent; [evidence](phase-17-evidence.md). P18 adds explicit capacity-checked deferral/whole retry/driver urgency, preserved attempts and cumulative fees; [evidence](phase-18-evidence.md), [API/demo](eligibility.md). P19 adds explicit closure, immutable end times, pending/replay, retained holder work and basic workday summaries; [evidence](phase-19-evidence.md), [API/demo](workday-closure.md). P20 adds online takeover, mandatory snapshot fencing and preserved former-phone evidence ([evidence](phase-20-evidence.md)). P21 adds actual source-branch receipt/disposition and explicit current custody ([evidence](phase-21-evidence.md)). P22 adds explicit branch activity with preserved full-capacity customer membership, subset-confirmed resume and receipt-funded new dispatch identities ([evidence](phase-22-evidence.md)). P23 adds bounded driver correction and compatible outcome evidence adoption with preserved history and effective totals ([evidence](phase-23-evidence.md)). P24 adds coherent scoped conditional monitoring/history, durable replacement revisions and received-evidence timing ([evidence](phase-24-evidence.md)). P25 adds durable signed delivery, retained replay and scoped operational reads ([evidence](phase-25-evidence.md)). P26 adds the external durable inbox, atomic projection, public received/applied checkpoints and honest replay/snapshot recovery ([evidence](phase-26-evidence.md)). P27 adds verified native source/OIDC forms and transactional two-way consumer proof ([evidence](phase-27-evidence.md)). Phases 28–42 have not started.

The [old package/ledger](phases-archive-v1/implementation-status-at-supersession.md) remains historical. Its earlier structure/link/hash validation does not establish the adequacy of its broad task boundaries, and is not evidence for the replacement's runtime behavior.

## 24 September 2026 — Phase 23 driver correction and compatible evidence adoption

- Clean Phase 22 HEAD `933a011`; no applicable AGENTS.md. Actual P17/P19/P20/P21/P22 prerequisites passed **83 tests / 6 files** before editing. Existing pinned dependencies retained. No prerequisite repair needed.
- **A:** closed corrected-field/effective-revision/evidence/event contracts and availability API. Actual receipt and closed-day reasons verified before atomic writes; **1 focused database/API test + 360 contract tests** passed.
- **B:** migration **0022**, append-only correction links/outcome/quantity/collection rows, shared owner/day/cycle/task locking, unchanged original attempt resolution, corrected effective progress and source intent. **4 focused tests** passed, including duplicate recovery and injected rollback at five write boundaries.
- **C:** **26 focused tests** passed before final review, including independent receipt/redispatch/day-end/duplicate races, monetary and generation validation, explicit old-device adoption, B2C correction/first-outcome adoption, broad staff/cross-account denial and prior shipping preservation. Added a real HTTP/public-client restart demo and active claimed-handover dependency regression. Public consumer negative controls pass. Later combined device/database/correction check: **67 tests / 3 files**, 104.81s; claimed-handover focused test passed separately.
- **Final:** `$env:VITE_TAWSEL_API_BASE_URL='http://127.0.0.1:3001'; npm run check` **PASS: 33 files / 778 tests**, 303.74s, including **49 correction/dependency tests**. Audit 0 vulnerabilities; lint, contracts, all typechecks, production builds and fixture isolation passed. Canonical inventory: **21 schemas / 217 valid / 137 invalid examples / 178 operations**. Migration 0022 applied locally; repeated migration current. Zero disposable test databases or application B2B task/round/outcome fixtures remain. The first final gate exposed two stale migration inventories; fixed and rerun. Existing callback-302 and MapLibre bundle warnings remain.
- Canonical schemas/examples/OpenAPI/generated client/reference, operation/UI action coverage, ERP planning/mapping/quickstart/conformance and [reproducible guide](corrections.md) are updated. No UI component was changed. Signed transport, arrival-only adoption, offline queue UI, native ERP and physical-device evidence remain unavailable/unimplemented in this phase. Review findings, exact commands/interim failures and final results are in [the evidence](phase-23-evidence.md). Phase 24 is not executed; no commit/push/publication.

## 24 September 2026 — Phase 22 branch interruption, resume and new dispatch cycles

- Started at clean HEAD `99fc8b8` (`phase 21`); no applicable nonempty AGENTS.md. Runtime identifies GPT-6; exact picker model suffix/reasoning unavailable. Requested Astra/xhigh and model-selection guide consultation recorded without claiming a switch. Pins retained.
- **Prerequisites/A:** 5 files / 99 real PostgreSQL prerequisite tests passed before editing; no prerequisite repair. Migration 0020, activity constraints, visible paused membership, source-bound branch pin, explicit arrival origin and claimed-subset resume; 2 focused tests passed before B. Unresolved arrived customers cannot enter branch service.
- **B:** 6 focused tests passed before C. Fifty retained customers use a one-stop branch segment with immutable paused forecasts and preserved admission reservations; overflow admission remains atomic. Stale planner completion, independent receipt/resume lock races, unavailable receipt and every rollback boundary are covered. New receipts during branch service visibly append to the paused sequence in a further review test.
- **C:** 12 focused tests passed before review; 77 broader regression tests passed, then 15 branch/cycle tests passed. Final suites contain **16 phase tests**, including competing redispatch, stock inflation denial, different-driver fresh receipt, old outcome/retry evidence and rollback. Migration 0021 preserves stable shipment identity, frozen old source/holder/history, append-only allocations and new execution IDs. Actual HTTP/public-only consumer and API outage/restart demo passed.
- **Final:** PowerShell `$env:VITE_TAWSEL_API_BASE_URL='http://127.0.0.1:3001'; npm run check` **PASS: 33 files / 736 tests**, 271.74s, audit 0 vulnerabilities; lint, contracts/types/builds and production fixture isolation passed. Canonical inventory **20 schemas / 212 valid / 131 invalid examples / 176 operations**. Migrations 0020–0021 applied locally; repeated migration current. No disposable test databases or application B2B task/round/outcome fixtures remain. Versions: Node 24.19.0 / npm 11.1.0 / PostgreSQL 18.6; all observed dependency versions and repaired failures in [evidence](phase-22-evidence.md). Existing callback-302 and MapLibre bundle warnings remain.
- Canonical schemas/examples/OpenAPI/client/generated reference/operation coverage, ERP planning/mapping/quickstart/conformance and current guides updated. Reproduce with `npm run test:branches -- --maxWorkers=1`, `npm run branches:demo`, `npm run test:erp:dispatch -- .local/phase-22-demo.json`. [Full-capacity and subset API examples](branch-interruption.md). Exact changed paths and commands are in the evidence.
- **Limits/handoff:** no new UI/browser, physical phone, live issuer/Engine, native ERP or signed transport proof. Driver auth/bootstrap and provider behavior are labelled fixtures. P23 may rely on 0020–0021, shared return/driver locks, receipt and redispatch dependency facts, immutable branch/stock history, current/cycle/custody reads, the new tests and public demo. Correction/adoption remains P23; branch UI P31. No next phase, commit, push, publication, map import or prior migration/Engine/Stitch edits.

## 24 September 2026 — Phase 21 source-branch return requests and actual subset receipt

- Started at clean HEAD `f790e49`; no AGENTS.md. Runtime identifies GPT-6, exact picker variant/effort unavailable; requested Astra/xhigh and model-guide consultation recorded without claiming a switch. Dependency pins retained.
- **Prerequisite/A:** 5 files / 75 prerequisite tests passed. Closed schemas, additive migration 0019, source-grouped offers and scoped native pending reads; 2 API/PostgreSQL tests passed before B. Wrong-source/quantity checks and SQL identity/conservation constraints are real failures, not stubs.
- **B:** native explicit service receipt/disposition grants and commands, per-item revision/idempotency, exact requested/received/unresolved/lost/damaged state, atomic dependencies/audit/event intent, accurate held progress/carry-forward; 8 tests passed before C. Original delivery and reported collection history is retained.
- **C:** 14 focused tests passed, including observed independent PostgreSQL lock barriers for receipt versus retry/outcome and duplicates, B2C denial and stale-phone evidence. Later source/tenant/revocation and retained-P20-upgrade checks bring the regression run to 4 files / 64 tests passed. Separate real multi-branch grouping check passed. A copied public-only HTTP consumer and actual API outage/restart demo passed; no DB/operator/issuer inputs reach the child.
- Canonical schemas/examples/OpenAPI/generated client/reference/operation map and ERP planning/mapping/quickstart/conformance are updated. Run `npm run returns:demo`, `npm run test:returns`, `npm run test:erp:returns`. Exact commands/failures/review/remaining checks: [Phase 21 evidence](phase-21-evidence.md).
- **Final gates:** `$env:VITE_TAWSEL_API_BASE_URL='http://127.0.0.1:3001'; npm run check` **PASS: 31 files / 703 tests**, including all **18 Phase 21 tests**; audit 0 vulnerabilities, contracts/types/builds/fixture isolation passed. Canonical inventory: 19 schemas / 199 valid / 127 invalid examples / 173 operations. Migration 0019 applied and repeated migration is current. Final demo/conformance/specification/whitespace checks passed. PostgreSQL 18.6 / Node 24.19.0 / npm 11.1.0; pins retained. Zero disposable test databases or application fixture tasks/rounds/outcomes remain. Existing callback-302 and MapLibre bundle warnings only. Earlier allowlist/demo typing/retention-fixture failures and their fixes are recorded in the evidence.
- **Limits/handoff:** driver auth/bootstrap in the demo are fixtures; no new UI/browser/physical-device/live issuer/Engine/real ERP or deployment evidence. P22 may rely on 0019, `returns/state.ts` locked claimed-subset predicate, request/receipt/disposition APIs, current custody and immutable dependency facts. P22 still owns branch resume/new cycle; P23 corrections; P25–27 transport and native ERP screens/transactional outbox. No next phase, commit, push, publication, map import or changes to prior migrations/Engine/Stitch assets.

## 24 September 2026 — Phase 20 online takeover and preserved evidence

- **Prerequisites/A:** clean Phase 19 HEAD, no AGENTS.md; 69 prerequisite tests passed. Closed takeover contracts/migration 0017 and driver/workday locks preserve original round/driver/baseline while incrementing ownership. Three tests proved concurrent takeover, exact lost-response retry and fault rollback before B.
- **B:** shared generation/snapshot fence covers current/origin, outcomes, eligibility/retry/urgency, closure and active planning/pin writes. Six focused checks passed before C. Original stale envelopes remain durable review evidence; duplicate transport, receipt and business acceptance are separate. Fixture constructors now retain installation identity. Execution/planning regression: 95 passed.
- **C:** scoped context/snapshot/status/evidence APIs and dynamic closed-day/dependency/correction metadata; adoption contract is designed only. Initial 13 checks passed, then review added nonadmitted-ID isolation and actual P19 retained-round upgrade tests: 15 focused tests passed. Actual two-session HTTP demo/public conformance pass with offline failure, discarded committed takeover response, API restart and retained former-phone evidence. Signed issuer is a fixture; sessions/database/HTTP are real.
- **Review:** no external reviewer feedback supplied. Fixed schema registration, fixture shapes, assignment-revision join, fixture phone identities, recovery probing outside an admission and stale migration/path/table inventories. Canonical review added atomic account-only notification intents and additive migration 0018, preserving tenant/kind recipient constraints; all **16 device / 22 kernel tests passed**. All intermediate failures are recorded in [phase evidence](phase-20-evidence.md).
- **Final:** PowerShell `$env:VITE_TAWSEL_API_BASE_URL='http://127.0.0.1:3001'; npm run check` passed **665 tests / 30 files**, 176.43s, audit **0 vulnerabilities**, lint, contract checks, typechecks, builds and production fixture isolation. Inventory: **18 schemas / 185 valid / 121 invalid examples / 168 operations**. Both public conformance modes passed; migrations **0017–0018** applied locally. Node **24.19.0**, npm **11.1.0**, PostgreSQL **18.6**, pg **8.23.0**, Fastify **5.12.5**, TypeScript **6.0.2**, Vitest **5.0.1**, Playwright **1.63.0**; existing pins retained. Existing callback-302 and MapLibre chunk warnings remain.
- Canonical schemas/examples/OpenAPI/client/catalog/reference, operation/requirement mapping and ERP planning/mapping/quickstart/conformance are updated. Run `npm run devices:demo`, `npm run test:devices`, `npm run test:erp:devices -- .local/phase-20-demo.json`. [API/demo and exact P21 handoff](device-ownership.md). P21 may rely on 0012–0018, stable round/attempt/source identities, shared fences, durable evidence/status APIs and the tested dependency hook; receipt producers must share invariant locks.
- **Limits:** P23 owns adoption; P33–35 own browser persistence/replay/safe logout and restricted revoked-account upload. No new UI/browser, physical-phone, live Engine, real ERP or deployment proof. No next phase, commit, push or publication.

## 24 September 2026 — Phase 19 round/day closure and retained held work

- **A:** distinct schemas, explicit current/ownership expectations, pending/idempotent results and holder-based carry-forward; focused policy test passed, followed by a real database constraint test. **B:** immutable closure/current history, actual UTC end times, source-filtered intent, atomic audit/result/planning writes, fresh candidate workday IDs and sorted lifecycle guards. Closure/start suites passed **29 tests**, with all **59 prerequisite tests** verified. Five injected transaction failures and separate-connection races are included.
- **C:** basic outcome/collection/held-work APIs and Cairo display helper; distinct admitted task/attempt counts, processed versus delivered, preserved old-day outcomes, due-time activation before the next round and no source resubmission. Focused suite passed **18 tests** before adding the HTTP demonstration as its 19th case. P18/database/planning regression passed **31 tests**. `npm run workdays:demo`, `npm run test:erp:workdays -- .local/phase-19-demo.json` and canonical public-only conformance all passed.
- **Final:** PowerShell `$env:VITE_TAWSEL_API_BASE_URL='http://127.0.0.1:3001'; npm run check` passed **630 tests / 29 files**, 246.57s, and all other gates. Inventory: **17 canonical schemas / 172 valid / 115 invalid examples / 167 operations**. Node **24.19.0**, npm **11.1.0**, PostgreSQL **18.6**, pg **8.23.0**, Fastify **5.12.5**, TypeScript **6.0.2**, Vitest **5.0.1**, Playwright **1.63.0**; existing dependency pins retained. The new marked local application database was empty; `npm run db:migrate` applied retained 0001–0015 and new **0016**. No application task/round/outcome fixtures or disposable test databases remain.
- Changes and failed interim checks are recorded in [phase evidence](phase-19-evidence.md): closure module/migration/tests/client, start race repair, P18 preparation eligibility, canonical schemas/examples/OpenAPI/catalog/references, ERP planning/mapping/quickstart/conformance, UI operation map and current guides. No UI component changed. Existing callback-302 OpenAPI and MapLibre chunk warnings remain. No external reviewer response was supplied; agent review findings and fixes are recorded.
- **Limits/handoff:** pending is not acceptance; P34 must persist the local queue/barrier and block new-start controls until relevant synchronization succeeds. P20 may rely on 0012–0016, current→active-round→open-day constraints, irreversible closure records/end times, owner generations, command/read/status/client contracts and the connected tests/demo. It must enforce open-day/active-round takeover; it is not implemented here. Physical return producers, corrections, signed transport, final reports, full closure UI and device/real ERP proof remain later phases. Engine datasets/mounts and original Stitch exports are preserved; no map import, commit, push or publication.

## 24 September 2026 — Phase 18 deferral, retry and driver urgency (historical entry)

Phase 18 implemented locally from HEAD `8fdc331`. Prerequisites: 59 real PostgreSQL tests passed. Checkpoint A: 3 predicate tests and 1 real database test passed. Checkpoint B: 7 real API/PostgreSQL tests passed, including capacity/duplicate races and injected rollback. See [ordered evidence](phase-18-evidence.md). Checkpoint C: 48 connected tests, 282 contract checks and the real HTTP demo/public conformance passed. Final `npm run check`: **27 files / 592 tests passed**, 120.81s; audit 0 vulnerabilities, lint, generated contracts, types, production build and fixture isolation passed. Migration 0015 applied locally. Demo and public-only ERP conformance passed; UI/source checks and diff whitespace check passed. No Phase 19 work was performed during that phase. See evidence for exact commands, versions, failed interim checks and remaining external/UI gaps. Exact picker model/effort is unavailable; runtime identifies GPT-6.

## 24 September 2026 — Phase 17 delivery outcomes, whole pieces and exact collection

- Started at clean HEAD `62322be` (`phase 16`); no applicable AGENTS.md. Exact Codex picker variant/effort is unavailable; runtime identifies GPT-6. Requested Astra/xhigh and model-selection guide are recorded without claiming a picker change. Existing pins retained: Node **24.11.1**, npm **11.6.2**, PostgreSQL **18.6**, pg **8.23.0**, Fastify **5.12.5**, TypeScript **6.0.2**, Vitest **5.0.1**, Playwright **1.63.0**.
- **Prerequisites:** actual P05 kernel/P10 intake/P15 start/P16 current tests passed **4 files / 69 tests** before editing. **A:** closed schemas and frozen BigInt/whole-piece arithmetic passed **9 focused tests / 251 contract tests** before persistence. The 350/250/50/0 examples, exact prepaid allocations, invalid quantities/currencies/amounts and B2C exclusions are verified.
- **B:** additive migration **0014**, immutable outcome/quantity/collection history, resolved-attempt FK, effective task projection, owner/revision/current checks, current resolution without movement, capacity/planning exclusion, atomic audit/receipt/outbound intent. **3 focused PostgreSQL tests**, then **4 files / 31 regression tests**, passed before expanding C. Injected failures after domain/progress/audit/outbox/result write stages rolled back every accepted effect.
- **C:** `npm run test:outcomes` passed **14 real PostgreSQL/API tests**, including independent duplicate/competing-action races, 5 fault stages, same-address independent results, prepaid/multiple-line/zero amounts, refusal/no-answer distinctions, exact aggregate overflow, B2C optional collection, authorization/recovery and delayed-plan/pin reactivation protection. `npm run outcomes:demo` and public-only conformance passed actual loopback HTTP readiness/start/current/outcomes, deliberately lost committed response recovery and two schema-valid pending source intents. Identity/bootstrap is a fixture; planning is manual; no live provider success is claimed.
- **Final:** `VITE_TAWSEL_API_BASE_URL=http://127.0.0.1:3001 npm run check` (PowerShell environment assignment) passed audit **0 vulnerabilities**, lint, canonical lint/check, types, **25 files / 549 tests** (104.53 s) and production builds/fixture isolation. Canonical inventory: **15 schemas / 148 valid / 98 invalid examples / 159 operations**. Final example-envelope consistency review was followed by regeneration and **259 passing contract tests**. `npm run db:migrate` applied **0014_delivery_outcomes.sql** locally. Existing callback-302 and MapLibre chunk-size warnings remain.
- Changes: `apps/api/src/outcomes/*`, planning/admission/pin integration, migration 0014, outcome arithmetic and connected suites/fixture, public schema/examples/OpenAPI/catalog/client/generated references, HTTP demo/conformance, ERP planning/mapping/quickstart, operation coverage and current phase guides. Exact paths, commands, initial failed checks and review findings are in [phase evidence](phase-17-evidence.md); [API/money/demo guide](outcomes.md) is reproducible.
- Limits/handoff: reported collection is not remittance/settlement; held return-required is not actual branch receipt. Signed sender/receiver, whole retries, corrections, complete outcome UI, offline queue, full workday reports and physical-device/native ERP proof remain later phases. No UI component changed or new browser usability claim is made. P18 may rely on immutable outcome/attempt/line/collection records, effective projection, fenced commands/read/status, resolved-current semantics, pending planning/event intents and the connected tests/demo; it must add explicit whole retry admission without reviving rejected partial remainder or recollecting prior fees. No P18 work, Engine import/assets change, commit, push or publication was performed.

## 24 September 2026 — Phase 16 explicit current target, heading and arrival

- Started 23 September at clean HEAD `10692a5` (`phase 15`), completed checks after Cairo midnight. No repository/parent AGENTS.md. Runtime identifies GPT-6; exact picker suffix/effort is unavailable, so requested Astra/high is not represented as observed. Model guide consulted; pins retained: Node **24.11.1**, npm **11.6.2**, PostgreSQL **18.6**, pg **8.23.0**, Fastify **5.12.5**, TypeScript **6.0.2**, Vitest **5.0.1**, Playwright **1.63.0**.
- **Prerequisites:** real P14 publication and P15 start/race/HTTP checks passed **3 files / 51 tests** before editing. Browser integration later found and repaired the P15 native-fetch receiver defect in `RoundsClient`; original Engine/Stitch assets and prior migrations remained unchanged.
- **A:** migration 0013, one-current constraint, admitted stable attempt references, closed selection/arrival/origin/result schemas, append-only evidence and explicit heading-replacement/arrived-resolution rules. **3 PostgreSQL checks + 231 contract checks** passed before B; API typecheck passed.
- **B:** atomic explicit heading/arrival/manual-origin commands, owner device/generation rejection evidence, relevant task revisions, live current/action-status reads, immutable action-time provenance, protected planning current/origin and source-filtered intent. **3 files / 62 tests** passed before C, including independent connection lock-observed selection/arrival races, reordering compatibility, stale pin rejection, delayed planner and outbox rollback.
- **C:** small Arabic RTL `/rounds/current` component with existing recipient/contact controls, one dominant heading-stage action and separate next suggestion. Real Chromium/HTTP/PostgreSQL test **1 passed**, then **1 passed against the production build**. Keyboard selection, touch arrival, read-only second browser, intercepted external-link clicks with zero commands and discarded committed-arrival response/reload recovery verified. Fixture account/session and routing providers are explicitly labelled. Screenshots and exact action/state report are linked in the [evidence](phase-16-evidence.md).
- **Final verification:** `npm run check` **PASS**, zero audit findings, canonical/lint/type checks, **23 files / 507 tests**, all builds/production fixture isolation. Existing redirect-only OpenAPI and MapLibre chunk warnings remain. Initial full run found unchanged P13 fingerprints being invalidated by a new null field; fixed by omitting absent evidence, preserving upgrade compatibility. Table inventory and randomized test-order assumptions corrected; focused review rerun **28 passed**. Specification checker repaired for actual P07–P16 operation availability, Git CRLF checkout and a stale negative mutation; all **157 operations / 64 action rows**, links/contrast/negative checks pass.
- `npm run current:demo`, public current/round conformance and conformance against the actual browser report **passed**. Local migration **0013 applied**; zero disposable test DBs or application task/round/attempt fixture rows remain. [API/contract/demo/handoff](current-activity.md), canonical examples/generated client/reference/operation map and ERP planning/mapping/quickstart/conformance updated.
- Live Engine probe `2026-09-23T21:17:50.907Z` (24 September Cairo): **all nine calls unavailable**, Docker inventory unavailable, datasets only `.gitkeep`; no import/start/mount change. No live issuer/Engine/native external app/physical phone/real ERP/owner-review/production deployment claim. P17 outcomes, P18 retry/eligibility, P20 takeover and P33–34 full queue integration remain outstanding.
- **P17 may rely on:** 0013, `apps/api/src/current`, actual current/attempt/physical-origin history, P15 ownership/admissions/baseline, protected planning inputs and `current-activity.test.ts` plus the real focused browser demo. P17 must add resolution states and clear current atomically without auto-selecting next or moving origin for a phone-only outcome. That outcome is explicitly **not implemented or tested here**. No Phase 17 execution, commit, push or publication.

## 23 September 2026 — Phase 15 online round start and departure authority

- Started at clean HEAD `68ee4a2` (`phase 14`); no AGENTS.md. Runtime identifies GPT-6; exact picker suffix/effort unavailable, so requested Astra/xhigh is not asserted as observed. Model guide consulted. Actual P05/P06/P08/P10/P14 prerequisites: **5 files / 106 PostgreSQL tests passed** after starting the managed local database with its installed binary directory on PATH; no prerequisite code repair.
- **A:** migration 0012, open-day/active-round uniqueness across branches, ownership/baseline/publication/admission references, closed start/readiness/result schemas and examples. **7 PostgreSQL lifecycle checks + 224 contract checks passed** before B. No caller sync boolean; P34 must provide the complete local journal barrier.
- **B:** authenticated online readiness/start/current/action-status APIs and client; sorted invariant locks, fresh authorized assignment/policy/revision/capacity validation, immutable first forecast/workload references, server device generation, departure/audit/result/outbox in one transaction. **10 focused tests and API typecheck passed** before C, including real lock-observed duplicate phones/actions, both withdrawal race orders and outbox-failure rollback.
- **C:** departure boundary at active receipt/source/pin resolution/independent intake, truthful command-time snapshots, staff edit/pin/planning denial and allowed own-driver pin correction. **4 files / 45 focused/regression tests passed**; real listening HTTP session/CSRF/client recovery passed with a signed issuer fixture. Review corrected new active membership's estimate horizon while preserving the first forecast. Final P15 suite: **18 passed**.
- **Final check:** `npm run check` **PASS** with documented local `VITE_TAWSEL_API_BASE_URL`, zero audit findings, lint/contracts/all typechecks, **22 files / 480 tests**, all builds and production fixture isolation. Existing redirect-only OpenAPI and MapLibre size warnings remain. Node **24.11.1**, npm **11.6.2**, PostgreSQL **18.6**; pins/lockfile unchanged. Local migration 0012 applied; zero disposable test DBs or application fixture tasks/rounds remain.
- `npm run rounds:demo` and `npm run test:erp:rounds` **passed**. Demo proves unavailable API creates no round, discards a committed response, recovers the exact action after restart, and returns the same round/owner to another device. Exact examples are in `.local/phase-15-demo.json`; [public sequence/demo/handoff](round-start.md), [ordered evidence, failures, review and changed paths](phase-15-evidence.md). Canonical operations/schema/examples/generated client/reference, action coverage and ERP planning/mapping/quickstart/conformance updated.
- Live Engine probe `2026-09-23T20:23:37.669Z`: **all nine calls unavailable**, Docker inventory unavailable, local data only `.gitkeep`. Optimized-start tests use labelled provider fixtures; manual start requires no Engine. No new UI/browser/physical-device/live issuer/real ERP/production/owner-review claim. Full queue gate remains P34 and takeover P20.
- **P16 may rely on:** migration 0012, `apps/api/src/rounds`, `RoundsClient`, current workday/round and retained start result, first forecast/workload/publication/admission references, source/driver authority guards and P05 atomic audit/outbox. Current activity is null until P16 explicitly implements heading/arrival. No Phase 16, commit, push or publication. Prior migrations, Engine/mounts/data/maps and original Stitch exports preserved.

## 23 September 2026 — Phase 12 Routing Engine adapters and vehicle profiles

- Starting HEAD `2eed5ea`, clean tree; no AGENTS.md. Runtime identifies GPT-6; picker suffix/effort unavailable, requested Astra/high not represented as observed.
- Prerequisites: P02 contract tests **185 passed**; P11 location tests **9 passed** with real PostgreSQL. Stale P10/P11 ledger rows corrected against existing evidence; no prerequisite code repair.
- **A:** canonical models, three distinct profile origins, coordinate/service/ID mapping. **5 focused tests** and API typecheck passed before B.
- **B:** bounded private HTTP transport and validated route/table/optimization conversions. **26 tests** and API typecheck passed before C.
- **C:** **30 adapter tests**, independent committed-receipt/Engine-failure PostgreSQL check, authenticated profile metadata/capability revocation check, consumer conformance and nine-call controlled demo passed. Final Node 24.19.0/npm 11.1.0 run: **18 files / 384 tests passed**, zero audit findings, lint/contracts/typechecks passed. Full check stopped at missing web-build environment; separate `npm run build` passed after setting the documented local API URL. [Exact commands, earlier failures and evidence](phase-12-evidence.md).
- Live probe at `2026-09-22T21:48:34.427Z` (23 Sep Cairo): all **9 calls unavailable**, Docker absent, local data only .gitkeep. Running versions/image-dataset compatibility and actual profile success remain unverified; no import/pull/startup or Engine config/mount changes.
- Canonical schemas/examples/generated client and operation coverage, ERP planning/mapping/quickstart/conformance, private config/demo and [boundary documentation](engine-boundary.md) updated. No UI changes or new browser/device claim.
- P13 may consume validated cancellable RoutingEngine, normalized Routing* models, explicit partial failures and P11 confirmed pins/revisions. Durable jobs and publication are P13; urgency/stitching/policy P14; actual outcome transactions P17. No next phase, commit, push or publication. Owner review pending.

## 22 September 2026 — Phase 11 confirmed locations and real map assets

- Starting HEAD `da8c75c`, clean tree; no AGENTS.md. Runtime identifies GPT-6; exact picker suffix/effort unavailable, so Astra/high is recorded as requested, not observed.
- Prerequisite check: **44 passed** across real PostgreSQL P09/P10/access tests. Original address storage, source revisions, current decisions/design references and command/access infrastructure inspected before edits.
- **A:** canonical scoped search/snapshot/confirmation API, private bounded Nominatim adapter, migration 0008, explicit human confirmation/provenance/history, tenant/branch/driver checks and atomic planning invalidation. **7 focused tests passed** before B; P09/P10 regression **28 passed**.
- **B:** real 45,260,921-byte Cairo PMTiles extract, verified checksum/structure, self-hosted Arabic style/glyphs/sprites/RTL/worker, range server/Nginx example and reusable keyboard/touch/manual picker connected to personal tasks and company review. Actual Chromium scenario **passed**; provider/account seams explicitly fixture-labelled.
- **C:** **9 focused tests passed** including actual PostgreSQL rollback, source/correction revisions, full-capacity rollback, branch/departure guard, source-scoped event payload, controlled HTTP timeout/malformed responses/cache eviction. Production-build browser scenario **passed** with real maps, failed tiles and manual recovery.
- Full `npm run check` **PASS: 17 files / 347 tests**, zero audit findings, lint/contracts/typechecks and all production builds. First full run failed only the stale migration-table inventory; corrected and fully rerun. Existing callback OpenAPI and lazy MapLibre chunk-size warnings remain documented. `maps-prepare.ps1` and local `db:migrate` passed; additive 0008 is applied.
- Canonical operation/schema/examples/client references, ERP planning/mapping/quickstart/conformance, UI/action/consistency guides, asset hashes/licenses/coverage and reproducible demo updated. [Exact evidence, failures and paths](phase-11-evidence.md); [demo and operations](locations-and-maps.md).
- Limits: private Nominatim port 8080 refused connection; no live geocoder pass or import is claimed. Maps cover Greater Cairo only (stored z0–15). Emulated touch is not physical-device/owner approval. P13 planning, P15 actual departure races, P25 signed event delivery, P28 full preparation and production deployment remain later work. Phase 12 may consume verified confirmed coordinates and revisions; **it was not executed**. No commit, push or publication; Engine datasets/mounts and original Stitch exports preserved.

## Current planning verification — revision 3

The D-110/D-111 amendment added model/reasoning settings to all 42 prompts and strengthened the ERP planning bundle, ownership mapping, external consumer setup/conformance checks and final release manifest. At the time of that planning verification, all application phases were Not started / Not run; the Phase 01 execution below is later evidence.

Python standard-library documentation validation on 21 September 2026: **PASS**.

- All 42 prompts contain exactly one model/effort block and agree with the consolidated guide: 35 Astra and 7 Sol recommendations, each with its phase-specific reason.
- The 126 checkpoints, 252 scenario rows and 195 earlier-phase dependency edges remain intact; all 65 requirement groups and 111 discovery decisions are mapped.
- All 66 current/archived planning documents were checked; 2,189 local-link occurrences and 562 requirement-fragment links resolve.
- ERP artifact ownership is present in the roadmap, phase prompts and coverage map; P26/P27 require early external conformance and P42 requires a clean consumer run plus release-manifest validation.
- All 46 protected files in the before/after snapshot are unchanged, including the archived package, original Stitch exports and the inspected Engine context/configuration files; all 18 image/HTML manifest hashes match.

This is document consistency and preservation evidence only. No application Vitest, external ERP integration, clean consumer setup or runtime model-performance comparison was executed. Those checks remain deliverables of the implementation phases.

## Earlier planning verification — revision 2

Replacement documentation was reviewed against the current master plan and validated with a Python standard-library check on 21 September 2026: **PASS**.

- 42 sequential prompts, 126 ordered checkpoints and 252 explicit acceptance-scenario rows.
- 195 unique earlier-phase dependency edges; no forward/cyclic dependency in the declared graph.
- 65 current requirement groups with phase links in both directions; all 109 discovery decision IDs mapped, including explicit supersessions.
- All 16 acceptance groups A–P, 8 canonical named Vitest files and 9 visual sources assigned.
- 64 current/archived Markdown documents checked; 2,069 local-link occurrences and 557 requirement-fragment links resolve.
- All 18 original Stitch image/HTML SHA-256 values still match the manifest; the old package is visibly superseded.

These results verify document structure and declared traceability, not that a future implementation already satisfies the business rules. They are not Vitest, browser, Engine, integration, device, load or deployment results. The semantic review separated ordinary execution UI, exception/correction UI and branch/closure UI into distinct tasks and retained early forecast capture, first-write atomic intent and independent received/applied evidence.

## Phase ledger

Each row records implementation, verification and owner review separately. None is completed merely because a prompt is detailed.

| Phase | Implementation | Verification | Owner review / evidence |
| --- | --- | --- | --- |
| [01 — Runnable workspace and test harness](phases/01-workspace-test-harness.md) | Implemented 22 Sep 2026 | Local checks pass; Engine runtime unavailable and not required | Awaiting owner review; [evidence](phase-01-evidence.md) |
| [02 — State vocabulary and canonical contract foundation](phases/02-state-contract-foundation.md) | Implemented 22 Sep 2026 | A/B/C passed; schemas/tooling verified, business operations designed | Awaiting owner review; [evidence](phase-02-evidence.md) |
| [03 — Visual system and requirement-driven action specification](phases/03-design-action-specification.md) | Specified 22 Sep 2026 | A/B/C document checks and paper demo pass; no runtime UI | Awaiting owner review; [evidence](phase-03-evidence.md) |
| [04 — Shared components and early simple-UX review](phases/04-representative-ui-review.md) | Implemented 22 Sep 2026 | 10 component tests + 6 Chromium tests pass; fixture only | Awaiting owner review; [review](ui-review.md), [evidence](phase-04-evidence.md) |
| [05 — PostgreSQL migrations and atomic command kernel](phases/05-postgres-atomic-command-kernel.md) | Implemented foundation | PostgreSQL 18.4 local checks passed | [Ordered evidence](phase-05-evidence.md); HTTP/auth/domain/sender remain later work |
| [06 — Tenant, branch and capability enforcement](phases/06-tenant-capabilities-isolation.md) | Implemented internal foundation | Real PostgreSQL isolation/migration/command tests and demo passed | [P06 evidence](phase-06-evidence.md); real identity/provisioning remains P07/P08 |
| [07 — Real login, recovery and separate sessions](phases/07-oidc-login-recovery-sessions.md) | Implemented 22 Sep 2026 | Ordered A/B/C; real PostgreSQL auth tests and 6 real Keycloak/Mailpit Chromium journeys passed | Awaiting owner review; [evidence and limits](phase-07-evidence.md), [reproducible demo](identity.md) |
| [08 — ERP provisioning and verified actor context](phases/08-erp-provisioning-actor-binding.md) | Implemented 22 Sep 2026 | Ordered A/B/C; PostgreSQL/provider fixtures, real Keycloak/browser and copied public HTTP consumer pass | Awaiting owner review; [evidence](phase-08-evidence.md), [demo](erp/consumer-quickstart.md) |
| [09 — Independent-driver task intake](phases/09-b2c-task-intake.md) | Implemented 22 Sep 2026 | Ordered A/B/C; real PostgreSQL/API tests, client tests and real Keycloak/Mailpit Chromium journey passed | Awaiting owner/device review; [evidence](phase-09-evidence.md), [demo](b2c-intake.md) |
| [10 — ERP task snapshots, receipt and atomic admission](phases/10-b2b-intake-admission.md) | Implemented 22 Sep 2026 | Prior recorded local PostgreSQL/API proof | [Existing P10 evidence](phase-10-evidence.md); ledger corrected in P12 |
| [11 — Confirmed locations and real map assets](phases/11-locations-map-assets.md) | Implemented 22 Sep 2026 | Prior local map/browser proof; 9 PostgreSQL/provider tests rechecked in P12 | [Existing P11 evidence](phase-11-evidence.md); live Nominatim unavailable |
| [12 — Routing Engine adapters and vehicle profiles](phases/12-engine-profile-adapters.md) | Implemented 23 Sep 2026 | Controlled HTTP / PostgreSQL verified; live Engine unavailable | [Evidence](phase-12-evidence.md), [demo](engine-boundary.md); owner review pending |
| [13 — Durable planning jobs and forecast revisions](phases/13-planning-jobs-forecast-storage.md) | Implemented 23 Sep 2026 | Ordered A/B/C; isolated PostgreSQL, worker kill/recovery, delayed provider and real HTTP restart verified | [Evidence](phase-13-evidence.md), [runbook/demo](planning-jobs.md); live Engine unavailable, owner review pending |
| [14 — Urgent-first route policy and manual fallback](phases/14-route-policy-manual-fallback.md) | Not started | Not run | No implementation result yet |
| [15 — Online round start and departure authority](phases/15-round-start-departure-lock.md) | Implemented 23 Sep 2026 | Ordered A/B/C PostgreSQL/HTTP checks passed; final verification recorded below | [Evidence](phase-15-evidence.md), [demo/handoff](round-start.md); P34 queue and P20 takeover remain later |
| [16 — Explicit current target, heading and arrival](phases/16-current-heading-arrival.md) | Not started | Not run | No implementation result yet |
| [17 — Delivery outcomes, whole pieces and exact collection](phases/17-outcomes-quantities-collection.md) | Not started | Not run | No implementation result yet |
| [18 — Deferral, whole-shipment retry and driver urgency](phases/18-deferral-retry-driver-urgency.md) | Not started | Not run | No implementation result yet |
| [19 — Round closure, workday closure and carry-forward](phases/19-workday-closure-carryover.md) | Implemented 24 Sep 2026 | A/B/C PostgreSQL/HTTP checks; 630-test full regression, migration and public conformance passed | [Evidence](phase-19-evidence.md), [demo/handoff](workday-closure.md); offline/device behavior remains P34, owner review pending |
| [20 — Online device takeover and preserved former-device evidence](phases/20-device-takeover-evidence.md) | Not started | Not run | No implementation result yet |
| [21 — Source-branch return requests and actual subset receipt](phases/21-source-return-receipt.md) | Not started | Not run | No implementation result yet |
| [22 — Branch interruption, resume and new dispatch cycles](phases/22-branch-interruption-redispatch.md) | Not started | Not run | No implementation result yet |
| [23 — Driver corrections and compatible evidence adoption](phases/23-bounded-driver-corrections.md) | Implemented 24 Sep 2026 | Ordered A/B/C checks; final 778 tests and all repository gates passed | [Evidence](phase-23-evidence.md), [demo/handoff](corrections.md); owner/device review pending |
| [24 — Coherent monitoring snapshots and scoped history](phases/24-coherent-monitoring-api.md) | Implemented locally | Ordered PostgreSQL/API checks, public HTTP demo and full gate passed: 34 files / 800 tests | [Evidence](phase-24-evidence.md), [API/demo and P25 handoff](monitoring.md); UI/native ERP/device/freshness measurements remain later |
| [25 — Durable outbox sender and signed delivery](phases/25-outbox-signed-delivery.md) | Implemented 24 Sep 2026 | Ordered A/B/C real PostgreSQL/HTTP/process checks passed; final gates recorded in evidence | [Evidence](phase-25-evidence.md), [protocol/demo/handoff](outbox-delivery.md); durable receiver/applied proof remains P26 |
| [26 — External mock inbox, projection and reconciliation](phases/26-mock-inbox-projection-recovery.md) | Implemented locally 24 Sep 2026 | Ordered A/B/C checks and standalone real process/database/HTTP conformance passed; final gates recorded in evidence | [Evidence](phase-26-evidence.md), [quickstart](erp/consumer-quickstart.md), [public recovery protocol](erp/receiver-protocol.md); P27 source/UI remains separate |
| [27 — Native mock ERP commands and source outbox](phases/27-native-mock-erp-source.md) | Not started | Not run | No implementation result yet |
| [28 — Connected daily work, preparation and route start UI](phases/28-online-preparation-journeys.md) | Not started | Not run | No implementation result yet |
| [29 — Connected ordinary driver delivery UI](phases/29-ordinary-driver-delivery-ui.md) | Not started | Not run | No implementation result yet |
| [30 — Focused driver exception and correction UI](phases/30-driver-exception-correction-ui.md) | Not started | Not run | No implementation result yet |
| [31 — Driver branch handover, resume and workday closure UI](phases/31-driver-branch-closure-ui.md) | Not started | Not run | No implementation result yet |
| [32 — Dispatcher monitoring and online synchronization feedback](phases/32-monitoring-sync-online-ui.md) | Not started | Not run | No implementation result yet |
| [33 — PWA downloads and atomic local action capture](phases/33-offline-local-capture.md) | Not started | Not run | No implementation result yet |
| [34 — Ordered replay and durable conflict recovery](phases/34-ordered-replay-conflict-recovery.md) | Not started | Not run | No implementation result yet |
| [35 — Safe offline account recovery and application updates](phases/35-offline-auth-updates-ux.md) | Not started | Not run | No implementation result yet |
| [36 — Effective workday reports and forecast comparison](phases/36-workday-timing-reports.md) | Not started | Not run | No implementation result yet |
| [37 — Equivalent authorized Excel exports](phases/37-authorized-excel-export.md) | Not started | Not run | No implementation result yet |
| [38 — Owner diagnostics and measured freshness/capacity](phases/38-diagnostics-freshness-capacity.md) | Not started | Not run | No implementation result yet |
| [39 — Recoverable deployment and migration release procedure](phases/39-deployment-migration-release.md) | Not started | Not run | No implementation result yet |
| [40 — Backup, isolated restore and recovery proof](phases/40-backup-restore-rehearsal.md) | Not started | Not run | No implementation result yet |
| [41 — Real-device and owner pilot walkthrough](phases/41-device-owner-pilot-review.md) | Not started | Not run | No implementation result yet |
| [42 — Final contract, readiness and ERP handoff](phases/42-final-contract-readiness-handoff.md) | Not started | Not run | No implementation result yet |

## 22 September 2026 — Phase 07 real login, recovery and separate sessions

Starting HEAD `2084bba` (`phase 6`), initially clean; work remains uncommitted. [Ordered evidence, failures, acceptance mapping and handoff](phase-07-evidence.md); [reproducible setup/demo and safe configuration](identity.md). Runtime identifies GPT-6; exact picker suffix/reasoning unavailable, so the requested Astra/high recommendation is not claimed as an observed setting.

- **A:** pinned local Keycloak/Mailpit, distinct realms/clients, migration 0003, PKCE/state/nonce, encrypted server token storage, secure separate cookies, CSRF/rate limits, current P06 context and logout. Four focused real-PostgreSQL/provider-fixture tests plus an actual Keycloak browser round trip passed before B.
- **B:** connected company code and phone registration/verified-email activation/recovery, immutable separate identities and independent logout; 10 focused auth tests and real issuer company/registration/recovery journeys passed before C. Recovery uses a pinned issuer authenticator for verified-email lookup while email login stays disabled.
- **C:** row-locked refresh rotation, absolute expiry, same-account reauthentication, live revocation/membership denial, safe recovery failures and local evidence preservation. Focused 21 auth tests passed; the final real issuer browser suite passed all six journeys. No durable queue claim.
- **Final verification:** `VITE_TAWSEL_API_BASE_URL=http://127.0.0.1:3001 npm run check` (PowerShell assignment in evidence) passed **12 files / 254 tests**, lint/types/contracts/builds and zero audit findings. Six P04 browser regression tests passed separately. Original source hashes and protected Engine/P05/P06 files preserved. One OpenAPI warning reflects the intentionally redirect-only callback. Fresh isolated realm import/offline policy inspection passed; a second issuer start was blocked by automatic review, so fresh-database browser execution remains unrun.
- **Contracts/delivery:** ten HTTP paths and twelve P07 operations verified locally; session schemas/examples/generated client/reference/coverage and ERP planning/mapping/consumer guidance updated. [Identity guide](identity.md) identifies issuer-hosted proof actions, safe secrets, demo users and exact startup commands. No production email/TLS, real device, owner approval, external ERP interoperability or P08 provisioning claim.
- **Handoff:** P08 may use migration 0003, `apps/api/src/auth`, stable issuer/subject principals, existing P05/P06 guards, canonical session types and real local issuer tests. It still owns provisioning/service credentials/verified actor delegation; P33/P35 own queues and account-exit guards. **No Phase 08 execution, commit, push or publication.**

## Entry checklist

Record the following from actual work:

- Phase number/title, code commit and relevant dependency/schema/config/Engine versions.
- Checkpoint A, B and C: concrete changes, focused check/result and any unresolved prerequisite.
- Changed artifacts and operations now implemented versus merely designed.
- Every acceptance scenario: test/demo evidence or precise failed/unrun reason.
- Exact meaningful Vitest and browser/device commands/results, identifying mocked boundaries versus actual services.
- UI additions/removals, screenshots/interaction findings and actual owner feedback, when applicable.
- Migration/config/setup/recovery notes and preserved Engine/user-data boundaries.
- Remaining required gaps, their practical effect and the exact artifacts the next phase may rely on.

No owner approval may be inferred from silence. Do not erase failed/unrun checks by advancing the phase number.
A later focused task may resolve a gap; cross-link its evidence instead of rewriting history as an earlier pass.
If actual implementation proves a phase too broad, preserve the remaining scope in named bounded subphases and update dependencies/coverage coherently.

## Outstanding environment evidence

No new live host inventory, deployed Engine/profile/dataset verification, production identity/email/TLS, map-coverage/resource, physical-device/PWA-duration, freshness/capacity or isolated backup restore was performed in this planning rewrite.
Operational targets remain unverified, and the detailed numerical defaults remain engineering proposals where the master plan labels them as such.

There is no released application, finalized implemented contract boundary or real shipping ERP connector yet.
The next execution prompt is [Phase 14 — Urgent-first route policy and manual fallback](phases/14-route-policy-manual-fallback.md). It may rely on the P13 durable queue, fenced worker, immutable candidate/forecast storage and session APIs, plus verified P12 adapters. [P13 evidence](phase-13-evidence.md) and [planning runbook](planning-jobs.md) define the exact limits. Complete policy/manual fallback remains P14 and active round start/baseline remains P15.

## 22 September 2026 — Phase 01 execution

Starting HEAD: `eacf6b3fa596a845b4290c6f3ad471b39551e8d8`. Full checkpoint logs and acceptance mapping are in [docs/phase-01-evidence.md](phase-01-evidence.md).

- Checkpoint A: added `apps/web`, `apps/api`, and `packages/shared`; safe required environment parsing; Node 24/npm 11 pinning and lockfile. `npm run dev` served the RTL shell on 5173 and actual workspace health on 3001. Docker Desktop was stopped, so Engine runtime state remains unverified and health truthfully reports `engine=not-checked`.
- Checkpoint B: fast selection passed 6 configuration assertions; integration selection passed 3 tests over a real ephemeral Fastify listener. An initial invalid-query test exposed silent additional-field stripping; validation was corrected and the unchanged test then passed. Missing API configuration exited 1 with the named key. Lint, typecheck, and build passed.
- Checkpoint C: added read-only CI, `scripts/setup-app.ps1`, lockfile-based setup, and operations documentation. The first isolated run exposed a hidden dependency on prior shared build output; command ordering was corrected. The repeated isolated `npm ci`/`npm run check` passed, preserved `.env` and an unrelated sentinel, and had no Engine data directory. A transient critical advisory in `concurrently@9.2.1` was removed by pinning `9.2.4`; final audit reports zero vulnerabilities.
- Browser evidence: a headed Chromium render at 390×844 with reduced motion showed the concise Arabic RTL shell. A first pass found a favicon 404; after adding the local SVG, console errors/warnings were zero. The typography follow-up verified computed Cairo weights 400/600/700/800, all four font loads, and localhost-only font requests. The current screenshot is `output/playwright/phase-01-cairo-rtl-mobile.png`; the OFL text is retained under `docs/licenses/`. P03 must formalize this baseline as shared typography tokens and P04 must reuse/verify it in representative components. The shell is static foundation content; the API evidence is from the real local handler, and no Engine call occurred.
- Actual runtime: Node `v24.19.0`, npm `11.1.0`, TypeScript `6.0.2`, Vite `8.3.0`, React `19.3.0`, `@fontsource/cairo 5.3.0`, Fastify `5.12.5`, Vitest `5.0.1`. Recommended model/effort was `gpt-5.6-sol`/`high`; the agent runtime exposed only GPT-5, not the exact picker variant or effort, so the latter are not asserted.
- Preserved boundaries: Engine Compose/startup/profile/VROOM config and Stitch exports were unchanged. No database/volume/import command ran. No ERP planning interface exists yet, so ERP mapping/consumer documents are unaffected and remain Phase 02+ deliverables.
- Handoff to Phase 02: rely on root `npm ci`, `npm run dev`, `npm run test:fast`, `npm run test:integration`, `npm run check`; workspace paths `apps/web`, `apps/api`, `packages/shared`; required `.env.example`; API port 3001; and the stable non-domain `GET /health` behavior. Do not treat workspace health as routing readiness or as a released public contract.

## 22 September 2026 — Phase 02 execution

Starting HEAD `3aec78ff91b27fdf17e34405eebdf5d62394986c`. Ordered evidence is in [phase-02-evidence.md](phase-02-evidence.md). Requested model/effort `gpt-6-astra`/`xhigh`; runtime exposes GPT-6 but exact picker variant/effort are unavailable and not inferred.

- Prerequisite: real Phase 01 `npm run test:ci` passed 2 files / 9 tests; no repair.
- Checkpoint A: canonical state/authority/invariant tables and two-of-three-piece manual design walk passed. No business handler or database guarantee is claimed. Full-capacity interruption proof remains P22.
- Checkpoint B: OpenAPI/common/envelope schemas, 60 valid and 35 invalid examples, portable generated client/reference and explicit Ajv 2020-12 tooling. Contract suite passed 105 tests; OpenAPI lint and consumer typecheck passed. Initial strictRequired error was fixed; no database/worker guarantee inferred.
- Checkpoint C: 147 explicit owned operations/events/local actions (31 event types); all master-plan families reviewed, only workspace health verified as a route. ERP start-here/planning/mapping and integration guide distinguish designed behavior, connector responsibilities and unknown real-ERP choices. `npm run test:contracts` passed 108 tests; `npm run contracts:demo` passed. Complete `npm run check` passed audit, lint, OpenAPI/generated checks, typechecks, 117 tests in 3 files and all builds. Final review added LF generation attributes and CI contract path coverage; clean `npm ci` passed with zero vulnerabilities.
- Exact versions, initial failures/fixes, acceptance mapping, changed paths and final verification are recorded in the [phase evidence](phase-02-evidence.md). Model picker variant/effort remain unavailable; no setting is inferred from the prompt. No UI/DB/worker/Engine/device/real-ERP verification is claimed.
- Phase 03 may rely on `docs/tracking-and-consistency.md`, `contracts/common.schema.json`, versioned envelopes/examples, `contracts/operations.json`, `docs/contract-coverage.md`, `docs/reference/public-contract.md`, generated `packages/api-client/src/schema.d.ts`, `docs/integration-guide.md` and the three `docs/erp/` foundation documents. Reproduce with `npm ci`, `npm run contracts:demo`, `npm run test:contracts`, `npm run check`. Preserve Cairo/RTL baseline and use the catalog's explicit actions and exclusions; complete feature schemas in their owning phases before handlers. No Phase 03 work executed, owner approval inferred, commit/push or publication performed.

## 22 September 2026 — Phase 03 specification

Starting HEAD `28d038a50717f2a3b41ec59c4715c54273b9e5f0`; initially clean. [Full ordered evidence and handoff](phase-03-evidence.md). Requested `gpt-6-astra` / `high`; runtime identifies GPT-6 but exact picker variant/effort unavailable. Consulted model-selection guide and did not infer the actual setting from the prompt.

- Prerequisite: actual Phase 01/02 code/artifacts inspected; `npm run test:ci` passed 117 tests, `npm run contracts:check` and `npm run contracts:lint` passed. No prerequisite repair needed.
- Checkpoint A: visually inspected all nine original image/HTML/metadata sets; recorded exact IDs/paths, layout roles/defects and retained/adapted/removed controls. `python scripts/check-ui-spec.py A` passed nine sets, 18 original hashes and all 105 control dispositions; extra decorative/click-styled affordances also reviewed. None inaccessible.
- Checkpoint B: created `DESIGN.md`, complete `docs/ui-spec.md`, `docs/ui-actions.md` and actual component registry/license review. `python scripts/check-ui-spec.py B` passed 63 action/effect rows covering 147 operations with role/state/surface/phase/requirements. Formalized existing self-hosted Cairo 400/600/700/800; no runtime component changes or dependency installation.
- Checkpoint C: 33 Arabic state cases and normal B2B login → daily → heading → arrival → result paper walkthrough, with B2C variation and clear blockers/receipt boundaries. Specified 360×800, 390×844, 1366×768, 1440×900, zoom, long Arabic/LTR text, safe areas and focus/reduced motion. `python -X utf8 scripts/check-ui-spec.py check` and `demo` passed; six deliberate omission/invented-operation mutations fail as required. These are document/token checks and an implementer paper review, not browser usability or owner approval.
- Canonical references: generated coverage/public reference cross-link action map; schemas/catalog/examples/client types unchanged because no wire contract changed. Existing ERP planning/mapping/index and client guidance now trace native ERP actions and honest waiting/receipt/application labels. External quickstart/conformance remains uncreated P26/P27 work.
- Final `npm run check` passed audit (zero vulnerabilities), lint, OpenAPI/generation, typechecks, 3 files/117 existing Vitest tests and all builds. `git diff --check` passed. Node 24.19.0 / npm 11.1.0 / Python 3.12.6; existing React 19.3.0, TypeScript 6.0.2, Vite 8.3.0, Fastify 5.12.5, Vitest 5.0.1, Cairo 5.3.0 retained. Full changed-path list, source hashes, corrected draft findings and failed exploratory search are in the phase evidence.
- Limits: no new UI runtime/browser/Playwright/DB/worker/Engine/issuer/map/device/ERP proof; no owner review received. P22 full-capacity branch interruption still needs transaction proof. Originals, Engine config/data/mounts, runtime application and dependency lockfile remain untouched. No publish/commit/push.
- Phase 04 may rely on `DESIGN.md`, `docs/ui-spec.md`, `docs/ui-actions.md`, `docs/ui-reference-audit.md`, `docs/ui-component-research.md`, the read-only checker/demo, unchanged original exports and verified P01/P02 foundation. It must implement and render representative fixtures, inspect current component APIs/pins, test interaction/focus/RTL and collect actual review findings. **Phase 04 has not started; design status is specified, owner review pending.**

## 22 September 2026 — Phase 04 representative UI review

Starting HEAD `3af79b8` (`phase 3`), initially clean. [Full ordered evidence](phase-04-evidence.md) and [actual review record](ui-review.md). Requested `gpt-5.6-sol` / `high`; runtime identifies the GPT-5 family but exact picker variant/effort are unavailable and not inferred.

- Checkpoint A: applied the P03 tokens and RTL/responsive rules; added shared button, field, status, contact, progress, tabs, stop and single Radix-overlay primitives. Cairo 400/600/700/800 remains self-hosted; Radix Dialog 1.1.23 and Lucide 1.47.0 are pinned with local license notices. Focused shared/web build and typecheck pass after correcting three strict-index findings.
- Checkpoint B: added a yellow-labelled, development-only `/__fixtures/driver-review` with login presentation, ready daily work, explicit start/heading/arrival/outcome stages, long Arabic/LTR data, loading/empty/missing-pin/another-device/no-answer/whole-piece partial-selection/pending/rejected states and selected-driver desktop monitoring. `driver-flow.test.tsx`: **PASS, 10 tests**. Production build tree-shakes fixture JavaScript and its marker scan passes; no business API/status was promoted from designed.
- Checkpoint C: Playwright Chromium covered 360×800, 390×844, 1366×768, 1440×900 and effective 200% reflow. Final `npm run test:browser:ui`: **PASS, 6 tests** for RTL, local Cairo weights/assets, long Arabic/LTR content, partial/no-answer/ownership/sync variants, reduced motion, focus/escape, draft retention, list/map correspondence and staff read-only authority. Visual inspection corrected the narrow initial start-action position and a desktop tab/panel-state mismatch before the final pass. Eight captures live under `output/playwright/phase-04/`.
- Initial/fixed failures: npm install/clean install exposed the existing OpenAPI-generator TypeScript-5 peer declaration while the checked workspace pins TypeScript 6; repository-scoped `.npmrc` now makes the deliberate legacy peer resolution reproducible for plain `npm ci`. The first production build lacked the intentionally required API-base environment; the first isolation script mishandled a URL path; the first browser launch lacked a completed headless-shell download; one reduced-motion check compared equivalent string formats. None is counted as a pass before correction.
- Boundaries: fixture-only UI; no identity, API, database, Engine, maps, worker, offline durability, device transfer or ERP call. No canonical contract/schema/example/client/ERP interface changed. No physical-device, browser-chrome zoom, OS-text, screen-reader or owner review evidence. Owner feedback remains **not received**.
- Final supported-runtime verification: bundled Node 24.19.0 + pinned npm 11.1.0; clean `npm ci` passed with zero vulnerabilities after the scoped peer-resolution repair; `npm run check` passed 4 files/127 Vitest tests and every audit/lint/contract/type/build/isolation gate; clean-install/final-audit browser rerun passed 6 tests. `git diff --check` passed.
- Handoff to Phase 05: reuse P01/P02 workspace and contract foundations. The new UI component/fixture artifacts may be reused only by later UI phases and do not establish transaction, lock or persistence behavior. No Phase 05 work, commit, push or publication was performed.

## 22 September 2026 — Phase 05 PostgreSQL and atomic command kernel

Starting HEAD `cbf1452c07b355887f346594ebe12c82637cf1c1`, initially clean. [Full ordered checkpoint evidence, changed paths and handoff](phase-05-evidence.md). Recommended `gpt-6-astra` / `xhigh`; runtime identifies GPT-6 but exact picker suffix/effort are unavailable and not inferred. Supported checks used Node 24.19.0 / npm 11.1.0 / PostgreSQL 18.4 / pg 8.23.0.

- **A — lifecycle:** dedicated application/control/test databases, target/owner/purpose/version guards, checksummed versioned SQL runner, transaction and pool lifecycle. Fresh/racing migration, mismatch/nonempty target refusal and rollback passed on PostgreSQL before B.
- **B — command:** tenant/source/action uniqueness plus canonical payload/actor hash, durable same-result retry, mismatch conflict, transactional feature/progress hooks, audit, rejection evidence and outbox intent. Faults after every write left zero partial accepted state. Focused 15 tests passed before C.
- **C — recovery:** independent-connection commit/rollback/collision races with database-observed barriers; actual process termination before commit and after commit/before response; fresh-pool recovery; 30-day full responses, unresolved holds and permanent compacted identities. Focused 23 tests passed before review additions; final database suite passed **27 tests** including connection loss, snapshot mutation, lock order and Fastify pool closure.
- **Verification:** clean install in an isolated source copy and root `npm run check` both passed **7 files / 167 tests**, audit (zero vulnerabilities), lint, canonical contracts/types, all builds and production fixture isolation. The final added pool-close test subsequently passed with the full 27-test database suite/API typecheck. Current inventory totals 168 tests. P04 prerequisite browser rerun passed **6 Chromium tests** after a small configurable-port/origin-check repair. Root clean install had a Windows locked-binary failure; dependencies were restored without stopping the user's existing Vite process and clean installation was proved in the isolated copy.
- **Reproduce:** `npm run db:local:start`, `npm run db:migrate`, `npm run test:database`, `npm run db:demo`; see [operations](operations.md) for credentials, native/CI setup, startup/shutdown and cleanup ownership. Dedicated PostgreSQL stop/start and compiled API health smoke passed; no disposable test databases or synthetic app tables remain.
- **Contracts/handoff:** canonical ActionResult full/compacted schema and examples, generated public types/reference/coverage and consumer/ERP planning/mapping guides updated. Internal `getCommandResult` is verified; HTTP `action.getResult` stays designed until P06–P08 authentication. No sender, shipment model, membership administration, ERP build or next-phase work is included. Engine configuration/data paths and original Stitch exports were not changed; Engine volume contents/runtime readiness remain unverified. No commit/push/publish.
- **P06 may rely on:** `db/migrations`, `apps/api/src/db`, `apps/api/src/commands` and `apps/api/test/support` for actual migration, transaction, identity, locks, audit/evidence/outbox and retention behavior. It must add real tenant/resource/permission assertions and trusted bindings; generic counter tests prove no feature-specific business rule.

## 22 September 2026 — Phase 06 tenant, branch and capability enforcement

Starting HEAD `62f27fe` (`phase 5`), initially clean. [Ordered evidence, acceptance mapping, changed paths and limits](phase-06-evidence.md); [permission contract and required guard integration](authorization.md). Recommended `gpt-6-astra / xhigh`; runtime identifies GPT-6 but exact picker suffix/effort is unavailable and not inferred. Model-selection guide consulted. Retained Node 24.19.0 / npm 11.1.0 / PostgreSQL 18.4 / pg 8.23.0; no dependency changes.

- **Prerequisites:** actual P02/P05 artifacts inspected; 27 PostgreSQL and 113 contract tests passed before editing, with generated-contract checks. No prerequisite repair needed.
- **A:** additive typed tenants, immutable subject links, separate accounts/memberships, single company role, explicit exceptions, branches/drivers/integrations and tenant-scoped FKs. Focused **18 tests passed** before B. Existing legacy keys gain no automatic trust; P05 accepted data survives the upgrade.
- **B:** current capability resolution, resource SQL/row guards and mandatory lifecycle composition, trusted scope assertions and transaction-lifetime authorization. Focused **4 effective-access tests passed** before C; role edits, explicit allow/deny/inherit, branch equality, powerless Admin label and disabled membership verified.
- **C:** actual PostgreSQL isolation and guarded commands with labelled principals/synthetic records. Initial **20 isolation tests passed**, then review added opposite-order revocation, actual context shape/mutation and retained-data upgrade checks. Full final inventory includes **39 authorization tests**, **28 database tests** and real observed independent-connection locking. No mock transaction or production identity-selecting header.
- **Verification:** final `VITE_TAWSEL_API_BASE_URL=http://127.0.0.1:3001 npm run check` (PowerShell environment assignment documented in evidence) passed every gate, **10 files / 223 tests**, zero audit vulnerabilities, contracts/types/builds and production fixture exclusion. Initial run passed 222 tests but build lacked the required environment variable; corrected and rerun, not treated as a pass. `db:migrate` applied 0002 then reported already current; `access:demo` passed and cleaned up. Zero leftover disposable DBs or synthetic application tables; whitespace/protected-path checks passed.
- **Contracts:** canonical override/effect/context schemas and lifecycle denial, 70 valid + 45 invalid examples, generated client/reference/operation coverage and ERP planning/mapping/client guidance updated. All 147 catalog operations retain their honest availability; no business HTTP operation/event became implemented merely from internal guard proof.
- **Limits/handoff:** authentication is still fixture-labelled; P07/P08 must verify actual issuer/session/service/actor bindings and provisioning. Feature adapters must load/lock authoritative scope and lifecycle on the same transaction and filter before aggregates/exports. No OIDC/password, company admin UI, shipment command, worker/export endpoint, browser/device/ERP/deployment proof or postdeparture staff override. Engine/configuration/data and original Stitch exports preserved. P07 may rely on 0002, `apps/api/src/access`, retained P05 primitives, canonical access types and runnable authorization tests/demo. **Phase 07 not executed; no commit/push/publish.**

## 22 September 2026 — Phase 08 ERP provisioning and verified service context

- Starting HEAD `f140e5dd38c11c75c75c64ac12cbcd8223e4d050`; clean tree, no AGENTS.md. Actual runtime identifies GPT-6; picker suffix/reasoning is unavailable, so requested Astra/xhigh is not asserted as observed.
- Prerequisites actually rerun: contracts 138, database 28, authorization 39, auth 21 tests passed. No prerequisite repair.
- **A:** closed schemas, operator source/subject bootstrap, scoped credentials/rotation/disable and actor=null service audit. Two API/PostgreSQL checks passed before B.
- **B:** all P08 projection commands through the atomic kernel, monotonic source revisions, durable issuer leases/retries and generation fencing. Twenty-nine provisioning/isolation tests passed before C, using real PostgreSQL and labelled issuer HTTP fixtures.
- **C:** repeatable company/two-branch/role/exception/two-driver CLI; generated client/examples and public conformance; real Keycloak provision/login/role-change/disable/session-revocation flow passed. Copied public-only client/conformance ran independently without application/database/operator credentials. Eleven P08 tests plus 22 isolation tests pass, including actual worker process termination and recovery.
- **Final checks:** npm run check passed audit (zero findings), lint/contracts/typechecks, 13 files / 289 tests and all builds. Real issuer browser and copied public HTTP consumer passed separately. Original Engine/Stitch and P05–P07 migration content is unchanged.
- Canonical operations now verified locally: 12 provisioning commands, two reads and provisioning.changed durable intent; signed delivery remains P25. Migrations 0004/0005, implementation, exact commands/results, captured UI, failures and limits are in [phase evidence](phase-08-evidence.md). [Public demo](erp/consumer-quickstart.md), [trust/recovery](provisioning.md), [ERP planning input](erp/ERP-PLANNING-INPUT.md) and [field mapping](erp/field-and-status-mapping.md) are current.
- Existing account UI reused; owner/device review, production issuer permissions/TLS/email and a real ERP connector remain unverified. ERP controls credential creation/enablement; source subjects need operator reservation. Human delegation is rejected; no staff actor is fabricated from a service credential.
- Phase 09 relied on the verified P05 kernel, P06 access guards, P07 sessions and P08 source/subject/branch/role/driver mappings, retained provisioning results and issuer status; its result is recorded below. This historical P08 handoff did not itself execute P09. No commit, push or publication occurred in P08.

## 22 September 2026 — Phase 09 independent-driver task intake

- Starting HEAD `main` at the Phase 08 tip; initially clean and no `AGENTS.md`. The active runtime exposes the Codex GPT-5 family, not the exact picker suffix/effort, so the requested `gpt-5.6-sol` / `high` setting is not asserted as observed.
- **Prerequisite:** the real P08 provisioning/isolation check passed 2 files / 33 PostgreSQL tests. A small P07 prerequisite gap was repaired by extracting its same-origin CSRF/session-cookie guard for reuse by feature routes.
- **A:** migration 0006, canonical B2C schemas/examples/OpenAPI/client, personal-tenant create/list/get/revise service, original address/pin source record, optional EGP minor units, intake events, revision/departure guards and P05 action replay. Focused API/PostgreSQL tests passed 4/4; contract suite passed 165 tests.
- **B:** connected Arabic RTL `/tasks` list/create/edit UI with contextual validation, optional collection/instructions, retained draft/action ID and explicit unresolved-location blocker. Client fixture tests passed 3/3 and the production build passed with its required API-base input. No fake map/pin UI was added.
- **C:** the real local Keycloak/Mailpit → browser session → Fastify → PostgreSQL Chromium demo registered a fresh personal account, created/reloaded two same-address tasks and corrected only one; Playwright passed 1/1. Visual inspection at 390×844 found the primary action, separate cards, phone/amount and amber location blocker readable without horizontal clipping. Operation/UI coverage, ERP boundary notes, generated client/reference and reproducible [demo](b2c-intake.md) are current.
- **Final checks:** after recording an offline local-database run and updating two migration-inventory assertions for 0006, `npm run check` passed zero audit findings, lint/contracts/types, 15 files / 300 tests and all production builds. Final Keycloak + separately started Mailpit browser rerun passed 1/1. OpenAPI retains only the existing redirect-callback 302 lint warning.
- Full commands, classifications, failures, changed areas and handoff are in [Phase 09 evidence](phase-09-evidence.md). P11 still owns real location resolution/maps; P13–P15 own planning/departure. No B2C splitting, inventory, returns, billing, Engine routing, offline guarantee, physical-device/owner approval, Phase 10 execution, commit, push or publication.

## 22 September 2026 — Phase 10 ERP snapshots, receipt and atomic admission

- Starting HEAD `c67d15a` (`phase 8`); preserved the existing uncommitted Phase 09 implementation. No AGENTS.md found. Runtime identifies GPT-6; exact picker model suffix/reasoning effort are unavailable, so the requested Astra/xhigh setting is not asserted as observed. No material prerequisite defect; the actual P05/P06/P08/P09 check passed **59 PostgreSQL tests** before editing.
- **A:** closed canonical source/assignment/revision/error contracts; explicit splitting and exact outstanding whole-piece/unit/shipping EGP amounts; immutable source snapshots/lines and dispatch/assignment history in migration 0007; source/tenant/branch authority and explicit operator intake grants. API typecheck and **3 focused tests passed** before B.
- **B:** prepared work stays upcoming; definitive receipt, remaining-stop reservations, audit/result and replan/outbox intent commit atomically. Independent connections with database-observed lock barriers prove competing whole batches cannot jointly exceed 50. Branch slots count; historical/prepared/unresolved/future-held records do not. Rollback leaves no tentative assignments/history/intent and retains rejection evidence. **9 tests passed** before C.
- **C:** revisioned predeparture source/urgency edits, ordinary removal/reassignment without reasons, source-scoped held/prepared reads and accepted/rejected/pending result recovery. Final focused suite **18 passed**, including source-versus-receipt and duplicate races, frozen-field guards, full-destination rollback and source/tenant isolation. A copied independent public-only consumer passed twice against real HTTP/PostgreSQL with no DB/operator/issuer credentials; its journal is a CLI fixture, not the future ERP transactional outbox.
- **Final verification:** `npm run check` **PASS**, zero audit findings, lint/canonical checks/all typechecks, **16 files / 336 tests**, production builds/fixture isolation. First run exposed two obsolete contract availability allowlists; fixed and rerun. `npm run db:migrate` applied 0007 locally. `npm run intake:demo` passed; zero disposable test DBs or application fixture tasks remained. Retained Node 24.19.0/npm 11.1.0/PostgreSQL 18.4 and dependency pins. One existing redirect-only OpenAPI warning remains.
- Canonical schemas/examples/OpenAPI/client/reference/operation map, ERP planning input/field mapping/consumer quickstart, source obligations and [reproducible intake demo](b2b-intake.md) are updated. Exact commands, changed paths, failed checks and evidence classifications are in [Phase 10 evidence](phase-10-evidence.md).
- **Handoff:** Phase 11 may rely on the real source snapshot/receipt/read APIs, stable task/cycle IDs, immutable original location/line inputs, held/prepared/readiness fields, P06/P08 authorization and migration 0007. Real geocoding/maps remain P11, planning worker P13, active start races P15, outcomes P17, return/redispatch P21–22 and signed/native ERP integration P25–27. No new UI/browser/device/Engine/real ERP/production proof, Phase 11 execution, commit, push or publication. Engine assets/mounts and original Stitch exports were preserved.

## 23 September 2026 — Phase 13 durable planning jobs and forecast revisions

- Started at clean HEAD `1567f05` (`phase 12`); no AGENTS.md. Runtime identifies GPT-6, exact picker suffix/effort unavailable; requested Astra/xhigh not represented as observed. Model guide consulted. Actual prerequisites passed **89 tests** before editing.
- **A:** migration 0009, canonical contracts and atomic intake/pin-to-job snapshots/intent linkage, bounded driver queue and immutable input. API typecheck + **30 focused PostgreSQL tests passed** before B.
- **B:** committed claims/leases, process recovery, token fencing and atomic draft/forecast/workload storage without round activation. **7 focused tests passed** before C, including an actual worker kill during HTTP and natural lease expiry.
- **C:** locked current-input comparison, superseded results/current requeue, session/CSRF planning/status/history APIs and typed client. **47 focused/regression tests passed**, plus real listening-HTTP session/client poll after API restart. Later review adds retained-data upgrade, status-shape and estimate-boundary checks.
- **Final verification:** `npm run check` **PASS**, audit zero vulnerabilities, lint/contracts/types, **20 files / 418 tests**, all builds and production fixture isolation. Existing callback/MapLibre warnings remain. Zero disposable test DBs; protected Engine/Stitch/prior migrations/lockfile unchanged. Actual final runtime Node 24.19.0 / npm 11.6.2 / PostgreSQL 18.6. Earlier failed inventory/reference assertions and the receipt-generation review correction are recorded in the evidence.
- Demo and public-only conformance pass. Re-run with `npm run planning:demo`, `npm run test:planning`, `npm run test:erp:planning`; worker via `npm run planning:worker`. [Exact ordered evidence, final checks and changed artifacts](phase-13-evidence.md), [trigger/input/forecast/API runbook](planning-jobs.md).
- Live probe at `2026-09-22T23:45:21.164Z` (23 Sep Cairo): nine route/table/optimization requests unavailable, Docker inventory unavailable, local data only `.gitkeep`; no import/start/config/mount edit. No UI/browser/device, production deployment, real ERP or owner-review claim.
- Canonical schemas/examples/OpenAPI/generated client/reference/operation map and ERP planning/mapping/quickstart/conformance updated. Phase 14 may use 0009, `apps/api/src/planning`, tested P12 adapters and stored draft/forecast identities; it still owns complete urgency/earliest/current-prefix/endpoint validation and manual fallback. Phase 15 owns active start/first baseline. No next phase, commit, push or publication.

## 23 September 2026 — Phase 14 urgent-first route policy and manual fallback

- Started at clean HEAD `c7c22c6` (`phase 13`); no AGENTS.md. Runtime identifies GPT-6; exact picker suffix/effort unavailable, so requested Astra/xhigh is not asserted as observed. Model-selection guide consulted. Actual P10/P12/P13 prerequisite check: **4 files / 66 tests passed** before editing; no prerequisite code repair.
- **A:** current prefix → eligible urgent → ordinary optimization, propagated group origin and common forecast offsets, separate waiting/service, explicit unassigned work. API typecheck and **2 focused tests passed** before B. Policy is a heuristic, not global optimality.
- **B:** migration 0010, complete ID/profile/coordinate/timing/earliest/endpoint/capacity validator, durable ready/partial states and invalid/dependency result distinction. **25 publication tests passed** before C, including malformed candidates producing no plan/forecast rows, fixed-endpoint timing and same-address identity. Historical P13 drafts remain unvalidated.
- **C:** migration 0011, revisioned manual order/select-first API, immutable manual forecast membership with null unknown road/arrival/finish estimates, retained valid order on failure, and atomic stale-optimizer supersession. **29 tests passed**, plus real HTTP/session/CSRF/client/API-restart check. Later review verifies actual ERP urgent/prepared/future inputs, explicit branch service, missing final-road dependency, independent manual CAS, rollback, and retained P13 upgrade.
- **Final check:** `npm run check` **PASS**, audit zero vulnerabilities, lint/OpenAPI/generated checks/all typechecks, **20 files / 453 tests**, all builds and production fixture isolation. Actual runtime Node **24.19.0**, npm **11.6.2**, PostgreSQL **18.6**; dependency pins/lockfile retained. Existing redirect-only OpenAPI warning and MapLibre chunk-size warning remain. Local migrations 0010/0011 applied; no disposable test databases or application fixture tasks/plans remained.
- `npm run planning:policy:demo`, `npm run planning:demo`, `npm run test:erp:planning` passed. New demo stores and inspects **manual → ready → manual** revisions with three preserved forecasts and a superseded delayed job. Canonical schemas/examples/client/operation coverage, ERP planning/mapping/quickstart and conformance updated. [Exact commands, failures, review findings and changed paths](phase-14-evidence.md); [reproducible policy/API/demo/handoff](route-policy.md).
- Live probe `2026-09-23T00:16:55.235Z`: all nine routing/profile requests unavailable; Docker inventory unavailable; datasets only `.gitkeep`. Actual Engine suitability and live grouped-policy behavior remain unverified. No fixture is labelled live evidence. No new UI/browser/device/real ERP or production claim.
- **P15 may rely on:** migrations 0009–0011, `planning/policy.ts` complete validation, `manual.ts` locked revisioned publication, P13 job/lease/input fences, ready/manual/partial APIs and preserved plan/forecast/workload identities. It must revalidate and bind a current ready/manual revision during online start; no active round/baseline exists yet. P16–18 execution/outcome/retry writers and P22 branch interruption remain assigned later work. No Phase 15 execution, commit, push or publication. Engine/Stitch assets and prior migrations preserved.
- Automatic approval review blocked cleanup of one empty temporary upgrade-test directory (“blocked by policy”, no further reason); it was left untouched. Verification and migrations ran separately. [Exact path and disposition](phase-14-evidence.md).
