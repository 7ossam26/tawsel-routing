# Phase 01 — runnable workspace and test harness evidence

Date: 22 September 2026  
Starting HEAD: `eacf6b3fa596a845b4290c6f3ad471b39551e8d8` (`main`)  
Recommended setting: `gpt-5.6-sol`, `high`  
Actual setting: the runtime identifies this agent only as GPT-5; the exact picker model variant and reasoning effort are not exposed to the agent, so they cannot be asserted as verified.

This ledger records application-workspace evidence only. Historical Engine results in `STACK-CONTEXT.md` were not promoted to current results.

## Prerequisite inspection

- No applicable `AGENTS.md` was present. The working tree was clean at the start.
- The existing Engine paths and ports were inspected: `docker-compose.yml`, `setup.ps1`, `profiles/motorcycle.lua`, `vroom-conf/config.yml`, `data/`, and the root operating documentation. The application API uses port `3001`, leaving VROOM on `3000` and OSRM/Nominatim ports unchanged.
- All three OSRM `.osrm.cell_metrics` artifacts and the Egypt PBF exist in this checkout. Docker Desktop's Linux daemon was unavailable (`docker compose ps` could not open `dockerDesktopLinuxEngine`), so current container health and the Nominatim volume were not verified. This does not block the Engine-independent workspace, but no current routing/geocoding success is claimed.
- Small prerequisite repair: the existing `.gitignore` did not protect application dependencies, build output, coverage, logs, or local environment files. Those patterns were added before installation; existing Engine ignore rules remain in place.
- Baseline SHA-256 hashes were captured for `docker-compose.yml`, `setup.ps1`, `profiles/motorcycle.lua`, `vroom-conf/config.yml`, `test-vrp.json`, and `stitch-export/manifest.json` for the final preservation check.

## Checkpoint A — workspace boot

### Changed

- Added npm workspaces with explicit `apps/web`, `apps/api`, and `packages/shared` boundaries.
- Added a minimal React/Vite Arabic RTL shell that explicitly says delivery capabilities are not present.
- Added a Fastify API with `GET /health`. Its response is scoped to workspace health and reports the Engine as `not-checked`.
- Added shared strict TypeScript configuration and configuration parsers. `.env.example` contains only safe local values; API port `3001` avoids VROOM's `3000`.
- Pinned the Node/npm range and exact package versions. The lockfile is generated by npm.

### Focused result

Runtime observed: Node `v24.19.0`, npm `11.1.0`. Node 24 is an LTS line; Vite 8 supports Node `^20.19.0 || >=22.12.0`, Vitest 5 supports Node `^22.12.0 || ^24.0.0 || >=26.0.0`, and Fastify 5 requires Node 20 or newer. Node `>=24.11.0 <25` therefore provides one supported overlap and matches the exercised host.

Commands and results:

```text
npm install
PASS — 215 packages installed; package-lock.json generated.

npm run typecheck
PASS — shared, API, and web projects.

npm run build
PASS — shared/API TypeScript output and Vite production bundle.

npm run dev
PASS — API listened on http://127.0.0.1:3001 and Vite on http://127.0.0.1:5173.

Invoke-RestMethod http://127.0.0.1:3001/health
PASS — status=ok, scope=workspace, engine=not-checked.

Invoke-WebRequest http://127.0.0.1:5173/
PASS — HTTP 200 with the application root.
```

The first dependency attempt exposed two unpublished version pins (`@eslint/js@10.0.3` and the initial `tsx` patch). Registry metadata was rechecked and the pins were corrected before the successful install and lockfile generation. No partial install or lockfile remained from the failed attempt.

### Unresolved dependency

Fresh Engine availability is unverified because Docker Desktop is stopped. The web/API boot result deliberately has no Engine dependency and makes no routing-success claim.

## Checkpoint B — testing and build

### Changed

- Added `fast` and `integration` Vitest projects with separate include boundaries and commands.
- Added six fast assertions for valid configuration, absent required configuration, and malformed port values.
- Added three integration tests that bind a real Fastify listener on an ephemeral localhost port and issue real HTTP requests to the actual handlers.
- Health describes only workspace liveness and says `engine=not-checked`. Unsupported health query input returns a stable 400 validation envelope; an unimplemented delivery route returns a stable 404 envelope.
- Configured Fastify/Ajv not to silently remove unexpected query fields, so the invalid-boundary test initially failed with HTTP 200 and then passed only after the validation behavior was corrected.

### Focused result

```text
npm run test:fast
PASS — 1 file, 6 tests.

npm run test:integration
PASS — 1 file, 3 tests; real localhost listener and HTTP requests.

node apps/api/dist/main.js  # with TAWSEL_API_HOST/PORT absent
EXPECTED FAILURE — exit 1, "Configuration error: TAWSEL_API_HOST is required".

npm run lint
PASS.

npm run typecheck
PASS — all three workspaces.

npm run build
PASS — shared, API, and web builds.
```

The first integration run failed because Fastify's default Ajv behavior removed an unsupported health query field and returned 200. The server now sets `removeAdditional: false`; the same request is rejected with the tested 400 response. This is retained failure evidence, not a substituted passing assertion.

### Unresolved dependency

No database, identity provider, worker, mock ERP, or Engine is exercised or claimed. PostgreSQL transaction/durability testing begins only when those responsibilities exist in later phases.

## Checkpoint C — reproducible setup

### Changed

- Added `.github/workflows/application-checks.yml` with read-only contents permission, Node `24.19.0`, `npm ci`, and `npm run check`.
- Added `scripts/setup-app.ps1`. It validates Node/npm, creates `.env` only when absent, preserves an existing `.env`, uses the lockfile, and optionally runs all checks. It never calls Docker or an Engine script.
- Added substantive `docs/operations.md` setup, start/stop, health semantics, check selection, CI, future-service, troubleshooting, and preservation guidance.
- Updated the root README, master-plan current evidence, coverage map, and implementation ledger. No ERP field/status/client document changed because this phase exposes no ERP public interface.
- Added a local SVG favicon after the first browser render exposed a 404.
- Replaced the system-font rendering baseline with locally packaged Cairo 400/600/700/800 for Arabic and Latin glyph ranges. The CSS disables synthetic weights, assigns deliberate paragraph/status/label/heading weights, and retains the SIL OFL 1.1 text in `docs/licenses/Cairo-OFL-1.1.txt`. There is no runtime Google Fonts dependency.

### Focused result

The first clean isolated run failed during API typechecking: it revealed an undeclared dependency on `packages/shared/dist` left by an earlier build. Root `typecheck` now builds the shared package first. The same isolated directory was then rerun with no pre-existing shared build output:

```text
.\scripts\setup-app.ps1 -Check
PASS — npm ci installed 215 packages; audit found 0 vulnerabilities.
PASS — lint, typecheck, 9 Vitest tests, and all builds.
PASS — existing .env preserved.
PASS — unrelated preserve-me.txt sentinel unchanged.
PASS — isolated copy contained no data/ Engine directory.
```

An audit during setup reported two critical findings through `concurrently@9.2.1`/`shell-quote`. The direct dependency was updated to the compatible patched `concurrently@9.2.4`; a fresh `npm ci` and `npm audit --audit-level=high` then reported zero vulnerabilities. The audit is part of `npm run check`.

Headed Chromium render evidence used a 390×844 viewport and `prefers-reduced-motion: reduce`. The Arabic heading, purpose text, web status, and explicit absence of delivery capability were present in the accessibility snapshot. The first pass had one favicon 404; after the local asset was added, the repeated run had zero console errors and warnings.

The typography follow-up was then measured in the same real browser. Computed styles reported `Cairo, Tahoma, Arial, sans-serif`, with heading `800`, paragraph `400`, label `700`, and status `600`; `document.fonts.status` was `loaded` and checks for all four Cairo weights returned true. The network log showed only localhost CSS/font requests and no Google Fonts request. Current screenshot: `output/playwright/phase-01-cairo-rtl-mobile.png`. This remains a static foundation shell with no fixture or domain interaction; API evidence came separately from the real local handler.

### Unresolved dependency

The GitHub-hosted workflow was authored from current official action documentation but was not run remotely in this task. Docker/Engine health remains unavailable because Docker Desktop is stopped. Neither gap changes the locally verified workspace result.

## Acceptance scenarios

| Scenario | Evidence and result |
| --- | --- |
| Required configuration absent | Fast test asserts the named key; built API exits 1 with `Configuration error: TAWSEL_API_HOST is required`. PASS. |
| Web and API started | `npm run dev`; web HTTP 200, real health handler returned workspace-only status. Headed Chromium render captured. PASS. |
| Engine unavailable | Docker daemon unavailable; all application checks passed and health returned `engine=not-checked`, never routing success. PASS. |
| Developer runs tests | Documented watch, fast, integration, CI, and aggregate commands; selections returned meaningful exit status. PASS. |
| Application setup repeated | Isolated lockfile setup passed, preserved `.env` and unrelated sentinel, and had no Engine data directory. PASS. |
| Invalid request to boundary | Actual HTTP request with unsupported health query returned stable 400; absent delivery route returned stable 404. PASS. |

## Versions and final verification

Observed runtime and direct package versions:

| Component | Version |
| --- | --- |
| Node.js | `24.19.0` |
| npm | `11.1.0` |
| TypeScript | `6.0.2` |
| React / React DOM | `19.3.0` |
| Cairo package | `@fontsource/cairo 5.3.0` (400/600/700/800, locally bundled) |
| Vite / React plugin | `8.3.0` / `6.1.1` |
| Fastify | `5.12.5` |
| Vitest | `5.0.1` |
| ESLint / typescript-eslint | `10.0.3` / `8.70.1` |

Final required command set:

```text
npm ci
npm run audit
npm run lint
npm run typecheck
npm run test:fast
npm run test:integration
npm run test:ci
npm run build
npm run check
```

All passed locally after the fixes above. Engine configuration, datasets, volumes, original Stitch exports, secrets, and ERP artifacts were not changed. No database/identity/worker guarantee is claimed.

## Phase 02 handoff

Phase 02 may rely on:

- root npm workspaces and the committed lockfile;
- `apps/web`, `apps/api`, and `packages/shared` boundaries;
- strict shared TypeScript configuration and the required `.env.example` flow;
- `npm run dev`, `test:fast`, `test:integration`, `test:ci`, `typecheck`, `build`, and `check`;
- API port `3001`, web port `5173`, and the tested non-domain `GET /health` envelope;
- `docs/operations.md` and `scripts/setup-app.ps1` as the reproducible local entry point.

It must not infer a released contract, delivery endpoint, application database, identity, worker, Engine adapter, routing success, or ERP capability from these artifacts. Phase 02 was not executed here.
