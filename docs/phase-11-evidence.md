# Phase 11 — confirmed locations and map assets

Started 22 September 2026 (Africa/Cairo), HEAD `da8c75c`; clean worktree. No repository/ancestor AGENTS.md found. Runtime identifies GPT-6; exact picker suffix/effort is unavailable, so the requested Astra/high is not asserted as observed. Consulted the model-selection guide. No delegation, commit, push or publication.

Read current master sections 9/16, discovery amendments through D-112, decision map, phase index and assigned requirement rows, implementation ledger, DESIGN/UI specification, P09/P10 evidence and actual intake/access/kernel/migration implementations. Inspected original location review and route preparation HTML/metadata/screens, plus active-trip map language. Prototype GPS/accuracy/StreetView controls are excluded by current decisions.

Prerequisite command (supported Node 24.19.0/npm 11.1.0 PATH from P10): `node --env-file=.env.database.local node_modules/vitest/vitest.mjs run --project integration apps/api/test/integration/b2c-intake.test.ts apps/api/test/integration/b2b-intake-admission.test.ts apps/api/test/integration/authorization-isolation.test.ts` — **44 passed, 3 files**, real isolated PostgreSQL. `npm run db:local:start` succeeded. Private Nominatim `http://localhost:8080/status?format=json` refused connection; no import/start attempted. Live provider evidence remains distinct from controlled provider tests.

References consulted: https://nominatim.org/release-docs/5.0/api/Search/ , https://docs.protomaps.com/basemaps/maplibre , https://docs.protomaps.com/pmtiles/maplibre , https://maplibre.org/maplibre-gl-js/docs/API/functions/setRTLTextPlugin/ .

## Checkpoint A (before map/picker implementation)

Added closed canonical location schemas/read/search/confirmation paths, generated client/reference, migration 0008, private Egypt-bounded Nominatim adapter (4s timeout, 5 results, 256KiB response, 128-entry/5-minute account/task/source-scoped cache, eight in-flight requests), human session routes, explicit manual/candidate confirmation, immutable confirmation history and atomic driver planning-input revision/pending intent. Source changes make old confirmations unusable. P09/P10 projections and P10 reservation rules now consume execution-location readiness. Staff authority is checked at the handler under shared invariant locks; assigned drivers with correction capability may correct after departure.

`npm run typecheck -w @tawsel/api` — PASS. `node --env-file=.env.database.local node_modules/vitest/vitest.mjs run --project integration apps/api/test/integration/location-provenance.test.ts` — **7 passed**, real isolated PostgreSQL. Earlier P09/P10/location regression pass: **28 tests, 3 files**. Proves original retention, explicit confirmation, invalid coordinate rejection, scoped candidate cache, controlled empty/503 provider behavior, hidden tenant IDs, stale/concurrent confirmation, history/replan idempotency, B2B staff/driver lifecycle and capacity rollback. B2B setup is explicitly a persisted source/receipt fixture; actual round-start boundary remains P15.

Initial checks caught a schema reference typo and existing LocationSnapshot name collision; fixed by adding LocationExecutionSnapshot. Two test-fixture mistakes (missing attacker capability and an unfinalized command identity) were repaired; final checks above passed. Nominatim live service remains unavailable. Event transport is P25; P11 uses durable internal replan intent, not a claimed published event.

## Checkpoint B (before final correction/failure hardening)

Explicitly extracted the real Protomaps 2026-09-22 planet into `.local/maps/cairo.pmtiles`, bbox `31,29.8,31.65,30.3`, zooms 0–15: **45,260,921 bytes**, SHA256 `e3b0dde005597c52421088f2bb5aa4c13e8eca4a3756b714848935bc4985f10c`. Command: `.local/map-tools/pmtiles.exe extract https://build.protomaps.com/20260922.pmtiles .local/maps/cairo.pmtiles --bbox=31.0,29.8,31.65,30.3 --maxzoom=15 --download-threads=4`; `.local/map-tools/pmtiles.exe verify .local/maps/cairo.pmtiles` passed. CLI 1.31.2. This is explicit asset preparation, not an Engine/map database import or ordinary app setup.

Pinned MapLibre 6.10.0, pmtiles 4.5.0, Protomaps style 5.7.2 and RTL plugin 0.4.0. `node scripts/maps-assets.mjs` prepared 261 supporting font/sprite/license files from pinned asset commit `028c18f713baecad011301ff7a69acc39bcc2ae7`; style uses local URLs and Arabic names. Fastify range serving plus Nginx serving example, manifest/coverage, shared picker, B2C list-to-review and company focused list/detail are connected. Confirmation never overwrites source input. Added source-scoped `location.pinConfirmed` durable event intent; actual delivery remains P25.

`npm run test:browser:locations` — **1 Chromium scenario passed (27.6s)** with actual local PMTiles ranges/glyphs/sprites/map rendering and API/PostgreSQL. Verified candidate confirmation/reload, keyboard center selection, desktop zoom, emulated touch, empty/failed search, failed tiles with retained original/current pin/manual confirmation, unrelated unresolved task stays independently usable, HTTP 206/header magic and 416 invalid range, zero external runtime requests, 360px no horizontal overflow. Authenticated accounts and Nominatim results are explicitly harness fixtures; live Nominatim and physical devices are not claimed. Captures: `output/playwright/phase-11-map-mobile.png`, `phase-11-map-desktop.png`, `phase-11-tile-failure.png`.

Initial browser attempts exposed a harness Secure-cookie omission, an early keyboard action before map load, and MapLibre 6 worker packaging. Corrected the fixture cookie, waited for real readiness and supplied a Vite-bundled worker URL. The actual render review caught a flex header squeezing Arabic text; fixed. Production build passed with map code split; worker production rendering is checked again at C. A generated invalid-example fixture lacked its expected keyword; corrected to `maximum`. No owner/device approval claimed.

## Checkpoint C

`npm run test:locations` — **9 passed**, real isolated PostgreSQL with controlled provider fixtures. Added database-trigger fault after pin/replan writes to prove total rollback; real local HTTP provider timeout/malformed data/cache eviction; hidden same-tenant branch access and wrong assigned driver; actual B2B event recipient/payload checks. Public consumer conformance runs against actual confirmed output. Revision/source invalidation and stale/capacity rejection never overwrite the original address. Candidate markers now attach immediately after selection. Runtime notices and draft persistence preserve manual recovery without requesting location permission.

Production command: `$env:VITE_TAWSEL_API_BASE_URL='http://127.0.0.1:3011'; npm run build -w @tawsel/web; $env:TAWSEL_MAP_PRODUCTION_CHECK='1'; npm run test:browser:locations` — **1 passed (24.9s)** against production preview, real self-hosted basemap and bundled worker. MapLibre is lazily loaded; the renderer chunk remains about 1.06MB (284KB gzip), worker about 508KB; Vite emits a size warning, not a failed build. No mobile hardware performance claim.

Visual review: real Arabic road/place labels, locally shaped glyphs and POI sprites rendered at z13 and z15. Cairo RTL cards, source address, single navy confirmation, 44px zoom controls and numeric input stayed readable at 390/360px and 1366px. Failed tiles visibly retained the confirmed pin and manual controls. No measured accuracy, GPS, route polyline or commercial map dependency. Added complete OSM/ESA attribution and bundled license notices after reviewing upstream data/font/sprite licensing.

First full `npm run check`: audit/lint/contracts/typechecks passed; **346/347 tests passed**, one migration table-inventory assertion still omitted the three P11 tables. Updated that explicit inventory (migration file inventory already included 0008). No product failure hidden under the build. A one-off local edit command had a PowerShell/Node quoting syntax error and made no changes; corrected with a patch. Final rerun is recorded below.

## Final verification and handoff

- `npm run check` — **PASS: 17 files / 347 tests**, zero audit findings, lint, OpenAPI/reference drift, all typechecks, shared/API/web production builds and fixture isolation. Existing redirect-only callback OpenAPI warning remains; MapLibre lazy chunk size warning is documented above.
- `powershell -NoProfile -File scripts/maps-prepare.ps1` — **PASS**, exact tool/archive SHA256, archive structural verification, complete pinned support assets and license bundle. No Engine import or dataset modification.
- `npm run db:migrate` — **PASS**, applied additive `0008_locations.sql` to the marked local application database. Earlier migration files are unchanged; 0008 is now immutable.
- Windows Playwright process termination left five disposable browser databases. Each exact name was checked for the test marker, current owner, zero connections, exactly the two P11-named tasks/timestamps and no B2B tasks before removal. The browser harness now has explicit test-only teardown; no production route exposes it. Final browser rerun checks teardown and lost-response retry below.
- Final production-browser rerun after teardown/recovery assertions — **1 passed (20.1s)**. The server committed a confirmation, its response was deliberately lost, the page reloaded, and the exact saved action replay returned location revision **1**, not a second update. Follow-up `npm run lint` and `npm run typecheck` passed. Database inventory afterward contained only `tawsel_test_control`, with zero leftover disposable databases. `git diff --check` and protected Engine/Stitch/earlier-migration diff checks passed.

Versions added: MapLibre GL 6.10.0, pmtiles JS 4.5.0, Protomaps style 5.7.2, mapbox-gl-rtl-text 0.4.0; asset commit and CLI 1.31.2 pinned above. Supported checks use Node 24.19.0/npm 11.1.0. Existing Fastify 5.12.5, PostgreSQL 18.4, pg 8.23.0, TypeScript 6.0.2, Vitest 5.0.1 and Playwright 1.63.0 retained. Some ignored one-off document/schema helpers used shell Node 25.2.1; authoritative tests/build/setup use Node 24.

Changed paths: `apps/api/src/locations/{assets,geocoder,routes,service}.ts`, API registration and P09/P10 readiness/reservation projections; `db/migrations/0008_locations.sql`; `apps/web/src/components/location-picker.tsx`, `location-review.tsx`, account/task links, style and Vite worker/proxy config; pinned workspace packages; canonical location/common/OpenAPI/catalog/examples and generated client/reference; public location client/conformance; API/browser tests and isolated demo; `scripts/maps-{prepare.ps1,assets.mjs}`, `maps/{manifest.json,nginx.conf}`, license notices and captured browser images; ERP planning/mapping/quickstart, UI/action/consistency/operation guides, current status/index and phase evidence. Original Engine/OSRM/VROOM data/config/mounts and Stitch exports are unchanged.

Phase 12 may rely on source-preserving confirmed named coordinates, explicit location/source/planning-input revisions, durable internal replan and source event intent, scoped human APIs, generated LocationExecutionSnapshot/ConfirmCommand/Event types, real Cairo archive/style/glyph/sprite/RTL stack and reproducible demonstration in `docs/locations-and-maps.md`. P12 routing itself was **not executed**.

Remaining limits: live private Nominatim is unavailable (connection refused at localhost:8080); provider candidates/timeout/error evidence is explicitly controlled, not live geocoding. Map coverage is Greater Cairo, zoom data through 15 with overzoom to 18, not all Egypt. Candidate cache is process-local; restart/eviction or routing confirmation to another instance requires re-search/manual input (P39 must select deployment affinity/shared-cache policy). Actual planning publication is P13; actual departure/start races P15; signed event delivery P25; complete location/preparation integration P28. Physical Android/iPhone, Safari, production Nginx/TLS, owner review and independent real ERP connector were not performed. No commit, push, publication or next phase.

Final live-provider recovery check: `docker ps -a --filter name=nominatim-egypt` / `docker context show` could not run because Docker is not on PATH; the standard Docker Desktop executable path is absent. `wsl.exe --list --quiet` returned installation/usage help rather than a usable container environment. No existing imported Nominatim could be verified or safely resumed. No runtime installation, volume change or import was attempted. Effect: the production adapter can query a configured private service, but live candidate quality/coverage remains an explicit external verification gap; manual confirmation and real map rendering work locally.
