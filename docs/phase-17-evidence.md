# Phase 17 evidence — 24 September 2026

Scope: authoritative full/partial/refused/no-answer outcomes, exact reported collection, held return-required quantities and atomic progress/outbound intent. No P18 retry, branch receipt, correction, settlement or full execution UI.

Starting point: `62322be` (`phase 16`), clean working tree. No applicable AGENTS.md in the repository or its ancestors. Read the current phase, master-plan §§7–8/10/18, discovery decisions through D-112, decision map, assigned requirement rows, UI amount/refusal specifications, implementation status, and actual P05/P10/P15/P16 contracts, migrations and writers. Existing pins are retained. Recommended picker: `gpt-6-astra` / `xhigh`; the runtime identifies GPT-6 but does not expose a verifiable picker variant/effort. The model-selection guide was consulted; no picker change or exact setting is claimed.

Prerequisites before editing: `npm run db:local:start` used the existing dedicated PostgreSQL cluster on loopback 55432 (PostgreSQL 18 bin temporarily on PATH). `npm run test:integration -- apps/api/test/integration/command-transaction.test.ts apps/api/test/integration/b2b-intake-admission.test.ts apps/api/test/integration/start-assignment-race.test.ts apps/api/test/integration/current-activity.test.ts` passed **4 files / 69 tests**, 33.41 seconds. Actual isolated PostgreSQL, independent connections and commit boundaries; identity/provider fixtures remain labelled in those suites. Log: `.local/phase-17-prerequisites.log`.

## Checkpoint A — arithmetic and contract

Closed outcome request/result/collection/quantity definitions reference the existing canonical envelope, task revisions and action-time provenance. Frozen source allocations use BigInt intermediates and bounded integer minor units; no ERP quote or allocation invention. B2C has optional collection and no piece/shipping-liability workflow. No-answer has no reported collection and no unpaid-shipping exception.

Focused results before B: `npm run test:fast -- apps/api/test/fast/outcome-arithmetic.test.ts` passed **9 tests** (349 ms): 350/250/50/0 examples, conservation, permission, fractional/bounded quantities, under/overpayment, currency, exact multi-line prepaid zero allocations, previous shipping, partial remainder exclusion, safe-integer boundary/overflow and B2C restrictions. `npm run contracts:generate` validated **15 schemas / 142 valid / 96 invalid examples / 157 operations**. `npm run typecheck -w @tawsel/api` passed. `npm run test:contracts` passed **251 tests** (1.62 s). These are arithmetic/schema checks, not database acceptance. First typecheck caught newly generated component references mistakenly placed under an OpenAPI tag; moved them into `components.schemas` and regenerated before passing. No prerequisite repair needed. Persistence and public HTTP paths remain checkpoint B.

## Checkpoint B — atomic command and projection

Added migration `0014_delivery_outcomes.sql`; earlier SQL is unchanged. Preserved outcome/quantity/collection rows reference stable attempts, frozen source lines and command identities. Resolved attempts retain real heading/arrival evidence, including null for phone-only results. `effective_task_outcomes` is written in the kernel's progress stage. Driver/workday/assignment/task locks, owner device generation, current compare-and-set, source/assignment/pin revisions and immutable admission references gate acceptance; route revision alone does not. Current resolution, capacity release, planning generation, audit, receipt and source-filtered event intent share the command commit. No Engine calls occur there. The round remains active with no automatic next heading; physical origin is unchanged.

Planning excludes effective outcomes for both account types. Active admission does not count resolved personal tasks, and execution-pin correction cannot restore a completed company reservation. Actual custody remains held; no receipt/settlement claim. Own-round effective reads, action-status APIs and typed client are implemented. Requiring the current attempt (or explicit null) prevents silently abandoning another current customer. Another outcome requires fresh current revision; retries of the same action return the original result.

Focused results before C: `npm run test:integration -- apps/api/test/integration/outcome-progress-outbox.test.ts` passed **3 tests**, including API partial outcome after heading/arrival, phone-only no-answer and real transaction failures after domain/progress/audit/outbox/result writes. Every injected fault left no outcome, quantity, collection, projection, audit, command result or outbound intent; current/planning remained equal to their baseline. The four-suite regression command adding `current-activity.test.ts`, `planning-jobs.test.ts` and `database-lifecycle.test.ts` passed **31 tests** in 25.01 s. API typecheck passed. Canonical generation now covers **159 operations** including own outcome read/recovery. After tightening result schemas and adding external dispatch-cycle reference, the focused three tests were rerun successfully.

Development checks caught and fixed a test syntax typo, the stale expected migration inventory, and missing explicit array types in strict schema conditionals. The P13 upgrade fixture now constructs the historical P13 input explicitly instead of calling a current snapshot writer against a pre-outcome database; immutable forecasts and current fingerprint equality still pass. These were test/contract integration repairs, not missing runtime prerequisites. Independent duplicate races and the portable two-task demonstration remain checkpoint C.

## Checkpoint C — connected evidence

`npm run test:outcomes` passed **14 tests** in 17.82 s against isolated PostgreSQL. Same-ID and different-ID races use two independent pools/PIDs and observe actual `pg_blocking_pids` blocking before releasing the first transaction. Exactly one effective outcome, delivered-piece count, shipping collection and source event survives. Five injected failure stages fully roll back; the failed action can be retried with its original ID. Tests cover same-address independent shipments, all four outcome types and explicit fee exception, malformed/forbidden quantities and exact underpayment rejection, multiple prepaid lines/zero allocations, simple B2C optional collection, exact aggregate overflow handling, stale device/task/current versions, harmless route-version compatibility, CSRF/route mismatch/revocation/cross-tenant reads, delayed provider results and pin-correction exclusion. Source event payloads are validated and rejected remainder remains held. A deferred FK now independently ties resolved attempts to accepted outcomes.

`npm run outcomes:demo` passed with real loopback HTTP and the public typed clients: readiness/start → heading → arrival → partial 250 EGP (2 delivered / 1 held) → no-answer on the second independent shipment (3 held / no collection or fee refusal). The successful partial response was discarded only after receiving it from the server; a fresh client queried/retried the original action and obtained the same result. Physical origin stayed at the first recorded arrival. Two source-filtered schema-valid pending intents were observed. The demo is also a test in the 14-test suite. Public-only `npm run test:erp:outcomes` passed canonical assertions/negative mutations, and `npm run test:erp:outcomes -- .local/phase-17-demo.json` passed against the actual demo report. Database and HTTP are real; identity/bootstrap is explicitly a fixture and planning is genuinely manual. No provider mock is substituted for this demonstration.

Contract/client/read/recovery/event references are updated: **15 schemas, 148 valid / 98 invalid examples, 159 operations**. `npm run test:contracts` passed **259 tests** (1.69 s). `npm run contracts:lint` passed with the existing OIDC callback 302-only warning. `python -X utf8 scripts/check-ui-spec.py check` passed: original 18 hashes, 105 control dispositions, 64 mapped rows covering 159 operations, 33 designed Arabic states, 71 links, contrast and six negative checks. This is specification evidence, not new UI interaction evidence; no product component changed.

Initial expanded tests exposed three fixture errors: reading a nonexistent top-level planning input, sending role provisioning through the intake route, and waiting for a provider when manual publication had already consumed the pending job. Corrected the assertions/public provisioning route and explicitly requested replan before the delay. Typecheck also caught an overbroad test authenticator return type and redundant discriminant comparison; fixed before final checks. No failed check is counted as a pass.

## Final verification and limits

`$env:VITE_TAWSEL_API_BASE_URL='http://127.0.0.1:3001'` then `npm run check` passed: audit **0 vulnerabilities**, ESLint, Redocly lint, deterministic contracts check, all workspace/script types, **25 files / 549 tests** (104.53 s), shared/API/web production builds and production fixture isolation. `.local/phase-17-check.log` records the exact run. Existing warning: OIDC callback has only 302 responses; existing MapLibre chunk exceeds 500 kB. No new dependency/pin changes. Observed versions: Node **24.11.1**, npm **11.6.2**, PostgreSQL **18.6**; retained package versions pg **8.23.0**, Fastify **5.12.5**, TypeScript **6.0.2**, Vitest **5.0.1**, Playwright **1.63.0**.

Self-review found an illustrative event envelope still using old outer task/source IDs after its payload changed to P17. Aligned envelope/correlation/resource references and the designed correction example's superseded outcome. `npm run contracts:generate` and `npm run test:contracts` passed again (**259 tests**, 1.71 s); no runtime behavior changed. No independent owner/code review is claimed. `npm run db:migrate` applied **0014_delivery_outcomes.sql** to the configured local application database; no earlier migration was modified.

Final read-only inventory confirmed PostgreSQL **18.6**, zero disposable test databases, and zero personal/company tasks, rounds, attempts or outcomes in the application database; demo data stayed isolated. Final `npm run contracts:check`, public demo conformance, UI specification checks and `git -c core.whitespace=cr-at-eol diff --check` passed. `package-lock.json`, earlier migrations and original Stitch assets remain unchanged.

There is no offline capture/replay, retry/deferral, branch receipt, correction, native ERP receiver, signed sender, settlement or complete outcome UI in P17. Live Engine was unavailable in prior phase evidence; this phase does not require or newly verify it. No Engine import, dataset/mount/configuration change, original Stitch edit, dependency upgrade, commit, push or publication is authorized or performed. No new browser/device check was run because no UI component changed; full outcome UI is P29–30. The HTTP demo's authentication fixture is not production OIDC/physical-device evidence.

## Changed artifacts and handoff

- Runtime: `apps/api/src/outcomes/{models,arithmetic,service,routes}.ts`; `apps/api/src/app.ts`, `apps/api/src/planning/queue.ts`, `apps/api/src/rounds/departure.ts`, `apps/api/src/locations/service.ts`; `db/migrations/0014_delivery_outcomes.sql`.
- Evidence: `apps/api/test/fast/outcome-arithmetic.test.ts`, `apps/api/test/integration/outcome-progress-outbox.test.ts`, `apps/api/test/support/outcome-fixture.ts`; migration inventories/retained historical input fixture in `database-lifecycle.test.ts` and `planning-jobs.test.ts`; `scripts/outcome-demo.ts`, `tests/erp-conformance/outcomes.ts`, package scripts.
- Public boundary: `contracts/outcomes.schema.json`, `contracts/openapi.yaml`, `contracts/operations.json`, canonical examples/README; `packages/api-client/src/outcomes.ts`, generated `schema.d.ts`, client README; `scripts/contracts.mjs`, contract foundation tests and generated `docs/reference/public-contract.md` / `docs/contract-coverage.md`.
- Handoff: `docs/outcomes.md`, current guide, ERP planning/mapping/quickstart/README, UI action coverage/checker, implementation status, current phase README/coverage matrix, master-plan/discovery implementation summaries and this evidence file. Original decisions were not amended.

Phase 18 can rely on the verified schema/handlers/read/recovery, append-only outcome/quantity/collection rows and preserved attempt times, `effective_task_outcomes`, driver/current/owner checks, atomic plan/outbox intent, exact arithmetic and independent race/fault/demo evidence. P13's initial-attempt uniqueness remains; P18 must explicitly add retry identity and admission. Preserve prior attempts and fees, count the shipment once, include earlier accepted collection when calculating cumulative progress, and never retry a rejected partial remainder. P21 alone records physical receipt; P23 alone adds correction. Stop at P17.
