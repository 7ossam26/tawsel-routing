# Phase 34 — Ordered replay and durable conflict recovery

25 September 2026. Started at `57c16e8` (phase 33), clean `main`. No applicable AGENTS.md in the repository or ancestors. Runtime identifies Codex/GPT-6; exact picker suffix/reasoning setting is unavailable, so Astra/xhigh is recorded only as the requested recommendation. No commit, push, publication or Phase 35 execution.

## Prerequisites

Read master-plan sections 6 and 10–12, current discovery through D-112, phase guide/decision map, assigned requirement rows, implementation ledger, actual journal/handlers/fences/correction/closure/start/outbox APIs and schemas. Existing implementation artifacts, not status labels, supplied the starting point.

Commands use Node `C:\Users\jo\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe` (24.19.0).

- `node --env-file=.env.database.local node_modules/vitest/vitest.mjs run apps/api/test/integration/round-start-http.test.ts apps/api/test/integration/workday-carryover.test.ts apps/api/test/integration/device-ownership.test.ts apps/api/test/integration/partial-return-correction.test.ts apps/api/test/integration/outbox-sender.test.ts apps/web/test/fast/monitoring-client.test.tsx apps/web/test/fast/local-action-capture.test.ts --maxWorkers=2 --testTimeout=30000`: **7 files / 115 passed**, 322.35s. `.local/phase-34-prerequisites.log`.
- Separate client checks: **2 files / 15 passed**, 10.43s. Focused server prerequisite subset (same files; `-t 'P15 real HTTP|queued end waits|B: mandatory snapshot|P23 C: former-phone evidence is|B: lost HTTP acknowledgement'`): **5 passed / 95 skipped**, 28.13s. These repeat relevant checks; no additional coverage claim.

Real isolated PostgreSQL for transaction checks; existing labelled identity/provider fixtures and simulated IndexedDB for client checks. No prerequisite defect found at this point.

## Checkpoint A — passed before client replay work

Per-action batch dispatch will reuse the existing services/transactions. Dependency validation runs under the existing driver fence; missing predecessors remain retryable, received incompatible evidence remains durable. Additive migration 0026 retains causal/version metadata atomically without rewriting envelopes or hashes.

Implemented `sync.submitActions`, paged own-driver `sync.listConflicts`, typed public client and canonical batch/status schema. Each entry invokes the original feature service and has its own commit/result. The shared owner fence validates committed dependency results and binds projected activity revisions. Missing history does not finalize an identity. Old-owner commands retain evidence before dependency checks. Explicit outcome adoption can validate a received former-phone movement chain without applying its movements or inventing an arrival; an earlier failed outcome still needs acceptance/adoption.

`node --env-file=.env.database.local node_modules/vitest/vitest.mjs run --project integration apps/api/test/integration/offline-device-takeover.test.ts --maxWorkers=1 --testTimeout=30000`: **5/5 passed**, 16.58s (`.local/phase-34-checkpoint-a.log`). Real isolated PostgreSQL, independent connections/observed blocking, HTTP injection with labelled principal/CSRF fixtures. Covers out-of-order waiting, per-entry partial success, same-ID result recovery/payload conflict, failed dependencies, route-only compatibility, stale-device receipt, and explicit once-only adoption with unchanged original receipt/no physical-origin mutation. API TypeScript passed after rebuilding generated client declarations. An initial typecheck ran before that rebuild and failed on absent Sync types; corrected, not counted as passing. No unresolved prerequisite.

## Checkpoint B — passed before recovery UI integration

`ReplayCoordinator` reads the real journal, verifies fresh same-account context, schedules only after predecessor receipts have been durably saved, retries original bytes, independently acknowledges returned entries, retains omitted/not-received entries and refreshes confirmed state only when the unsent journal is drained. Browser Web Locks coordinate the installation/account. Unsupported lock APIs stop coordinated replay instead of guessing ownership. The browser releases locks on tab/process death; no clock-based lease or forced takeover.

`node --env-file=.env.database.local node_modules/vitest/vitest.mjs run apps/api/test/integration/offline-device-takeover.test.ts apps/web/test/fast/local-action-capture.test.ts --maxWorkers=2 --testTimeout=30000`: **2 files / 18 passed**, 24.69s (`.local/phase-34-checkpoint-b.log`). New tests connect actual Dexie journal/coordinator/public HTTP client/Fastify/PostgreSQL. Lost first HTTP response commits only the first action; reopen/retry recovers the same identity before releasing arrival/outcome. Ack write failure and incomplete response leave all unsent descendants; concurrent coordinators produce one effect. Wrong/expired fresh authentication sends zero commands. IndexedDB and the test lock are simulated; actual Web Locks/reconnect/process-reopen evidence remains checkpoint C. Initial mismatch in an Arabic substring assertion was fixed; the first run was 7 passed / 1 failed. NodeNext import/type differences found by the combined test typecheck were corrected using Dexie's named export and a correctly typed fault seam.

Primary references consulted: [Web Locks](https://developer.mozilla.org/en-US/docs/Web/API/Web_Locks_API), [Dexie transactions](https://dexie.org/docs/Dexie/Dexie.transaction()). Existing dependency versions/pins retained. No unresolved prerequisite.

## Checkpoint C — passed before final verification/handoff

Production lifecycle replay now runs on reconnect, reopen, foreground and manual retry. `/sync` reads authorized paged evidence and offers explicit current-owner outcome adoption with a fresh snapshot. Original receipts remain separate from adopted outcomes. Same-journal locking covers new capture, replay, and the full synchronized-start barrier through fresh plan/readiness/start. A pending day-end cannot grant an offline or second-phone start. Legacy session pointers are reconciled only against saved IndexedDB receipts.

`node --env-file=.env.database.local node_modules/vitest/vitest.mjs run apps/api/test/integration/offline-device-takeover.test.ts apps/web/test/fast/preparation-flow.test.tsx --maxWorkers=2 --testTimeout=30000`: **2 files / 19 passed**, 37.47s (`.local/phase-34-checkpoint-c.log`). The connected test now covers actual journal → public client → HTTP → real PostgreSQL, route-reordered outcome acceptance, lost day-end response blocking the start callback, fresh readiness/new-day acceptance after same-ID retry, second-phone active-round denial, and real subset receipt/closed-day evidence-adoption refusal with unchanged receipts/history. Preparation tests use a labelled component-only lock shim; they do not establish browser coordination. No unresolved checkpoint dependency.

First actual production-PWA replay browser run after starting existing Keycloak: `node scripts/offline-demo.mjs --replay`: **2/2 passed**, 39.2s (`.local/phase-34-browser.log`). Separate scenarios prove full Chromium process reopen after a lost committed response and native Web Locks with two tabs plus a second storage-isolated phone context. Local Keycloak/Fastify/PostgreSQL are real; OSRM/VROOM and intentional network faults are labelled fixtures. The first attempt failed because Keycloak was stopped (`ECONNREFUSED :8085`); starting the existing installation fixed that prerequisite without reimporting realms.

## Review, failures and repairs

- First broader fast check: **566 passed / 9 failed, 19 files / 575 tests**, `.local/phase-34-fast.log`. Missing invalid-example keyword labels, operation lifecycle allowlists/generated drift, and two component tests without a Web Locks fixture caused failures. Fixed the contracts/allowlists and explicitly labelled the component-only lock shim. Focused rerun **2 files / 437 passed**, 10.67s, `.local/phase-34-focused-fast.log`.
- Real database regression: **113 passed / 2 failed, 5 files / 115 tests**, 264.77s, `.local/phase-34-database-regression.log`. Upgrade harnesses execute retained commands on historical schemas before new migrations; unconditional metadata writes referenced an absent table. The kernel now detects that historical absence; production still requires the complete migration set. The first upgrade rerun then exposed expected migration suffix lists missing 0026 (`.local/phase-34-upgrade-regression.log`). Updated the five existing upgrade lists. Rerun `node --env-file=.env.database.local node_modules/vitest/vitest.mjs run --project integration apps/api/test/integration/offline-device-takeover.test.ts apps/api/test/integration/database-lifecycle.test.ts apps/api/test/integration/device-ownership.test.ts -t 'C:|upgrade' --maxWorkers=2 --testTimeout=30000`: **3 files / 11 passed / 20 skipped**, 40.72s, `.local/phase-34-boundaries.log`.
- `python scripts/check-ui-spec.py` initially refused the newly verified P34 operation lifecycle because its explicit allowlist ended at P33. Added only the implemented sync operations and P34 local UI. Rerun passed A/B/C, 89 local links, contrast and negative controls (`.local/phase-34-ui-spec.log`). These static checks do not prove usability.
- Inspected actual 390px RTL captures: pending/retry status stays separate from confirmed 0/2 progress; recovered 1/2 progress leaves the next stop unstarted; evidence shows task, result and reported amount with one dominant adoption action; reload shows adopted outcome separately from retained supporting movements. No horizontal overflow in the tested recovery view. An English injected network error visible in the initial retry capture prompted Arabic network/timeout fallback copy before the final browser rerun. No human owner or physical-device review is asserted.
- Existing P33 browser assertions assumed no automatic replay. They now explicitly block only the replay endpoint while verifying unsent-monitor exclusion and exit guarding, then release the network fault and assert all three durable acknowledgements. Historical Phase 33 evidence is preserved.

## Changed artifacts and version boundary

- API/kernel: `apps/api/src/sync/{routes,service}.ts`, `commands/{kernel,dependencies}.ts`, `devices/{fence,service}.ts`, `corrections/{service,evidence-dependencies}.ts`, closure/current/outcome/eligibility/return adapters and app registration. Migration `db/migrations/0026_replay_dependencies.sql`; existing upgrade expectation lists.
- Browser: `replay.ts`, `journal-lock.ts`, `replay-runtime.ts`, `replay-lifecycle.tsx`, `start-barrier.ts`, `execution-pointers.ts`, `conflicts-page.tsx`, existing capture/execution/current/preparation/local-status/shell and concise status styling.
- Verification: `offline-device-takeover.test.ts`, journal/preparation tests, `playwright.replay.config.ts`, `tests/replay-browser/replay.spec.ts`, `scripts/offline-demo.mjs`, labelled fixture role capability setup, updated P33 browser regression and `tests/erp-conformance/sync.ts`.
- Canonical contract: `sync.schema.json`, device evidence/read constraints, OpenAPI, 4 valid/4 invalid examples, operation lifecycle map; typed `SyncClient` and regenerated schema/reference/coverage. No added npm dependency or changed package version. Inventory: **28 schemas / 258 valid / 159 invalid examples / 185 operations**.
- Docs: replay runbook/demo/Phase 35 handoff, implementation ledger, phase guide/coverage, local-capture/device/start/consistency references, ERP planning/mapping/consumer quickstart, UI action route and explicit static-operation checks.

Supported runtime used: Node **24.19.0**, npm **11.6.2**, PostgreSQL **18.4**, Chromium **153.0.8010.12** / Playwright **1.63.0**; existing Keycloak **26.7.4** with Temurin **25**. Existing pins retained: Fastify **5.12.5**, React **19.3.0**, Vite **8.3.0**, Dexie **4.4.6**, Workbox **7.4.1**, fake-indexeddb **6.2.5**, TypeScript **6.0.2**, Vitest **5.0.1**. A package-version probe through `fake-indexeddb/package.json` was blocked by its exports; the installed package file supplies the pin. No dependency upgrade, map import, Engine mount/dataset change or original Stitch edit.

## Remaining limits and exact handoff

See [reproduction commands and exact Phase 35 artifacts](ordered-replay.md#exact-phase-35-handoff). Full auth-expiry/same-account recovery UX, safe logout/switch across tabs and pending-aware service-worker/application upgrades belong to Phase 35 and were not executed. Physical Android/iOS/installed-home-screen behavior, power-loss/eviction/storage-clear recovery, actual 24-hour observation, live Engine, commercial ERP, production TLS/deployment/load SLO and human owner approval remain unrun. Browser contexts simulate phones; dropped replies and provider responses are controlled fixtures. No background-sync, browser eviction or phone-loss guarantee. No commit, push, publication or next-phase execution.

## Final verification

All commands below use the supported Node directory first on PATH. npm was invoked explicitly as `node 'C:\Program Files\nodejs\node_modules\npm\bin\npm-cli.js' ...`; the commands in the table use `npm` as shorthand.

| Command | Actual result |
| --- | --- |
| `npm run check` | Audit **0 vulnerabilities**, lint, canonical/generated checks and all workspace/script types passed. Contract lint passed with the pre-existing OIDC callback 302-only warning. Broad Vitest run: **51 files / 972 tests, 969 passed / 3 failed**, 723.47s; `.local/phase-34-final-check.log`. The chain stopped before build. |
| `python scripts/check-ui-spec.py` | Passed metadata/hash/control map, 185 operation mapping, state cases, links, contrast and negative checks. `.local/phase-34-ui-spec.log`. |
| `node --import tsx tests/erp-conformance/sync.ts` | Passed canonical per-entry/waiting/receipt semantics; example-only, no live ERP claim. |
| `node --env-file=.env.database.local node_modules/vitest/vitest.mjs run apps/api/test/integration/offline-device-takeover.test.ts apps/api/test/integration/database-lifecycle.test.ts apps/api/test/integration/outbox-sender.test.ts apps/api/test/integration/retry-deferral-urgency.test.ts --maxWorkers=2 --testTimeout=30000` | **4 files / 47 passed**, 141.95s. All three previously failing files plus final mixed accepted/review batch, duplicates and replay cases. `.local/phase-34-final-regression.log`. |
| `npm run lint`, `npm run typecheck`, `npm run test:fast` | Passed; **19 files / 577 fast tests**, 23.95s. Includes final pointer reconciliation and Arabic failure-copy changes. `.local/phase-34-final-validation.log`. |
| `npm run build` | Shared/client/API builds passed; web configuration refused missing `VITE_TAWSEL_API_BASE_URL`, so the root chain stopped. The replay helper below supplies the explicit local fixture URL and builds/checks the production PWA; no production URL was invented. |
| `npm run build -w @tawsel/mock-erp` | Passed TypeScript and production native UI build; `.local/phase-34-final-mock-build.log`. |
| `node scripts/offline-demo.mjs --replay` | Final **2/2 browser scenarios passed**, 36.6s (14.6s reconnect/process-reopen; 14.1s two-tab/two-context takeover/adoption). Fresh production web build, Workbox **16 assets / 2,381,077 bytes**, production-fixture isolation passed. `.local/phase-34-final-browser.log`. |
| `node --import tsx tests/erp-conformance/sync.ts .local/phase-34-reconnect-evidence.json .local/phase-34-two-device-evidence.json` | Passed actual public receipt/original-envelope/adopted-outcome checks for both scenarios. `.local/phase-34-consumer-conformance.log`. |
| `node scripts/offline-demo.mjs` | Updated capture regression **1/1 passed**, 20.1s (12.0s test), including actual IndexedDB abort, offline process reopen, unsent monitoring exclusion/exit guard with explicit blocked replay, then three durable acknowledgements after releasing the fault. `.local/phase-34-capture-browser.log`. |

The broad run's three failures were a fresh table inventory missing `command_replay_metadata`, an older outbox migration fixture that accidentally included 0026 while excluding 0024/0025, and the existing real HTTP eligibility demo exceeding its 10-second limit (14.157s under four workers). Corrected the inventory, bounded the historical fixture at `<0024`, and gave that complete HTTP demo a 30-second test bound consistent with other connected demos. Assertions and application validation were retained. Final focused results below cover every failing file plus replay and the last UI changes; the entire 972-test suite was not repeated after these repairs.

Actual reviewed captures: [pending retry](../output/playwright/phase-34-retry-mobile.png), [confirmed recovery](../output/playwright/phase-34-recovered-mobile.png), [explicit review](../output/playwright/phase-34-evidence-review.png), [retained evidence after adoption](../output/playwright/phase-34-adopted-evidence.png). Historical Phase 33 screenshots are not overwritten; its updated capture-regression run writes `.local/phase-34-capture-*.png` and `.local/phase-34-capture-regression.json`.

Final retry capture was inspected again: Arabic network/timeout copy now replaces the injected English error, pending count remains three, confirmed progress remains 0/2 and further downloaded work stays distinct. Builds retain the existing large MapLibre chunk advisory; this task makes no load/performance claim. Keycloak was started from the existing installation for browser checks and remains available locally; disposable test APIs/databases were closed by their harnesses. `git diff --check` passed.
