# Phase 41 — device and owner pilot evidence

26 September 2026. Starting clean HEAD `414987cb8fd493d89aef0b8fe56b91348856f35d` (Phase 40). No applicable AGENTS.md found in repository/ancestors. Runtime identifies Codex/GPT-6; exact picker suffix/effort is unavailable. Requested Astra/high is a recommendation, not an observed switch; model-selection guide consulted. Only Phase 41; no publication, commit, push or Phase 42 execution.

## Prerequisites

Read current plan acceptance A–P, discovery amendments through D-112, phase index/decision/requirement/model maps, status, DESIGN, UI specification/review/reference audit and P27–40 evidence. Inspected real identity, intake/location/planning, driver/branch, native mock, offline/recovery, report/export, deployment/restore code and tests. Opened all nine original reference images; metadata/HTML and extension action inventory remain authoritative for design only. No Engine import/configuration/dataset/mount or original Stitch edit.

Initial `npm run test:deployment` failed with local PostgreSQL stopped and the host Node 25.2.1. Started the existing managed database and issuer; selected Node 24.19.0 explicitly. Corrective prerequisite run: **7 files / 29 passed**, 90.53s, `.local/phase-41-prerequisites-rerun.log`:

```powershell
$env:PATH='C:\Users\jo\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin;'+$env:PATH
node --env-file=.env.database.local node_modules/vitest/vitest.mjs run apps/api/test/fast/recovery-store.test.ts apps/api/test/fast/deployment-config.test.ts apps/api/test/integration/deployment-recovery.test.ts apps/api/test/integration/offline-session-update.test.ts apps/api/test/integration/report-export.test.ts apps/mock-erp/test/integration/mock-source-outbox.test.ts apps/web/test/fast/preparation-flow.test.tsx --maxWorkers=1 --testTimeout=30000
```

Read-only `node --import tsx scripts/engine-demo.ts --live`: **failed**, nine route/table/optimization probes unavailable, Docker inventory unavailable. No live Engine acceptance. P40 physical restore measurement is retained historical evidence, not rerun here; off-host recovery remains unverified.

## Checkpoint A — reproducible walkthrough

Added isolated personal pilot harness, continuous browser journey and [owner-run steps/device/feedback sheets](verification/pilot-walkthrough.md). Fresh synthetic issuer account, real PostgreSQL and application sessions; tasks/pins/execution created through actual UI. Default routing is explicitly controlled HTTP; `TAWSEL_PILOT_ENGINE=live` uses configured adapters with no fixture fallback. Loopback-only credentials/control routes stay outside production. Cleanup removes only harness-created resources, including on setup failure; teardown acknowledgement follows completed cleanup. Owner explicitly chose local preparation/verification now and devices/owner review later; no usability feedback received.

`node node_modules/@playwright/test/cli.js test -c playwright.pilot.config.ts`: **1/1 passed**, 49.1s total, actual Chromium **153.0.8010.12**, Node **24.19.0**, Windows. Two UI-created persisted tasks at the same address remain distinct; both pins selected on actual Cairo basemap; real issuer login, planning worker, explicit start/heading/arrival/outcome/day closure and XLSX download. Workbook inspection checks both recipient/outcome rows and `=1+1` as string, not formula. Captures/result in `output/playwright/phase-41/`. Long name, keyboard outcome action/44px target and 320/360/390/1366 CSS width checks passed. This is a desktop dev-shell run with controlled routing, not production PWA/device/registration/recovery-email proof.

First browser attempt failed because the new test tried to focus a still-disabled busy button immediately after arrival. Wait for enabled state added; unchanged application behavior then passed. Subsequent teardown review added explicit cleanup acknowledgement; final rerun recorded below.

`playwright.source.config.ts`: first rerun exercised native actions but failed on obsolete P27 label `أحداث توصيل الصادرة`, replaced by P32's separate delivery/application counters. Small prerequisite test repair now verifies current delivery counters, explanation and pending application state while retaining the real public projection checkpoint assertion. Corrected **1/1 passed**, 1.1m. The native source browser uses actual Keycloak/HTTP/separate PostgreSQL/mock workers; driver execution setup uses public APIs. No commercial ERP or physical custody claim. Historical tracked captures restored byte-for-byte after copying new evidence into P41's scenario directories.

The first exception run failed on a real regression: P33's shared accepted-outcome path labelled every non-no-answer outcome as delivery. `apps/web/src/current-activity.tsx` now limits delivery wording to full delivery; partial/refusal use outcome wording. Added a connected UI test submitting refusal and asserting no delivered claim. `node node_modules/vitest/vitest.mjs run --project fast apps/web/test/fast/driver-flow.test.tsx`: **18/18 passed**, 12.63s. This is a fetch/jsdom fixture, separately from the real PostgreSQL/browser rerun. No API, money rule or business transition changed.

The next exception run reached phone refusal and found a second real regression: the journal required arrival for refusal despite the existing public server rule and offered UI action. `local-work.ts` now accepts a permitted refusal without synthesizing movement, while rejecting a different current stop and disallowed outcomes. Local durability/reopen and negative no-write checks added. `node node_modules/vitest/vitest.mjs run --project fast apps/web/test/fast/local-action-capture.test.ts apps/web/test/fast/driver-flow.test.tsx`: **34/34 passed**, 7.23s. These are IndexedDB/fetch fixtures; real browser/DB refusal results are recorded separately. Other outcome stage gates remain unchanged.

Independent real PostgreSQL regression: `node --env-file=.env.database.local node_modules/vitest/vitest.mjs run apps/api/test/integration/partial-return-correction.test.ts apps/api/test/integration/offline-device-takeover.test.ts apps/api/test/integration/reporting-query.test.ts apps/api/test/integration/diagnostics.test.ts --maxWorkers=1 --testTimeout=30000`: **4 files / 83 passed**, 385.54s. No full integration-suite rerun is claimed.

Third exception attempt reached retry but hit the real 120/minute session-context rate limit (429, explicit “wait one minute” copy). It is a failed run, not business rejection. Added opt-in `TAWSEL_BROWSER_SLOW_MO` to that Playwright config; paced rerun uses 500ms. Production limits remain unchanged. Read amplification under very rapid navigation/shared-IP use is a pilot condition to measure, not a reason to silently disable authentication controls.

The 500ms-paced attempt also hit 429; `TAWSEL_BROWSER_SLOW_MO=1500 node node_modules/@playwright/test/cli.js test -c playwright.exceptions.config.ts` then **passed 1/1** (168.3s including setup/teardown). Complete partial/refusal/explicit unpaid fee/correction, downstream receipt/day denial, retry, capacity and future-time cases ran against actual PostgreSQL/Keycloak. New captures are in `output/playwright/phase-41/exceptions/`; public result is retained privately at `.local/phase-30-browser-evidence.json`. No rate-limit bypass or accepted fake response.

Branch/closure initial run reached accepted day closure but failed an obsolete manual-recheck expectation. The Playwright trace shows the closed-day summary after automatic reconnect replay reconciled the original command. Updated the test to require either the original verification button or already-confirmed closure, then require closed state. The next unpaced attempt failed with actual session-context HTTP 429 at the old-phone refresh. Opt-in 1500ms pacing is also available in the branch config; no server limits changed.

The paced branch attempt exposed the same stale expectation one navigation earlier: replay resolved closure before the preparation page could display its pending gate. The browser scenario now deliberately blocks the replay HTTP route while observing that gate, then restores the network and requires confirmed closure. Actual closure still commits on PostgreSQL before its response is dropped; no accepted result is synthesized.

A later 1500ms branch attempt still exceeded 120 context reads in one IP/minute bucket across two contexts, failing before branch continuation. Trace inspection confirms HTTP 429 and bursts around navigation/replay/takeover, not a business-rule refusal. Testing 3000ms pacing retains the production limit; shared-IP navigation capacity remains an explicit pilot condition.

Final branch command: `TAWSEL_BROWSER_SLOW_MO=3000 node node_modules/@playwright/test/cli.js test -c playwright.branch-closure.config.ts`: **1/1 passed**, 278.6s total. Actual native subset receipt (2 of 3), retained remainder, explicit takeover, original lost-response reconciliation, round/day closure and next-day continuation passed. Console duplicate React-key warnings in retained-work review remain a focused checkpoint C finding. Checkpoint A local walkthrough is reproducible; owner, physical devices and live Engine remain unavailable.

## Checkpoint B — local production PWA / deferred devices

Physical Android/Chrome and iPhone/Safari were explicitly deferred. Actual approximately 24 elapsed hours is **not started**. Starting production-PWA recovery/replay reruns on desktop Chromium; no OS installation or physical-storage claim.

`node scripts/offline-demo.mjs --recovery`: **2/2 passed**, browser 38.5s (70.5s including production build). Actual Workbox installation/process reopen, native IndexedDB migration abort/recovery, pending logout/update gates, expired/missing-cookie same-account Keycloak recovery, explicit update and cached account isolation. Separate empty browser context demonstrates only server-received work is recoverable.

`node scripts/offline-demo.mjs --replay`: **2/2 passed**, browser 34.6s (43.5s including build). Original committed-response loss survives browser-process reopen, original action identity resolves before descendants, two tabs share native Web Locks, logical second phone explicitly takes over and reviews/adopts eligible former-phone evidence. Controlled routing and network/session faults are labelled. [Timestamped commands](../output/playwright/phase-41/checkpoint-b.json); fresh captures under `recovery/` and `replay/`. Existing fast storage quota/abort, no-write/start guards and retained old timestamp fixtures passed within the 608-test run. Simulated quota/old timestamps do not prove physical pressure or elapsed retention. Both physical platforms, OS home-screen installation and actual elapsed day remain unrun. Checkpoint B local results recorded before C.

## Checkpoint C — focused usability corrections

Observed preparation: duplicate dominant save/start actions, internal input revision and stale “planning works” notice beside a ready plan. Code inspection also found blank coordinate text coerced to numeric zero. Branch run console shows the same pending closure ID rendered from both the journal and legacy pointer; combine those representations into one recovery link while keeping closure blocked. These are implementer findings; no owner feedback is invented.

Focused C Vitest: `node node_modules/vitest/vitest.mjs run --project fast apps/web/test/fast/preparation-flow.test.tsx apps/web/test/fast/branch-closure-flow.test.tsx`: **2 files / 29 passed**, 6.30s. Missing origin/fixed endpoint sends no planning/start request; explicit zero is preserved; journal plus pointer yields one recoverable link and still no closure write. UI boundary is fetch/jsdom/fake-indexeddb, not a transaction claim.

`node node_modules/@playwright/test/cli.js test -c playwright.preparation.config.ts`: initial attempt failed on old P28 stale-start copy, superseded by the replay-aware start guard. Corrected test checks the current refresh instruction, **zero created rounds** after refusal and the actual refreshed public input revision before start. Final **1/1 passed**, 39.4s: real PostgreSQL/Keycloak, controlled HTTP Engine outage/manual fallback. `playwright.monitoring.config.ts`: **1/1 passed**, 31.3s, authorized monitoring/reconnect with real committed change. [Timestamped C runs](../output/playwright/phase-41/checkpoint-c.json). Historical captures preserved separately.

## Final verification and handoff

- `node node_modules/vitest/vitest.mjs run --project fast`: **22 files / 612 passed**, 34.75s; `.local/phase-41-fast-final.log`.
- `node .local/runtime/node_modules/npm/bin/npm-cli.js run typecheck`: **passed** all workspace and script checks; `.local/phase-41-types-final.log`. Direct `tsc -p tsconfig.scripts.json` also passed.
- `node node_modules/eslint/bin/eslint.js .`: **passed**, `.local/phase-41-lint-final.log`.
- `node scripts/contracts.mjs check`: **passed**, 31 schemas, 268 valid / 167 invalid examples, 187 operations/events/local actions. Canonical generation adds only P41 evidence narrative. Public schemas, examples, client, ERP planning/mapping/quickstart and conformance interfaces need no behavior/version change.

Logs/traces, synthetic credentials and raw service evidence remain in ignored local paths. Public screenshots and timestamped manifests are in `output/playwright/phase-41/`. Failed run manifests are historical attempts; the phase ledger identifies the final corrected commands. No full integration-suite, live Engine, target deploy, separate restore, physical device, elapsed day or owner acceptance claim.

Final `node node_modules/@playwright/test/cli.js test -c playwright.pilot.config.ts`: **1/1 passed**, 45.7s. Includes completed cleanup acknowledgement, blank-origin feedback, one enabled primary Start action, updated ready capture, exact exported task IDs, `12550` EGP minor units/exponent 2 and formula-looking recipient as a string. Public final result is `output/playwright/phase-41/b2c.json`; screenshot `ready.png` was opened and checked after the fix. Total final browser scenarios across pilot/source/exceptions/branch/recovery/replay/preparation/monitoring: **10 passed**. Earlier failed attempts remain documented; the paced branch pass precedes the duplicate-link fix, whose focused component regression and final closure journey passed afterward.

Actual runtime: Node 24.19.0, Chromium 153.0.8010.12 / Playwright 1.63.0, Keycloak 26.7.4, Vitest 5.0.1, PostgreSQL server/binary 18.4. Supported bundled Node was explicitly selected. Existing dependency pins retained; no package upgrade or live-service version claim.

Changed code: `apps/web/src/{current-activity,local-work,preparation-flow,closure-page}`, their four owning fast test files; `scripts/pilot-browser-server.ts`, `playwright.pilot.config.ts`, `tests/pilot-browser/pilot.spec.ts`; paced exception/branch configs and source/branch/preparation browser expectations; `scripts/inspect-report-export.ts`; npm commands and ignored trace folders. Documentation: implementation status, phase index/coverage, UI review, offline capture, this evidence, readiness/walkthrough and generated operation-coverage narrative.

Phase 42 may rely on those exact verified artifacts and prior canonical schemas/client, keeping all readiness gaps visible. It is not executed here. No commit, push, publication, deployment, Engine changes or original Stitch edits.

Final workspace build: initial `node .local/runtime/node_modules/npm/bin/npm-cli.js run build` failed closed because `VITE_TAWSEL_API_BASE_URL` was unset. Corrective PowerShell command `$env:VITE_TAWSEL_API_BASE_URL='http://127.0.0.1:3041'; node .local/runtime/node_modules/npm/bin/npm-cli.js run build`: **passed** foundation, API, web/PWA fixture isolation and native mock builds. URL is the local rehearsal API, not a target deployment. Existing web bundle-size warning remains; no dependency upgrade. Logs: `.local/phase-41-build-missing-config.log`, `.local/phase-41-build-final.log`. Final `git diff --check` passed.

Cleanup verified: the new pilot credentials and stop marker are absent after completed teardown. The managed PostgreSQL and local Keycloak processes started for this run were stopped; the walkthrough explains restarting them. Final link audit found no broken local links in the three P41 documents. Tracked historical screenshots, Engine data/configuration, original Stitch exports, migrations and lockfile remain unchanged.
