# Phase 08 — ERP provisioning and verified service context

Date: 22 September 2026. Starting HEAD `f140e5dd38c11c75c75c64ac12cbcd8223e4d050` (`phase 7`), clean tree. No repository/ancestor AGENTS.md found. Read master plan (especially §5–6, §10, §12–13), current discovery/decision amendments through D-112, phase index, assigned requirement rows, implementation ledger, P02 schemas/catalog/generator, P05 kernel, P06 access guards/migration, P07 actual issuer/session setup and evidence, ERP handoff/planning/mapping and integration guide.

Runtime identifies GPT-6; exact picker model suffix and reasoning setting are not exposed. Requested recommendation: `gpt-6-astra / xhigh`; not represented as an observed or changed runtime setting.

Prerequisites before edits: `npm run test:contracts` **138 passed**; `npm run test:database` **28 passed**; `npm run test:authorization` **39 passed**; `npm run test:auth` **21 passed**. Real disposable PostgreSQL for database/access/auth; auth uses labelled signed HTTP issuer fixture. Node 24.19.0, npm 11.6.2 for these initial commands; subsequent checks use existing npm 11.1.0 shim. No prerequisite repair needed.

Design: explicitly permitted provisioning service operations; no human delegation is accepted. Audit actor is null and service is the authenticated integration (credential ID separately retained). Operator bootstrap reserves existing issuer subjects to a source. ERP creates/manages credentials at the issuer; Tawsel never accepts passwords or creates a credential store. User acceptance remains pending until a worker verifies the reserved subject at the configured issuer. Disable denies locally immediately and durably schedules issuer-session revocation. External requests never share the application command transaction.

## Checkpoint A — passed before B

Added closed canonical provisioning schemas, migration 0004 source/credential/subject-grant/mapping/reconciliation tables, scoped bearer credential verification under tenant locks, operator-only bootstrap, rotation/expiry/recovery and source disable, safe configuration discovery and canonical Problem failures. Selected explicitly permitted service operations; human assertions are rejected, actor null and service/credential/operator identity audited. Existing issuer subjects require an operator-reserved source grant; no identity can be claimed merely by naming it.

`node --env-file=.env.database.local node_modules/vitest/vitest.mjs run --project integration apps/api/test/integration/provisioning-actor.test.ts`: **2 tests passed**, real isolated PostgreSQL and actual Fastify handlers. Covers bootstrap retry/one audit, source identity, missing credential, forged actor, wrong integration, unprivileged bootstrap, expiry after rotation and source disable. `contracts:generate` passed seven canonical schema files; API typecheck first exposed three fixture typing errors (fixed before this test). Issuer external reconciliation is checkpoint B and no issuer-ready behavior is claimed at A. No unresolved prerequisite.

## Checkpoint B — passed before C

Implemented versioned branch/disable, role, user/disable/role/exception/branch, and minimal driver/vehicle-reference projections through the P05 kernel. Same entity revisions span all user commands. Source-scoped mappings, access updates, command result/audit and provisioning.changed outbox intent commit together. Rejected tentative account/reference writes roll back to kernel savepoint while retaining evidence. Role inheritance and explicit exceptions use unchanged P06 semantics.

Durable issuer worker claims with leases, performs configured Keycloak Admin HTTP outside database transactions, and fences completion by lease/generation. Existing subject verification is required before enabling its local binding. Disable commits local denial immediately and schedules idempotent issuer-session logout. Pending/running/retry/ready, attempts, next attempt and safe errors are queryable. ERP still owns issuer account/password creation and enabling; Tawsel never remotely enables issuer accounts.

Focused command: `node --env-file=.env.database.local node_modules/vitest/vitest.mjs run --project integration apps/api/test/integration/provisioning-actor.test.ts apps/api/test/integration/authorization-isolation.test.ts` **29 passed**. Seven P08 tests cover duplicate/concurrent calls, changed/stale revisions, one projection/event, unauthorized subject rollback, missing refs, duplicate driver ownership, issuer outage/retry/expired lease recovery, newer disable during held issuer HTTP, and provision → signed fixture login → role/exception changes → live membership denial. Extended isolation proves identical external IDs across companies and same-company integrations, wrong credentials/body scope and revoked capability denial. Issuer protocol/admin are explicitly HTTP fixtures; database transactions are real. No unresolved dependency before C.

## Checkpoint C — public consumer and actual issuer passed

Added standalone public HTTP client and persisted-command CLI fixture for a company, two branches, configurable role, explicit allow/deny and two drivers with minimal vehicle references. Operator setup reserves existing issuer subjects; fixture creation of those users happens at Keycloak through labelled local setup, not a Tawsel password API. Canonical OpenAPI/examples/types/reference/coverage, ERP planning/mapping and consumer quickstart updated together. Twelve command operations, two reads and provisioning.changed durable intent are verified locally; signed delivery remains P25.

Actual local setup: `node scripts/provisioning-local-setup.mjs`, `npm run db:migrate`, `npm run provisioning:bootstrap`, `npm run provisioning:demo`. Company P08LOCAL and two uniquely named local users were created. Actual issuer verification succeeded, each after one attempt. The API was started with the three private environment files and explicit TAWSEL_API_HOST/PORT; an identified old P07 compiled API on port 3001 was stopped and replaced, preserving the original Vite, Keycloak and Mailpit processes. No existing LOCAL/company/personal identities were reset.

`npm run test:browser:provisioning`: **1 passed**, final observed run **9.7 seconds** (test body 4.4 seconds). Chromium used actual Keycloak 26.7.4 and the application PostgreSQL database: new user.provision revision → visible pending → real worker ready → code/S256 login → authorized context with two branches/driver and execution.own/reports.read → role removal changes the existing session to reports.read only (deny/allow exceptions persist) → user.disable gives immediate 403 before worker → worker logout → actual Keycloak admin session list empty. Re-running restores the labelled driver's projection through a newer public revision, then disables it again. Driver-2 remains enabled. No shipment/monitoring endpoint is claimed; authorized read here is the real session/access context, with P06 resource isolation separately tested.

`npm run test:erp:provisioning`: **PASS** against the running HTTP API. Discovery, immutable replay, changed-ID payload conflict, forged actor/wrong integration, versioned branch disable/stale enable and scoped status passed. Also copied only public client/types/example/conformance source into `C:\Users\jo\AppData\Local\Temp\tawsel-p08-consumer-z67pow`, with scoped consumer config and no bootstrap/operator/database credentials. From that independent working directory: `node --import file:///C:/Users/jo/Desktop/tawsel-routing/node_modules/tsx/dist/loader.mjs tests/erp-conformance/provisioning.ts` **PASS**. The loader is the pinned TypeScript runtime; application/domain/database code was not copied or imported. Private copied config and journal paths stay untracked. This proves the provisioning boundary only, not P27's transactional ERP outbox or full two-way interoperability.

`npm run test:provisioning`: **33 passed** (11 P08 tests plus 22 authorization-isolation tests). Added operator recovery/reservation ownership, role/branch reassignment and clear semantics, newer enable with stable account/reverification, schema validation of real outbox payloads, same-company cross-source worker foreign key rejection, and an actual forked worker killed after real fixture HTTP success but before completion commit. An independent connection observes running/lease state; unexpired lease cannot be stolen; expiry plus a fresh connection reconciles to ready. No database transaction was mocked.

Self-review added `enabled` to public status so ready/disabled is unambiguous, froze honest command-time issuer status while GET returns current state, required integration.manage for source administration, and strengthened worker subject/source ownership through additive migration 0005. Migration 0004 was already applied locally, so its checksum was preserved rather than rewritten. No external reviewer or owner approval is claimed.

## Failures and corrections retained

- Initial A fixture typecheck found an object index signature, duplicate object property and forged-union typing; fixed before A passed.
- Existing contract/UI-spec checks correctly refused new paths outside the old P07 allowlist. Extended them narrowly to owner-P08 integration-provisioning; all other designed operations remain forbidden. Public Node CLI example needed Node types in its package typecheck. Fixed and rerun.
- First manual API restart lacked explicit TAWSEL_API_HOST; it failed closed. Restarted with documented host/port. No false running-server claim.
- PowerShell's npm shim consumed the forwarded --once flag in one invocation, starting the normal supervised loop, which processed the two users successfully. Interrupted that task-owned loop and added a dedicated provisioning:worker:once script; direct once invocations then reported no due work. No worker is left polling unexpectedly.
- First browser test passed provisioning/login/role/local-disable assertions, then failed because the expected Arabic denial sentence differed from the existing P07 UI. Corrected the assertion to the actual established copy and reran the complete flow. Screenshot review caught an initial loading-state capture; the final screenshot waits for the signed-in notice. No product UI was changed.

## Verification and limits

Final full command (PowerShell): `$env:VITE_TAWSEL_API_BASE_URL='http://127.0.0.1:3001'; npm run check` **PASS**: zero audit vulnerabilities, lint, OpenAPI lint (one retained redirect-only callback warning), generated artifacts, typechecks, **13 files / 289 Vitest tests**, API/web/shared builds and production fixture exclusion. Canonical checks: **7 schema files, 90 valid + 58 invalid examples, 148 operations**. Existing pins retained: Node 24.19.0, npm 11.1.0, PostgreSQL 18.4, Keycloak 26.7.4, Vitest 5.0.1, Playwright 1.63.0, TypeScript 6.0.2; initial prerequisite commands used npm 11.6.2 as recorded above. No dependency upgrade was needed. Primary Keycloak Admin/OIDC documentation was checked and linked in provisioning.md.

Actual final screenshots inspected: `output/playwright/phase-08-provisioned-account.png` and `phase-08-disabled-account.png`, 390×844, Cairo/RTL, readable account/denial states and visible focused recovery/retry controls, no horizontal clipping. They use existing connected P07 UI, not fixture screens. Real phones, owner review, production SMTP/TLS, dedicated production issuer permissions/supervision/load, hosted CI execution and a real ERP connector remain unverified. P07's full six-browser suite was not rerun; its signed-provider/auth regression tests are included in the full 289-test pass and P08 reuses the actual company browser flow. No new UI behavior requires a new design approval.

## Changed artifacts and exact handoff

- Implementation: `apps/api/src/provisioning/{schema,credentials,service,domain,routes,issuer,worker}.ts`, API startup/registration; additive migrations 0004/0005. P05/P06/P07 migration contents preserved.
- Tests/tooling: provisioning-actor, authorization-isolation extension, migration inventory, extended labelled issuer fixture and process-crash helper; public `tests/erp-conformance/provisioning.ts`; real browser spec/config; public client/example and local setup/worker scripts; CI path filters. Private setup/consumer credentials remain ignored.
- Canonical/consumer artifacts: provisioning schema, OpenAPI, valid/invalid examples, operations lifecycle, regenerated API client/reference/coverage, client README; ERP planning/mapping/quickstart, integration/identity/access/operations/consistency guides, phase ledger/index/coverage and master/discovery current status.

Phase 09 may rely on the actual P05 atomic command kernel, P06 live access/resource guards, P07 company/personal Sessions.use/context, and P08 tenant/source/subject/branch/role/exception/driver mappings, retained provisioning POST results, service credential trust and separate issuer status. Its B2C intake/resource/lifecycle code does not exist yet and must enforce its own rules. Human actor delegation, task intake, receiver/source UI and signed delivery are not provided by a P08 service credential. **Phase 09 was not run; no commit, push or publication.**

Final after-review checks: focused provisioning-actor rerun **11 passed** after strengthening the no-duplicate-event assertion; contracts:check passed; provisioning:worker:once exited with no due work. Python UI-spec audit passed all source hashes/action/state/contrast/link and negative mutation checks; git diff --check passed and protected Engine/Stitch/0001–0003 path diff is empty. The existing model-selection guide was consulted; exact picker setting remains unavailable. No external review feedback occurred.

Final link/database audit: **1,198 local file links** in changed Markdown resolve (fragments separately handled by existing document checks); **zero leftover disposable test databases**. An initial inline PowerShell audit command had a quoting/parser error before execution; moved the SQL into a script and reran successfully without printing connection secrets. The P08 API remains running on 127.0.0.1:3001 with the existing local issuer/sink/web server for review; no reconciliation polling worker is left running. Consumer fixture files contain private local secrets and remain ignored. The final captured driver-1 state is deliberately disabled; driver-2 is available and the browser demo is repeatable.
